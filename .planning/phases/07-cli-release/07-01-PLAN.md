---
phase: 07-cli-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/bin/moon-solid
  - packages/cli/index.ts
  - packages/package.json
autonomous: true
requirements: [CLI-01]
user_setup: []

must_haves:
  truths:
    - "bin/moon-solid exists and bin/moon-react no longer exists"
    - "package.json bin field maps moon-solid to bin/moon-solid (value path renamed)"
    - "CLI source has zero user-facing 'moon-react' strings; internal enum renamed MOON_SOLID_ARGS"
    - "A component scaffolded by the CLI is valid Solid source that typechecks against solid-js"
  artifacts:
    - path: "packages/bin/moon-solid"
      provides: "Renamed CLI entrypoint that spawns tsx cli/index.ts"
      contains: "cli"
    - path: "packages/cli/index.ts"
      provides: "CLI arg parser with Solid-named enum"
      contains: "MOON_SOLID_ARGS"
    - path: "packages/package.json"
      provides: "bin field pointing to renamed file"
      contains: "bin/moon-solid"
  key_links:
    - from: "packages/package.json"
      to: "packages/bin/moon-solid"
      via: "bin field value"
      pattern: "\"moon-solid\":\\s*\"bin/moon-solid\""
    - from: "packages/cli/index.ts"
      to: "packages/cli/commands/add.ts"
      via: "addCommand dynamic import copies Solid .tsx source"
      pattern: "import.*commands/add"
---

<objective>
Rename the CLI bin from `moon-react` to `moon-solid` (file + package.json value), apply the cosmetic internal string renames, and prove the CLI scaffolds valid Solid source.

Purpose: Completes CLI-01. The CLI is framework-agnostic file-copy plumbing (verified in 07-CONTEXT D-01/D-02 + RESEARCH ARCHITECTURE.md) — all 37 lib components are already Solid (Phases 1-4), so the CLI emits Solid templates automatically once the bin is renamed. Only the file rename, the `bin` value fix, and a cosmetic enum rename remain.

Output: `packages/bin/moon-solid` (renamed via git mv), updated `bin` field in package.json, `MOON_SOLID_ARGS` enum, and a scaffold-then-typecheck proof that a generated component compiles against solid-js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cli-release/07-CONTEXT.md

<read_first>
- packages/package.json (bin field line 52-54: KEY is "moon-solid", VALUE still "bin/moon-react")
- packages/bin/moon-react (the file to rename — spawns `npx tsx cli/index.ts`, no "react" in runtime path)
- packages/cli/index.ts (enum MOON_REACT_ARGS lines 8-11; referenced lines 15, 21, 41-42)
- packages/cli/helpers.ts (framework-agnostic; MOON_UI_PACKAGE = "@moondesignsystem/ui" is the Moon CSS pkg, NOT this package — leave unchanged)
- packages/cli/commands/add.ts (copyComponent copies Capitalized .tsx from lib src → consumer src/components — framework-agnostic)
- packages/cli/components-meta.ts (component keys lowercase, e.g. `button:` line 90; framework-agnostic)
- packages/cli/directories-constants.ts (path resolution only — no strings to change)
- packages/src/components/Button.tsx (the Solid source the CLI copies for `--add button`)
</read_first>

<interfaces>
CLI flow (do not change public flag strings `--add`, `--add-components`):
- packages/bin/moon-solid (CommonJS): `spawn('npx', ['tsx', cliPath, ...argv])` where cliPath = `../cli/index.ts`
- packages/cli/index.ts: `enum MOON_REACT_ARGS { ADD_COMPONENTS = "--add-components", ADD = "--add" }` — rename enum NAME only, keep string VALUES.
- packages/cli/commands/add.ts: `copyComponent(name, baseDir, destDir)` capitalizes name, copies `{Capitalized}.tsx` from `DIRECTORIES.COMPONENTS` (= `../src/components`) to consumer `src/components`.
- package.json `bin`: `{ "moon-solid": "bin/moon-react" }` → must become `{ "moon-solid": "bin/moon-solid" }`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv bin file and fix package.json bin value</name>
  <files>packages/bin/moon-solid, packages/package.json</files>
  <action>
1. Run `git mv packages/bin/moon-react packages/bin/moon-solid` (preserves history). Do NOT use plain rename/copy — use git mv so the move is tracked.
2. In packages/bin/moon-solid: the content is framework-agnostic (spawns tsx on cli/index.ts). Scan for any comment or string mentioning "moon-react" or "react" and update to "moon-solid"/Solid wording if present. (Current content has none — verify and leave functional code untouched.)
3. In packages/package.json: change the `bin` value from `"moon-solid": "bin/moon-react"` to `"moon-solid": "bin/moon-solid"`. The KEY is already correct; only the VALUE path changes.
  </action>
  <verify>
    <automated>test -f packages/bin/moon-solid && ! test -f packages/bin/moon-react && node -e "const b=require('./packages/package.json').bin; process.exit(b['moon-solid']==='bin/moon-solid'?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/bin/moon-solid` succeeds (file exists)
    - `! test -f packages/bin/moon-react` succeeds (old file gone)
    - package.json bin value is exactly "bin/moon-solid" (node check exits 0)
    - `git status` shows the rename as a tracked move (R), not delete+add
  </acceptance_criteria>
  <done>bin/moon-solid exists, bin/moon-react gone, package.json bin → bin/moon-solid.</done>
</task>

<task type="auto">
  <name>Task 2: Rename internal MOON_REACT_ARGS enum to MOON_SOLID_ARGS</name>
  <files>packages/cli/index.ts</files>
  <action>
In packages/cli/index.ts: rename the enum `MOON_REACT_ARGS` to `MOON_SOLID_ARGS` at its declaration (line 8) and all 3 usage sites (the `.find` comparison ~line 15, the `args[0] === ...ADD` check ~line 21, and the `.includes`/`as` cast ~lines 41-42). This is a cosmetic internal rename only — do NOT change the enum string VALUES `"--add-components"` and `"--add"` (those are the public CLI flags). Verify the rest of cli/ (helpers.ts, components-meta.ts, directories-constants.ts, commands/add.ts) has no "moon-react"/"react"-specific strings: `MOON_UI_PACKAGE = "@moondesignsystem/ui"` in helpers.ts is the Moon CSS package (a separate dependency), not this library — leave it unchanged.
  </action>
  <verify>
    <automated>grep -c "MOON_SOLID_ARGS" packages/cli/index.ts && ! grep -rq "MOON_REACT_ARGS" packages/cli/ && grep -q '"--add-components"' packages/cli/index.ts && grep -q '"--add"' packages/cli/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MOON_SOLID_ARGS" packages/cli/index.ts` returns >= 4 (declaration + 3 usages)
    - `! grep -rq "MOON_REACT_ARGS" packages/cli/` succeeds (zero old references)
    - Public flag strings `--add-components` and `--add` still present in index.ts
    - No "react"-specific string introduced anywhere in cli/ except the unchanged `@moondesignsystem/ui` Moon CSS reference
  </acceptance_criteria>
  <done>Enum renamed MOON_SOLID_ARGS everywhere; public flags unchanged; cli/ confirmed framework-agnostic.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold-then-typecheck a generated Solid component</name>
  <files>(no source changes — verification task; uses a throwaway temp dir)</files>
  <action>
Prove the CLI emits valid Solid source that compiles against solid-js. The CLI copies `packages/src/components/Button.tsx` (already Solid: imports from `solid-js`, uses `mergeProps`/`splitProps`, `class` not `className`). Run a scaffold-then-typecheck:

1. Create a throwaway temp dir OUTSIDE the repo tree (e.g. `$env:TEMP\moon-cli-smoke` on Windows / `$(mktemp -d)` on POSIX).
2. Copy `packages/src/components/Button.tsx`, `packages/src/helpers/mergeClasses.ts`, and `packages/src/types/index.ts` into the temp dir preserving the relative import structure (Button imports `../helpers/mergeClasses` and `../types`) — i.e. mirror `src/components/Button.tsx`, `src/helpers/mergeClasses.ts`, `src/types/index.ts`. This mirrors exactly what `add.ts copyComponent` does (Button's internalDeps pull mergeClasses + types).
3. In the temp dir, install `solid-js@^1.9.13` and `typescript`, write a minimal `tsconfig.json` with `"jsx": "preserve"`, `"jsxImportSource": "solid-js"`, `"moduleResolution": "bundler"`, `"strict": true`, `"noEmit": true`.
4. Run `npx tsc --noEmit` and confirm exit 0 (the scaffolded Button typechecks against solid-js with zero React imports).
5. Confirm the copied Button.tsx contains `from "solid-js"` and `class=` and contains NO `className`/`from "react"`.
6. Clean up the temp dir.

(Alternative if a temp-install is too heavy: run `npx tsc --noEmit` of `packages/src/components/Button.tsx` within the packages workspace which already has solid-js + the jsx-preserve tsconfig — this equivalently proves the scaffolded source compiles against solid-js. Prefer the temp-dir version as it proves the copy in isolation; the in-workspace check is an acceptable fallback. Document which was used in the SUMMARY.)
  </action>
  <verify>
    <automated>grep -q "from \"solid-js\"" packages/src/components/Button.tsx && grep -q "class=" packages/src/components/Button.tsx && ! grep -q "className" packages/src/components/Button.tsx && ! grep -q "from \"react\"" packages/src/components/Button.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Scaffolded Button source imports `from "solid-js"`, uses `class=`, has zero `className` and zero `from "react"`
    - `tsc --noEmit` of the scaffolded component (temp dir OR in-workspace fallback) exits 0 against solid-js
    - Temp dir cleaned up (no smoke artifacts committed to repo)
    - SUMMARY records which method was used and the tsc exit code
  </acceptance_criteria>
  <done>A CLI-scaffolded component is proven to be valid Solid source compiling against solid-js — CLI-01 scaffold-then-typecheck satisfied.</done>
</task>

</tasks>

<verification>
- `test -f packages/bin/moon-solid && ! test -f packages/bin/moon-react`
- package.json bin value === "bin/moon-solid"
- `! grep -rq "MOON_REACT_ARGS" packages/cli/` (zero stale enum refs)
- Public flags `--add` / `--add-components` intact
- Scaffolded Button typechecks against solid-js (tsc exit 0), zero React imports
</verification>

<success_criteria>
CLI-01 (scaffold half) satisfied: bin renamed to moon-solid (file + package.json value), CLI internals Solid-named and confirmed framework-agnostic, and a scaffolded component proven to be valid compiling Solid source.
</success_criteria>

<output>
After completion, create `.planning/phases/07-cli-release/07-01-SUMMARY.md`
</output>
