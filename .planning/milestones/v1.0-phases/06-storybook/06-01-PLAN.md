---
phase: 06-storybook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/package.json
  - docs/.storybook/main.ts
  - docs/.storybook/preview.ts
  - docs/stories/shared/LinksBlock.tsx
  - docs/stories/components/Button.stories.tsx
autonomous: false
requirements: [STORY-01]
user_setup: []

must_haves:
  truths:
    - "docs/ installs and resolves @moondesignsystem/solid via file:../packages built dist"
    - "Storybook framework is storybook-solidjs-vite (no @storybook/react-vite anywhere in docs config)"
    - "The Button story renders a Solid component with working controls under the Solid renderer"
    - "build-storybook exits 0 with the spike config (Button + LinksBlock only)"
    - "a11y, docs, themes, vitest(panel), chromatic addons load without crashing the build"
  artifacts:
    - path: "docs/package.json"
      provides: "Solid toolchain deps; package renamed moon-solid-docs"
      contains: "storybook-solidjs-vite"
    - path: "docs/.storybook/main.ts"
      provides: "framework: storybook-solidjs-vite + import.meta.resolve getAbsolutePath"
      contains: "storybook-solidjs-vite"
    - path: "docs/.storybook/preview.ts"
      provides: "Preview type from storybook-solidjs-vite + createJSXDecorator decorator"
      contains: "createJSXDecorator"
    - path: "docs/stories/shared/LinksBlock.tsx"
      provides: "Solid port of the docs container (class not className, zero react import)"
      contains: "class="
    - path: "docs/stories/components/Button.stories.tsx"
      provides: "Solid CSF Button story (spike target)"
      contains: "storybook-solidjs-vite"
  key_links:
    - from: "docs/stories/components/Button.stories.tsx"
      to: "@moondesignsystem/solid"
      via: "import { Button }"
      pattern: "from .@moondesignsystem/solid."
    - from: "docs/.storybook/main.ts"
      to: "storybook-solidjs-vite"
      via: "framework.name"
      pattern: "name: .storybook-solidjs-vite."
---

<objective>
SPIKE GATE (D-00, BLOCKING). Swap the docs/ Storybook toolchain from React to SolidJS, port ONLY the Button story + the LinksBlock docs container it depends on, then prove `npm run build-storybook` is green BEFORE any of the other 32 stories are ported. This validates the storybook-solidjs-vite@10 renderer, the createJSXDecorator decorator, autodocs, and all 5 addons against the real build.

Purpose: De-risk the highest-risk phase. If the spike build fails (renderer or an addon incompatible), STOP and surface a decision (pin Storybook 9 / remove the offending addon) — do NOT let Wave 2 bulk-port onto a broken base. This plan OWNS the shared config (package.json, main.ts, preview.ts), so it must complete before the parallel story plans run.
Output: A green spike build of docs/ Storybook with one Solid Button story.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-storybook/06-CONTEXT.md
@.planning/phases/06-storybook/06-RESEARCH.md

<read_first>
- .planning/phases/06-storybook/06-RESEARCH.md — Pattern 1 (main.ts verbatim), Pattern 2 (preview.ts verbatim), Pattern 4 (Button story verbatim), Pattern 5 (shared Solid component), Installation block, Pitfalls 1/2/3/6/7
- docs/package.json — current React toolchain to swap
- docs/.storybook/main.ts — current createRequire getAbsolutePath to replace
- docs/.storybook/preview.ts — current decorator to wrap in createJSXDecorator
- docs/stories/shared/LinksBlock.tsx — React FC to port to Solid (className→class, @moondesignsystem/react→/solid in the npm link href)
- docs/stories/components/Button.stories.tsx — React CSF spike target
- packages/package.json — confirms the lib is named @moondesignsystem/solid (file:../packages), exports solid condition
</read_first>

<interfaces>
<!-- Built dist is consumed via file:../packages. The lib's public API for the spike: -->
From @moondesignsystem/solid (packages/src/index.ts):
  export { Button }  // Solid Component<ComponentProps>

ComponentProps comes from 'solid-js' (NOT React.ComponentProps).

main.ts getAbsolutePath (v10 form, from RESEARCH Pattern 1):
  const getAbsolutePath = (packageName: string): string =>
    path.dirname(import.meta.resolve(path.join(packageName, 'package.json'))).replace(/^file:\/\//, '');
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap docs/ toolchain deps + rename package</name>
  <files>docs/package.json</files>
  <read_first>docs/package.json; 06-RESEARCH.md Installation block + Standard Stack versions</read_first>
  <action>
Rewrite docs/package.json per D-01 and RESEARCH Installation block:
- Rename "name": "moon-react-docs" → "moon-solid-docs".
- dependencies: replace "@moondesignsystem/react": "file:../packages" → "@moondesignsystem/solid": "file:../packages"; remove "react"; add "solid-js": "^1.9.13"; replace "@tanstack/react-table": "^8.21.3" → "@tanstack/solid-table": "^8.21.3". Keep @tailwindcss/vite, tailwindcss.
- devDependencies: remove "@storybook/react-vite" and "@vitejs/plugin-react"; add "storybook-solidjs-vite": "^10.1.1" and "vite-plugin-solid": "^2.11.12". Keep all addons (@chromatic-com/storybook, @storybook/addon-a11y, @storybook/addon-docs, @storybook/addon-vitest, @storybook/addon-themes) at their existing ^10.3.3 / ^5.1.1 carets, plus chromatic, typescript, vite.
- Do NOT add @solidjs/web (Solid 2 only — Pitfall 6).
Then build the lib and install docs so the file: link resolves the built dist:
  cd packages && npm run build
  cd docs && npm install   (if npm errors on the @solidjs/web optional peer, retry: npm install --legacy-peer-deps)
  </action>
  <verify>
    <automated>node -e "const p=require('./docs/package.json'); const all={...p.dependencies,...p.devDependencies}; if(p.name!=='moon-solid-docs')throw new Error('name'); if(all.react||all['@moondesignsystem/react']||all['@storybook/react-vite']||all['@vitejs/plugin-react']||all['@tanstack/react-table'])throw new Error('react dep remains'); if(!all['storybook-solidjs-vite']||!all['vite-plugin-solid']||!all['@moondesignsystem/solid']||!all['solid-js']||!all['@tanstack/solid-table'])throw new Error('missing solid dep'); console.log('deps ok')"</automated>
  </verify>
  <done>docs/package.json has zero react/@storybook/react-vite/@vitejs/plugin-react/@tanstack/react-table; has storybook-solidjs-vite, vite-plugin-solid, solid-js, @moondesignsystem/solid, @tanstack/solid-table; package renamed; packages built; docs installed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite main.ts + preview.ts for storybook-solidjs-vite</name>
  <files>docs/.storybook/main.ts, docs/.storybook/preview.ts</files>
  <read_first>06-RESEARCH.md Pattern 1 (main.ts) + Pattern 2 (preview.ts) — use VERBATIM; docs/.storybook/main.ts + preview.ts current files</read_first>
  <action>
main.ts (per RESEARCH Pattern 1 verbatim):
- Remove `createRequire` import + `const require = createRequire(...)`.
- `import type { StorybookConfig } from 'storybook-solidjs-vite'` (was @storybook/react-vite).
- `import path from 'path'`.
- Replace getAbsolutePath body with: `path.dirname(import.meta.resolve(path.join(packageName, 'package.json'))).replace(/^file:\/\//, '')`.
- `framework: { name: 'storybook-solidjs-vite', options: { docgen: {...} } }` per Pattern 1.
- addon-vitest entry becomes `{ name: getAbsolutePath('@storybook/addon-vitest'), options: { cli: false } }`.
- Keep stories globs, staticDirs, managerHead (gtag/icon/style) UNCHANGED.

preview.ts (per RESEARCH Pattern 2 verbatim):
- `import type { Preview } from 'storybook-solidjs-vite'` (was @storybook/react-vite).
- Add `import { createJSXDecorator } from 'storybook-solidjs-vite'`.
- Keep `withThemeByClassName` import + usage unchanged (framework-agnostic).
- Wrap the SECOND decorator (the dir/theme DOM-manipulation one) body in `createJSXDecorator(...)`; KEEP `return Story()` call form (NOT `<Story/>` — Pitfall 3). Body logic identical.
- Keep globalTypes (direction LTR/RTL), parameters (storySort, controls matchers, a11y test:'todo'), tags:['autodocs'] unchanged.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const m=fs.readFileSync('docs/.storybook/main.ts','utf8'); const p=fs.readFileSync('docs/.storybook/preview.ts','utf8'); if(/@storybook\/react-vite/.test(m+p))throw new Error('react-vite remains'); if(/createRequire/.test(m))throw new Error('createRequire remains'); if(!/storybook-solidjs-vite/.test(m)||!/import\.meta\.resolve/.test(m))throw new Error('main.ts not migrated'); if(!/createJSXDecorator/.test(p)||!/storybook-solidjs-vite/.test(p)||!/return Story\(\)/.test(p))throw new Error('preview.ts not migrated'); console.log('config ok')"</automated>
  </verify>
  <done>main.ts framework=storybook-solidjs-vite, import.meta.resolve getAbsolutePath, no createRequire, addon-vitest cli:false; preview.ts Preview from storybook-solidjs-vite, createJSXDecorator wrapping the Story() decorator; zero @storybook/react-vite in either.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Port LinksBlock + Button story to Solid (spike target)</name>
  <files>docs/stories/shared/LinksBlock.tsx, docs/stories/components/Button.stories.tsx</files>
  <read_first>06-RESEARCH.md Pattern 4 (Button story verbatim) + Pattern 5; docs/stories/shared/LinksBlock.tsx + Button.stories.tsx current; packages/src/assets/icons/*.tsx (Solid component pattern precedent)</read_first>
  <action>
LinksBlock.tsx (React FC → Solid, D-05):
- Remove any React import. Keep imports from @storybook/addon-docs/blocks (DocsContainer/DocsPage) and storybook/internal/types (Renderer) and ../utils/component-links.
- Type the component with Solid props: `const LinksBlock = (props: Props) => (...)` — do NOT destructure props; reference props.context / props.component inside JSX (use a local `const { context, component } = props` ONLY if needed for the getMoonLink/getGithubLink calls, acceptable in this non-reactive docs helper, but prefer props.x access).
- Change ALL `className` → `class`. Update the NPM href from .../package/@moondesignsystem/react → /@moondesignsystem/solid.
- Keep the DocsContainer/DocsPage structure and the 3 link cards unchanged otherwise.

Button.stories.tsx (per RESEARCH Pattern 4 VERBATIM):
- `import type { Meta, StoryObj } from 'storybook-solidjs-vite'`.
- `import { Button } from '@moondesignsystem/solid'`.
- `import type { ComponentProps } from 'solid-js'` and `type Type = ComponentProps<typeof Button>`.
- `import LinksBlock from '../shared/LinksBlock'`.
- Keep argTypes + the render fn body (destructure variant/size/context, conditional spread) EXACTLY; render returns `<Button {...buttonProps}>Button</Button>` directly — NO double-wrapper `() => () =>` (Pitfall 1).
- Keep `export const ButtonStory` args unchanged.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const l=fs.readFileSync('docs/stories/shared/LinksBlock.tsx','utf8'); const b=fs.readFileSync('docs/stories/components/Button.stories.tsx','utf8'); if(/className/.test(l))throw new Error('className in LinksBlock'); if(/from .react.|@moondesignsystem\/react|@storybook\/react/.test(b))throw new Error('react import in Button story'); if(!/from .storybook-solidjs-vite.|storybook-solidjs-vite/.test(b)||!/@moondesignsystem\/solid/.test(b))throw new Error('story not solid'); if(/=>\s*\(\s*\)\s*=>/.test(b))throw new Error('double-wrapper render'); console.log('spike files ok')"</automated>
  </verify>
  <done>LinksBlock is class-not-className with no react import; Button story imports from storybook-solidjs-vite + @moondesignsystem/solid, ComponentProps from solid-js, render returns &lt;Button/&gt; directly with no double-wrapper.</done>
</task>

<task type="auto">
  <name>Task 4: Spike build gate — build-storybook green</name>
  <files>docs/package.json</files>
  <read_first>06-RESEARCH.md Validation Architecture + spike success criteria (Pattern 4)</read_first>
  <action>
With ONLY Button.stories + LinksBlock ported (the other 32 component stories + gettingStarted.mdx still import React and will be ported in Wave 2 — but Storybook's stories glob picks them up, so they must NOT break the spike build). Temporarily restrict the spike build to the ported files to isolate the gate:

Run the spike build by temporarily narrowing the stories glob in main.ts to Button only:
  - Option A (preferred, reversible): set an env-guarded or temporary `stories: ['../stories/components/Button.stories.@(ts|tsx)']` for the spike, run `cd docs && npm run build-storybook`, confirm exit 0, then RESTORE the full globs (the full glob is required for Wave 2/3).
  - If exit 0 with the full glob already (because unported stories happen not to break resolution), no narrowing needed — but do not assume; isolate Button.

Capture build exit code. If non-zero: read the error, determine whether it is the renderer, createJSXDecorator, an addon (a11y/docs/themes/vitest/chromatic), or LinksBlock/docs.container — and STOP, surfacing the failure + the candidate decision (pin SB9 / remove addon) rather than proceeding.
After a green spike, RESTORE the full stories globs in main.ts (Wave 2 needs them).
  </action>
  <verify>
    <automated>cd docs && npm run build-storybook</automated>
  </verify>
  <done>`npm run build-storybook` exits 0 with the Button spike; stories globs restored to full in main.ts; no addon crashed the build (addon-vitest missing-Playwright warning is acceptable).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Solid Storybook spike: storybook-solidjs-vite framework swap, createJSXDecorator preview, ported LinksBlock + Button story, green build-storybook.</what-built>
  <how-to-verify>
1. Run `cd docs && npm run dev` (storybook dev -p 6006), open http://localhost:6006.
2. Confirm the Button story renders an actual Moon button (not [object Object] / blank / serialized function).
3. Toggle the Controls: change size and variant — the rendered button updates.
4. Confirm the a11y panel appears (no crash), the Themes toolbar shows light/dark, the Text Direction toolbar shows LTR/RTL, and the Button autodocs page (with the LinksBlock header) renders.
5. Confirm no fatal console errors (an addon-vitest "missing Playwright" warning is acceptable).
If ANY of the renderer/addons is broken, describe the failure so a fallback decision (pin Storybook 9 / remove the offending addon) can be made BEFORE Wave 2 bulk-ports the other 32 stories.
  </how-to-verify>
  <resume-signal>Type "approved" to unlock Wave 2 bulk porting, or describe the spike failure.</resume-signal>
</task>

</tasks>

<verification>
- `cd docs && npm run build-storybook` exits 0 (spike scope).
- grep: zero `@storybook/react-vite` in docs/.storybook/*; zero `react`/`@moondesignsystem/react`/`@storybook/react` in Button.stories.tsx; zero `className` in LinksBlock.tsx.
- Human-verify: Solid Button renders with working controls + all addons load.
</verification>

<success_criteria>
- storybook-solidjs-vite is the framework; createJSXDecorator decorator in preview.ts; Button story is Solid CSF; LinksBlock is Solid.
- build-storybook green; spike human-approved; full stories globs restored for Wave 2.
</success_criteria>

<output>
After completion, create `.planning/phases/06-storybook/06-01-SUMMARY.md`. Note any addon quirks or the --legacy-peer-deps decision for downstream plans.
</output>
