---
phase: 04-compound-portal-composite
plan: 05
type: execute
wave: 2
depends_on: ["04-01", "04-02", "04-03", "04-04"]
files_modified:
  - packages/src/components/index.ts
  - packages/src/index.ts
  - packages/eslint.config.js
  - packages/vitest.config.ts
autonomous: false
requirements: [CMPD-01, CMPD-02, COMP-01]

must_haves:
  truths:
    - "src/index.ts exports ALL 37 components with identical names + exported types"
    - "eslint.config.js no longer ignores any ported component; eslint . runs across every component file"
    - "npm run build emits dual .js + .jsx (preserved-JSX) + .d.ts for all 37 components with zero errors"
    - "vitest run + eslint . are green across the whole package"
    - "Zero React/react-dom imports and zero cloneElement remain across all 37 component source files"
  artifacts:
    - path: "packages/src/index.ts"
      provides: "Complete 37-component public entry"
      contains: "Dialog"
    - path: "packages/src/components/index.ts"
      provides: "Regenerated barrel covering all 37"
      contains: "Authenticator"
    - path: "packages/eslint.config.js"
      provides: "Lint config with the per-phase component ignore-list removed"
  key_links:
    - from: "src/index.ts"
      to: "all 37 component modules"
      via: "explicit re-exports (or export * from ./components)"
      pattern: "Dialog|Drawer|Authenticator"
    - from: "eslint.config.js"
      to: "src/components/**"
      via: "ignore list emptied of component allow-list entries"
      pattern: "components"
---

<objective>
Final wiring + validation gate for Phase 4 (D-07). After all 18 Phase-4 components are ported (plans 01-04), regenerate the barrel, extend src/index.ts to export the FULL 37-component set, remove the eslint per-phase ignore allow-list so every component is linted, then prove the whole package builds (dual JS+JSX+d.ts), tests, and lints green. This is the LAST component-porting phase — after this the 37-set is complete.

Purpose: Make the entire ported library publicly exported and tooling-clean; close CMPD-01, CMPD-02, COMP-01.
Output: Updated barrel + src/index.ts + eslint.config.js; green build+test+lint gate; human verification of the complete 37-set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-compound-portal-composite/04-CONTEXT.md
@packages/src/index.ts
@packages/eslint.config.js
@packages/src/components/index.ts

<interfaces>
<!-- src/index.ts currently exports the Phase 1-3 subset. The 14 Phase-4 files to ADD (with their exported types): -->
- Dialog (no type), Drawer (no type), BottomSheet (no type)
- Dropdown (no type), Menu (MenuSizes), Select (SelectSizes, SelectVariants), Tooltip (TooltipPositions), Snackbar (SnackbarVariants)
- Accordion (AccordionSizes, AccordionVariants), TabList (TabListSizes), Table (TableSizes), List (ListSizes), Pagination (no type), Authenticator (AuthenticatorSizes, AuthenticatorVariants)

The barrel generator: `npm run barrels` (scripts/generate-barrel) regenerates packages/src/components/index.ts. src/index.ts is the hand-maintained public entry (re-exports from ./components/* per Phase 1-3 convention). Per D-07 + Claude's Discretion: either extend the explicit list to all 37 OR switch to `export * from "./components"` (now SAFE — every component is ported, no React imports remain). Prefer the explicit list to keep type re-exports precise and match the established Phase 1-3 style, UNLESS `export *` cleanly re-exports the same names+types.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate barrel, extend src/index.ts to all 37, un-ignore components in eslint</name>
  <files>packages/src/components/index.ts, packages/src/index.ts, packages/eslint.config.js</files>
  <read_first>
    - packages/src/index.ts (current Phase 1-3 export list — the convention to extend)
    - packages/src/components/index.ts (auto-generated barrel — full name+type inventory)
    - packages/eslint.config.js (the ignore allow-list block to remove)
    - .planning/phases/04-compound-portal-composite/04-CONTEXT.md (D-07)
  </read_first>
  <action>
    1. Run `npm run barrels` (from packages/) to regenerate src/components/index.ts so it includes every component. Verify it lists all 14 Phase-4 components with their type re-exports (Dialog, Drawer, BottomSheet, Dropdown, Menu+MenuSizes, Select+SelectSizes/SelectVariants, Tooltip+TooltipPositions, Snackbar+SnackbarVariants, Accordion+AccordionSizes/AccordionVariants, TabList+TabListSizes, Table+TableSizes, List+ListSizes, Pagination, Authenticator+AuthenticatorSizes/AuthenticatorVariants). REQUIRED: the barrel generator does NOT emit `TooltipPositions` (confirmed — it is absent from the current src/components/index.ts). After `npm run barrels`, you MUST hand-add `export type { TooltipPositions } from "./Tooltip";` to src/components/index.ts (and the corresponding `export type { TooltipPositions } from "./components/Tooltip";` to src/index.ts in step 2) so the public API is preserved. This is not conditional — do it every run.
    2. Extend packages/src/index.ts to export ALL 37 components. Add the 14 Phase-4 entries following the EXACT existing style (`export { default as X } from "./components/X"` + `export type { ... } from "./components/X"` where a type exists). Keep alphabetical ordering consistent with the existing file. (Alternatively, if cleaner and verified equivalent, replace the whole body with `export * from "./types";` + `export * from "./components";` — only do this if a build/typecheck confirms identical public names+types are emitted.)
    3. In packages/eslint.config.js, remove the entire per-phase component ignore block: delete the `"src/components/**/*"` glob and ALL the `"!src/components/*.tsx"` allow-list negations (lines covering the Phase 1-3 components) so that NO component file is ignored and `eslint .` lints every component. Keep the other ignores (dist, node_modules, cli, bin, scripts, legacy `src/tests/*.test.tsx` React files, vite/vitest config). The legacy React test ignores under src/tests/ stay (those are Phase 5).
  </action>
  <verify>
    <automated>cd packages; npm run barrels; node -e "const fs=require('fs');const idx=fs.readFileSync('src/index.ts','utf8');const star=/export \* from \".\/components\"/.test(idx);const need=['Dialog','Drawer','BottomSheet','Dropdown','Menu','Select','Tooltip','Snackbar','Accordion','TabList','Table','List','Pagination','Authenticator'];const miss=need.filter(n=>!new RegExp('default as '+n+'\\\\b').test(idx)&&!star);if(miss.length)throw new Error('src/index.ts missing component: '+miss.join(','));const types=['MenuSizes','SelectSizes','SelectVariants','SnackbarVariants','TooltipPositions','AccordionSizes','AccordionVariants','TabListSizes','TableSizes','ListSizes','AuthenticatorSizes','AuthenticatorVariants'];const tmiss=types.filter(t=>!new RegExp('\\\\b'+t+'\\\\b').test(idx)&&!star);if(tmiss.length)throw new Error('src/index.ts missing type export: '+tmiss.join(','));const bar=fs.readFileSync('src/components/index.ts','utf8');if(!/TooltipPositions/.test(bar))throw new Error('barrel index.ts missing TooltipPositions (hand-add required)');const esl=fs.readFileSync('eslint.config.js','utf8');if(/!src\/components\//.test(esl))throw new Error('eslint still has component allow-list negations');if(/\"src\/components\/\*\*\/\*\"/.test(esl))throw new Error('eslint still ignores src/components/**');const vt=fs.readFileSync('vitest.config.ts','utf8');if(!/src\/tests\/atoms/.test(vt))throw new Error('vitest include glob does not cover src/tests/atoms');console.log('wiring OK: 14 components + 12 types + TooltipPositions + vitest glob + eslint lint-all')"</automated>
  </verify>
  <done>Barrel regenerated covering all 37; src/index.ts exports all 37 (names + types); eslint.config.js no longer ignores any component file (allow-list block removed).</done>
</task>

<task type="auto">
  <name>Task 2: Full gate — build (dual) + test + lint green across all 37</name>
  <files>packages/src/components/index.ts, packages/src/index.ts</files>
  <read_first>
    - packages/package.json (build/test/lint script names)
    - .planning/phases/04-compound-portal-composite/04-CONTEXT.md (D-07)
  </read_first>
  <action>
    Run the full validation gate from packages/. Resolve any failures by fixing the offending component source (parity-preserving) — do NOT weaken configs or skip tests.
    1. `npm run build` — must emit dual output (.js + preserved-JSX .jsx + .d.ts) for all 37 components with zero errors. Confirm the `solid` export condition output (.jsx) exists for the new components.
    2. `npx vitest run` — the full suite (toolchain + src/tests/atoms/**) must pass green, including the 18 new Phase-4 tests.
    3. `npx eslint .` — must pass with zero ERRORS across all 37 component files (pre-existing Phase-3 cosmetic warnings noted in STATE.md are acceptable; new component files should aim for zero errors and ideally zero new warnings).
    4. Cross-file invariant sweep — confirm zero React across ALL 37: run a grep gate over src/components for `from "react"`, `react-dom`, `cloneElement`, `React.Children`, `className`.
  </action>
  <verify>
    <automated>cd packages; npm run build && npx vitest run && npx eslint . && node -e "const fs=require('fs');const dir='src/components';const files=fs.readdirSync(dir).filter(f=>f.endsWith('.tsx'));const bad=[];for(const f of files){const s=fs.readFileSync(dir+'/'+f,'utf8');if(/from \"react\"|from 'react'|react-dom/.test(s))bad.push(f+':react');if(/cloneElement|React\.Children/.test(s))bad.push(f+':clone');if(/className/.test(s))bad.push(f+':className');}if(bad.length)throw new Error('VIOLATIONS: '+bad.join(', '));console.log('All '+files.length+' component files React-free, clone-free, className-free')"</automated>
  </verify>
  <done>npm run build (dual JS+JSX+d.ts for 37) green; vitest run green (incl. 18 new tests); eslint . zero errors; grep sweep proves zero React/react-dom/cloneElement/React.Children/className across all 37 component files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 4 complete: all 18 remaining components (Dialog, Drawer, BottomSheet, Snackbar, Tooltip, Dropdown, Menu, Select, Accordion, TabList, Table, List, Pagination, Authenticator) ported to SolidJS. The full 37-component set is now ported, exported from src/index.ts, linted, and passing the dual build + test gate. Drawer.Trigger + BottomSheet.Trigger use display:contents spans; Dropdown uses display:contents; TabList uses createContext — zero cloneElement anywhere. Dialog/Drawer/BottomSheet use signal-ref-via-context + Portal.
  </what-built>
  <how-to-verify>
    1. From packages/ run `npm run build` — confirm it completes with zero errors and emits .js + .jsx + .d.ts (check dist/ for the preserved-JSX `solid` condition output of a Phase-4 component, e.g. Dialog).
    2. Run `npx vitest run` — confirm the full suite passes (toolchain + all atoms/ tests), and confirm the output shows at least 14 new Phase-4 component test files in src/tests/atoms/ (Dialog, Drawer, BottomSheet, Dropdown, Menu, Select, Tooltip, Snackbar, Accordion, TabList, Table, List, Pagination, Authenticator) — a partial run is NOT acceptable.
    3. Run `npx eslint .` — confirm zero errors.
    4. Spot-check parity: open packages/src/components/Drawer.tsx and confirm Trigger is a `<span style={{ display: "contents" }} onClick={...}>` (not cloneElement); open Dialog.tsx and confirm `<Portal mount={document.body}>` + `dialogRef()?.showModal()`.
    5. Confirm src/index.ts exports all 37 components (Dialog through Tooltip present).
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 4 complete, or describe any build/test/lint/parity issue to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package public API → consumers | src/index.ts is the published surface; exporting wrong names/types breaks the framework-swap contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | src/index.ts export surface | mitigate | Automated check asserts all 14 Phase-4 names present; build + d.ts emit verifies type re-exports resolve |
| T-04-10 | Elevation of privilege | eslint un-ignore (removing component guards) | accept | Removing the per-phase ignore allow-list only widens linting; the grep sweep + eslint gate prove no React/cloneElement slipped through |
</threat_model>

<verification>
- src/index.ts exports all 37 components (14 Phase-4 added with correct type re-exports).
- eslint.config.js component ignore allow-list removed; `eslint .` lints every component.
- `npm run build` dual output green; `npx vitest run` green; `npx eslint .` zero errors.
- Grep sweep: zero `from "react"` / `react-dom` / `cloneElement` / `React.Children` / `className` across all src/components/*.tsx.
</verification>

<success_criteria>
- The complete 37-component set is ported, exported, linted, built (dual), and tested green.
- Drawer.Trigger + BottomSheet.Trigger use display:contents span; zero cloneElement anywhere in the package.
- Dialog/Drawer/BottomSheet use signal-ref-via-context + Portal.
- Human verification confirms build + test + lint + parity. Phase 4 done.
</success_criteria>

<output>
After completion, create `.planning/phases/04-compound-portal-composite/04-05-SUMMARY.md`
</output>
