---
phase: 07-cli-release
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified: []
autonomous: false
requirements: [REL-02, CLI-01, REL-01]
user_setup: []

must_haves:
  truths:
    - "Fresh `npm run build` produces dist with index.js + index.jsx + index.d.ts"
    - "`npm publish --dry-run` exits 0; tarball includes dist/cli/bin/moon-solid/README/LICENSE/src and excludes node_modules/tests/.planning"
    - "A smoke consumer resolves the package via the `solid` export condition to dist/index.jsx and a component imports/compiles"
    - "Whole-repo milestone gate green: packages build+test+lint, docs build-storybook, npm publish --dry-run"
  artifacts:
    - path: "packages/dist/index.jsx"
      provides: "Raw-JSX solid export-condition target (shipped in tarball)"
      contains: "solid-js"
  key_links:
    - from: "packages/package.json exports.solid"
      to: "packages/dist/index.jsx"
      via: "solid export condition"
      pattern: "\"solid\":\\s*\"./dist/index.jsx\""
    - from: "npm publish --dry-run tarball"
      to: "bin/moon-solid"
      via: "files[] includes bin"
      pattern: "bin/moon-solid"
---

<objective>
Prove the Solid package publishes cleanly and resolves through the `solid` export condition for a real consumer, then run the whole-repo final milestone gate.

Purpose: Completes REL-02 and the D-07 milestone-complete checkpoint — the LAST plan of the LAST phase. Depends on 07-01 (renamed bin/moon-solid in tarball) and 07-02 (Solid README in tarball). Builds fresh dist, runs `npm publish --dry-run`, validates the tarball file list, proves the `solid` condition resolves to `dist/index.jsx` via a minimal smoke consumer, then re-runs full validation (build+test+lint + build-storybook + dry-run) as the milestone gate, ending in a human-verify checkpoint.

Output: dry-run exit-0 evidence, tarball file-list evidence, smoke-consumer resolution proof, and a green whole-repo milestone gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-cli-release/07-CONTEXT.md
@.planning/research/STACK.md
@.planning/phases/07-cli-release/07-01-SUMMARY.md
@.planning/phases/07-cli-release/07-02-SUMMARY.md

<read_first>
- packages/package.json (exports.solid -> ./dist/index.jsx line 26; files[] lines 31-38: dist, cli, bin, README.md, LICENSE, ./src; build script line 43)
- .planning/research/STACK.md (the `solid` export-condition mechanics — how a Solid+Vite consumer resolves it)
- .planning/phases/07-cli-release/07-01-SUMMARY.md (confirm bin/moon-solid is in place before packing)
- .planning/phases/07-cli-release/07-02-SUMMARY.md (confirm README is Solid before packing)
</read_first>

<interfaces>
- Build: `cd packages && npm run build` -> runs `vite build` + `build-solid-condition.mjs` + `tsc --emitDeclarationOnly`, producing dist/index.js, dist/index.jsx, dist/index.d.ts.
- exports map: `{ ".": { "solid": "./dist/index.jsx", "import": "./dist/index.js", "types": "./dist/index.d.ts" } }`.
- files[]: `["dist", "cli", "bin", "README.md", "LICENSE", "./src"]`.
- Solid-condition resolution: a consumer with `conditions: ["solid"]` (vite-plugin-solid / SolidStart sets this) resolves `@moondesignsystem/solid` to `dist/index.jsx` (raw JSX), NOT `dist/index.js`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh build + npm publish --dry-run + tarball file-list audit</name>
  <files>(no source changes — build + dry-run verification)</files>
  <action>
1. `cd packages && npm run build` — fresh dist. Confirm dist/index.js, dist/index.jsx, dist/index.d.ts all exist after build (the dual-build emits all three).
2. `cd packages && npm publish --dry-run` — must exit 0 with no missing-file / missing-export-condition errors.
3. Capture the dry-run tarball file list (use `npm pack --dry-run --json` or parse the publish dry-run "Tarball Contents" output). Assert the list:
   - INCLUDES: `dist/index.js`, `dist/index.jsx`, `dist/index.d.ts`, the `cli/` files, `bin/moon-solid`, `README.md`, `LICENSE`, and `src/` component sources (so the CLI-copied Solid source ships).
   - EXCLUDES: `node_modules`, `tests`/`src/tests`, anything under `.planning`.
   - Confirm `bin/moon-react` is NOT present (old bin gone).
4. Confirm `exports.solid` in the published package.json points to a file (`dist/index.jsx`) that actually exists in the tarball.
Record the dry-run exit code and the relevant tarball lines in the SUMMARY.
  </action>
  <verify>
    <automated>cd packages && test -f dist/index.js && test -f dist/index.jsx && test -f dist/index.d.ts && npm pack --dry-run --json 2>/dev/null | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const f=JSON.parse(s)[0].files.map(x=>x.path);const need=['dist/index.js','dist/index.jsx','dist/index.d.ts','bin/moon-solid','README.md','LICENSE'];const bad=f.some(p=>p.startsWith('node_modules')||p.includes('.planning')||p==='bin/moon-react');const ok=need.every(n=>f.includes(n))&&f.some(p=>p.startsWith('src/'))&&f.some(p=>p.startsWith('cli/'))&&!bad;process.exit(ok?0:1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` produces dist/index.js + dist/index.jsx + dist/index.d.ts
    - `npm publish --dry-run` exits 0 (no missing-file/export errors)
    - Tarball INCLUDES dist (3 entry files), cli/, bin/moon-solid, README.md, LICENSE, src/
    - Tarball EXCLUDES node_modules, tests, .planning; does NOT contain bin/moon-react
    - exports.solid target (dist/index.jsx) is present in the tarball
  </acceptance_criteria>
  <done>Fresh build emits the dual entry, dry-run exits 0, and the tarball file list is correct (Solid bin + src shipped, junk excluded).</done>
</task>

<task type="auto">
  <name>Task 2: Smoke consumer resolves the `solid` export condition</name>
  <files>(no repo source changes — throwaway smoke consumer outside repo tree)</files>
  <action>
Prove the mandatory `solid` export condition resolves to `dist/index.jsx` (raw JSX) for a real consumer. Pick the LIGHTEST method that genuinely proves it (D-06 discretion):

Preferred (resolution-level, fast):
1. `cd packages && npm pack` to produce `moondesignsystem-solid-3.0.0.tgz`.
2. In a throwaway temp dir OUTSIDE the repo, `npm init -y`, install the tgz + `solid-js@^1.9.13` + `vite` + `vite-plugin-solid`.
3. Write a tiny `import { Button } from "@moondesignsystem/solid"` entry and a Node/Vite resolution check using the `solid` condition (e.g. `import.meta.resolve` with conditions, or a vite build with vite-plugin-solid which sets `conditions:["solid"]`) and assert resolution lands on the package's `dist/index.jsx` (NOT `dist/index.js`).
4. Confirm a `vite build` of the entry compiles (the raw-JSX `.jsx` is transformed by vite-plugin-solid and the Button imports without error). If a full vite build is too heavy, a `node --conditions=solid -e "console.log(require.resolve(...))"`-style / `import.meta.resolve` check that the `solid` condition lands on `index.jsx` is the acceptable minimum.
5. Clean up the temp dir and the .tgz.

This validates D-03's mandatory solid condition end-to-end for a real consumer. Record in the SUMMARY which method was used and the resolved path.
  </action>
  <verify>
    <automated>grep -q '"solid": "./dist/index.jsx"' packages/package.json && cd packages && test -f dist/index.jsx && head -5 dist/index.jsx | grep -q "."</automated>
  </verify>
  <acceptance_criteria>
    - `npm pack` produces a .tgz that installs into a clean consumer
    - Under the `solid` condition, `@moondesignsystem/solid` resolves to `dist/index.jsx` (raw JSX), NOT dist/index.js
    - A component (Button) imports and the consumer compiles/builds (or the resolution check confirms the .jsx target)
    - Temp dir + .tgz cleaned up (no smoke artifacts committed)
    - SUMMARY records the method used and the resolved path
  </acceptance_criteria>
  <done>A real Solid consumer is proven to resolve the package via the `solid` condition to dist/index.jsx and import a component — REL-02 smoke half satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Whole-repo final milestone validation gate</name>
  <files>(no source changes — full validation run)</files>
  <action>
Run the complete D-07 milestone validation across the whole repo (now fully Solid):
1. `cd packages && npm run build` — exit 0.
2. `cd packages && npx vitest run` — full suite green (Phase 5 baseline: 278/278 passing, 35 files).
3. `cd packages && npx eslint .` — 0 errors.
4. `cd docs && npm run build-storybook` — exit 0 (Phase 6 baseline: Storybook 10.4.1, all 37 stories, 267 modules).
5. `cd packages && npm publish --dry-run` — exit 0 (re-confirm post all changes).
Capture each exit code / pass-count in the SUMMARY. If ANY step fails, fix the root cause (own the issue) before proceeding to the checkpoint — do not hand a red gate to the human.
  </action>
  <verify>
    <automated>cd packages && npm run build && npx vitest run && npx eslint . && npm publish --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (packages) exits 0
    - `npx vitest run` passes full suite, zero failures/skips
    - `npx eslint .` reports 0 errors
    - `npm run build-storybook` (docs) exits 0, all 37 stories build
    - `npm publish --dry-run` exits 0
    - All exit codes / counts recorded in SUMMARY
  </acceptance_criteria>
  <done>The entire Solid repo passes build + test + lint + build-storybook + publish-dry-run green — milestone validation complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Milestone-complete checkpoint</name>
  <what-built>
The React->SolidJS migration is complete. CLI bin renamed to moon-solid and proven to scaffold Solid (07-01), README + CHANGELOG Solid-rewritten with a prepared 3.0.0 major changeset (07-02), npm publish --dry-run green with the solid export condition verified in a smoke consumer (07-03 T1-T2), and the whole-repo gate is green: packages build+test+lint, docs build-storybook, publish dry-run (07-03 T3).
  </what-built>
  <how-to-verify>
1. Review the 07-03 SUMMARY: confirm `npm publish --dry-run` exit 0 and the tarball file list (dist .js/.jsx/.d.ts, cli, bin/moon-solid, README, LICENSE, src; no node_modules/tests/.planning).
2. Confirm the smoke-consumer evidence shows the `solid` condition resolving to dist/index.jsx.
3. Confirm the final gate evidence: packages build/test (278/278)/lint(0 errors) green, docs build-storybook exit 0 (37 stories), dry-run exit 0.
4. Optionally run yourself: `cd packages && npm run build && npx vitest run && npx eslint . && npm publish --dry-run` and `cd docs && npm run build-storybook`.
5. Confirm `bin/moon-react` no longer exists and `packages/bin/moon-solid` does.
Note: real `npm publish` is intentionally OUT of scope (deferred to human/CI post-milestone) — only the dry-run is gated here.
  </how-to-verify>
  <resume-signal>Type "approved" to mark the milestone complete, or describe any gap to address.</resume-signal>
</task>

</tasks>

<verification>
- `npm publish --dry-run` exit 0; tarball correct (dist 3 files, cli, bin/moon-solid, README, LICENSE, src; excludes node_modules/tests/.planning)
- Smoke consumer resolves `solid` condition -> dist/index.jsx and imports a component
- Final gate: packages build+test+lint green, docs build-storybook green, dry-run green
- bin/moon-react absent; bin/moon-solid present
</verification>

<success_criteria>
REL-02 + D-07 satisfied: package publishes cleanly (dry-run), the solid export condition resolves end-to-end in a real consumer, the whole-repo milestone gate is green, and the human-verify milestone-complete checkpoint passes — the React->Solid migration milestone is COMPLETE.
</success_criteria>

<output>
After completion, create `.planning/phases/07-cli-release/07-03-SUMMARY.md`
</output>
