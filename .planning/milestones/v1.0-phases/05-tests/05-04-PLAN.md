---
phase: 05-tests
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/src/tests/atoms/Table.test.tsx
  - packages/src/tests/atoms/TabList.test.tsx
  - packages/src/tests/atoms/Tag.test.tsx
  - packages/src/tests/atoms/Tooltip.test.tsx
  - packages/src/tests/atoms/Select.test.tsx
  - packages/src/tests/atoms/Switch.test.tsx
  - packages/src/tests/atoms/Textarea.test.tsx
  - packages/src/tests/Table.test.tsx
  - packages/src/tests/TabList.test.tsx
  - packages/src/tests/Tag.test.tsx
  - packages/src/tests/Tooltip.test.tsx
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "Table, TabList, Tag, Tooltip each have exactly ONE Solid test (atoms/) with legacy behavioral coverage merged"
    - "Select, Switch, Textarea atom tests verified at-parity; Select onChange/onInput parity gap explicitly checked against React source"
    - "No flat legacy file remains for Table, TabList, Tag, Tooltip"
  artifacts:
    - path: "packages/src/tests/atoms/Table.test.tsx"
      provides: "Single Solid Table test merged with legacy row/column-rendering coverage (no tanstack)"
      contains: "render(() =>"
    - path: "packages/src/tests/atoms/TabList.test.tsx"
      provides: "Single Solid TabList test merged with legacy tab-selection coverage"
    - path: "packages/src/tests/atoms/Tag.test.tsx"
      provides: "Single Solid Tag test merged with legacy variant coverage"
    - path: "packages/src/tests/atoms/Tooltip.test.tsx"
      provides: "Single Solid Tooltip test merged with legacy trigger/content coverage"
    - path: "packages/src/tests/atoms/Select.test.tsx"
      provides: "Single Solid Select test asserting onChange callback fires (CR-01 parity-checked)"
  key_links:
    - from: "packages/src/tests/atoms/*.test.tsx"
      to: "packages/src/components/*.tsx"
      via: "render(() => <X/>)"
      pattern: "render\\(\\(\\) =>"
---

<objective>
Migrate and merge the legacy React tests for Table, TabList, Tag, Tooltip into their existing Solid atom tests (one authoritative file each in `src/tests/atoms/`), preserving richer legacy coverage (tab selection, table row/column rendering, variant matrices, tooltip trigger/content). Verify atom-only tests for Select, Switch, Textarea at parity — including an explicit Select onChange-vs-onInput behavioral parity check against the React source (STATE blocker). Delete the 4 flat legacy files.

Purpose: Phase 5 goal — ONE authoritative Solid test per component, parity ≥ legacy (D-01, D-07). Resolves the Phase-4/5 Select parity-gap flag and the CR-01 Select onChange fix verification (CONTEXT specifics).
Output: 7 verified single-source atom test files; 4 flat legacy files deleted; Select onChange/onInput parity decision recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tests/05-CONTEXT.md
@.planning/research/PITFALLS.md

<consolidation_decision>
CHOSEN DIRECTORY: `packages/src/tests/atoms/` (consolidate DOWN). Vitest `include` already covers `src/tests/atoms/**`, so merged tests run immediately — NO config edit here. EDIT the existing `atoms/X.test.tsx`, DELETE the flat legacy `src/tests/X.test.tsx`. DO NOT touch `vitest.config.ts` or `eslint.config.js` — plan 05-05 owns those.
</consolidation_decision>

<translation_rules>
Per D-03 / PITFALLS #14:
- `@testing-library/react` → `@solidjs/testing-library`
- `render(<X/>)` → `render(() => <X/>)` (MANDATORY)
- `jest.fn()` → `vi.fn()`, `jest.spyOn` → `vi.spyOn`; import `{ vi }` from "vitest"
- `className=` → `class=`; keep jest-dom matchers; rely on auto-cleanup (no manual cleanup)
- Reactivity is synchronous — assert immediately after `fireEvent`.
- Controlled inputs (Switch, Select, Textarea): fire `fireEvent.input` (NOT `fireEvent.change`) where the component binds `onInput` (D-03/PITFALLS #9). Exception: native `<select>` that genuinely binds `onChange` — see Select task.
</translation_rules>

<select_parity_note>
STATE blocker (Phase 4→5): the Select component kept native `onChange` (not bridged to `onInput`). CONTEXT specifics say "Select test must now assert onInput fires onChange callback (CR-01 fix)". Before writing the Select assertion you MUST read `packages/src/components/Select.tsx` to confirm which event the component actually binds (`onInput` vs `onChange`), and fire the MATCHING event in the test so the assertion reflects real component behavior. If the component binds `onChange` on a native `<select>`, that is correct (selects fire `change` on selection) — fire `fireEvent.change`. If it binds `onInput`, fire `fireEvent.input`. Record which path was true in the SUMMARY. Do NOT modify the component — this plan only tests; if a genuine behavioral gap is found, flag it in the SUMMARY for a follow-up (do not silently fix).
</select_parity_note>

<interfaces>
Reference Solid test style (already correct — match this):
@packages/src/tests/atoms/Button.test.tsx
Legacy React style being migrated FROM (sample):
@packages/src/tests/accordion.test.tsx
Auto-cleanup + matchers wiring:
@packages/src/tests/setupTests.ts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Merge legacy Table + TabList; verify Switch/Textarea atom parity</name>
  <files>packages/src/tests/atoms/Table.test.tsx, packages/src/tests/atoms/TabList.test.tsx, packages/src/tests/atoms/Switch.test.tsx, packages/src/tests/atoms/Textarea.test.tsx, packages/src/tests/Table.test.tsx, packages/src/tests/TabList.test.tsx</files>
  <read_first>
    - packages/src/tests/Table.test.tsx, packages/src/tests/TabList.test.tsx (legacy coverage to preserve)
    - packages/src/tests/atoms/Table.test.tsx, atoms/TabList.test.tsx (existing Solid coverage to merge INTO)
    - packages/src/tests/atoms/Switch.test.tsx, atoms/Textarea.test.tsx (atom-only — verify)
    - packages/src/components/Table.tsx, TabList.tsx, Switch.tsx, Textarea.tsx (parity targets — Table must have zero @tanstack/react-table; TabList uses createContext)
  </read_first>
  <action>
    - Table: merge legacy row/column/header rendering coverage into the atom file, translated per <translation_rules>. Table source has no tanstack dep — assert real DOM table structure (rows/cells/classes) via `<For>`-rendered output. `git rm src/tests/Table.test.tsx`.
    - TabList: merge legacy tab-selection coverage (clicking a tab → active class / panel switch) into the atom file. TabList uses createContext for selection — fire `fireEvent.click` and assert the active-tab class synchronously. `git rm src/tests/TabList.test.tsx`.
    - Switch, Textarea: atom-only (no legacy). Verify render + class + primary behavior. Switch binds `onInput` (Phase 3 onChange→onInput) — if the test fires a toggle/value change it MUST use `fireEvent.input` not change (D-03). Textarea likewise uses `onInput`. Enrich only if a primary behavior is unasserted. Do NOT delete.
  </action>
  <verify>
    <automated>cd packages && npx vitest run src/tests/atoms/Table.test.tsx src/tests/atoms/TabList.test.tsx src/tests/atoms/Switch.test.tsx src/tests/atoms/Textarea.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The vitest run above exits 0
    - Flat files gone: `test ! -f packages/src/tests/Table.test.tsx && test ! -f packages/src/tests/TabList.test.tsx`
    - `grep -rl "@testing-library/react" packages/src/tests/atoms/Table.test.tsx packages/src/tests/atoms/TabList.test.tsx packages/src/tests/atoms/Switch.test.tsx packages/src/tests/atoms/Textarea.test.tsx` returns nothing
    - `grep -n "jest\." packages/src/tests/atoms/Table.test.tsx packages/src/tests/atoms/TabList.test.tsx` returns nothing
    - Switch/Textarea atom tests use `fireEvent.input` (not `fireEvent.change`) if they fire a value change
    - All four files contain `render(() =>`
  </acceptance_criteria>
  <done>Table/TabList single-source merged ≥ legacy; Switch/Textarea verified (onInput); flat legacy Table/TabList removed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Merge legacy Tag + Tooltip; verify Select atom parity + onChange/onInput parity check</name>
  <files>packages/src/tests/atoms/Tag.test.tsx, packages/src/tests/atoms/Tooltip.test.tsx, packages/src/tests/atoms/Select.test.tsx, packages/src/tests/Tag.test.tsx, packages/src/tests/Tooltip.test.tsx</files>
  <read_first>
    - packages/src/tests/Tag.test.tsx, packages/src/tests/Tooltip.test.tsx (legacy coverage to preserve)
    - packages/src/tests/atoms/Tag.test.tsx, atoms/Tooltip.test.tsx (existing Solid coverage to merge INTO)
    - packages/src/tests/atoms/Select.test.tsx (atom-only — verify + parity-check)
    - packages/src/components/Select.tsx (CRITICAL — confirm onInput vs onChange binding before writing Select assertion), Tag.tsx, Tooltip.tsx (parity targets)
  </read_first>
  <action>
    - Tag: merge legacy variant/size coverage into the atom file, translated per <translation_rules>. `git rm src/tests/Tag.test.tsx`.
    - Tooltip: merge legacy trigger/content coverage into the atom file. Tooltip may use `<Show>` for content visibility — query presence/absence accordingly; if it portals, query `document.body`. `git rm src/tests/Tooltip.test.tsx`.
    - Select: atom-only (no legacy). Follow <select_parity_note> exactly: read `Select.tsx`, determine the actual bound event, fire the matching event (`fireEvent.input` or `fireEvent.change`) against a `vi.fn()` handler, and assert the callback fires with the selected value. This is the CR-01 verification. Record the binding (onInput vs onChange) and whether a behavioral gap vs React exists in the SUMMARY. Do NOT modify the component. Enrich the Select test with render + class assertions if missing. Do NOT delete (no flat legacy file).
  </action>
  <verify>
    <automated>cd packages && npx vitest run src/tests/atoms/Tag.test.tsx src/tests/atoms/Tooltip.test.tsx src/tests/atoms/Select.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The vitest run above exits 0
    - Flat files gone: `test ! -f packages/src/tests/Tag.test.tsx && test ! -f packages/src/tests/Tooltip.test.tsx`
    - `grep -rl "@testing-library/react" packages/src/tests/atoms/Tag.test.tsx packages/src/tests/atoms/Tooltip.test.tsx packages/src/tests/atoms/Select.test.tsx` returns nothing
    - `grep -n "jest\." packages/src/tests/atoms/Tag.test.tsx packages/src/tests/atoms/Tooltip.test.tsx` returns nothing
    - Select atom test fires the event the component actually binds (verified against Select.tsx) against a `vi.fn()` and asserts it fires with the selected value
    - SUMMARY records the Select onChange/onInput binding finding
    - All three files contain `render(() =>`
  </acceptance_criteria>
  <done>Tag/Tooltip single-source merged ≥ legacy; Select verified with documented onChange/onInput parity finding; flat legacy Tag/Tooltip removed.</done>
</task>

</tasks>

<verification>
- `cd packages && npx vitest run src/tests/atoms/` passes for all files this plan touched
- No `@testing-library/react` import, no `jest.` reference in any file this plan owns
- The 4 flat legacy files owned by this plan are deleted
- Select onChange/onInput parity finding documented in SUMMARY
</verification>

<success_criteria>
- Table, TabList, Tag, Tooltip each = ONE Solid test ≥ legacy coverage
- Select, Switch, Textarea atom tests verified at parity (correct controlled-input event)
- Select onChange/onInput parity gap explicitly checked against React source and documented
- 4 flat legacy files removed via git rm
- All touched tests use render(() => <X/>), vi.*, @solidjs/testing-library
</success_criteria>

<output>
After completion, create `.planning/phases/05-tests/05-04-SUMMARY.md` (include the Select onChange/onInput parity finding).
</output>
