---
phase: 06-storybook
plan: 05
type: execute
wave: 2
depends_on: [01]
files_modified:
  - docs/stories/shared/Version.tsx
  - docs/stories/gettingStarted.mdx
  - docs/stories/components/CircularProgress.stories.tsx
  - docs/stories/components/LinearProgress.stories.tsx
  - docs/stories/components/Tag.stories.tsx
  - docs/stories/components/Pagination.stories.tsx
  - docs/stories/components/TabList.stories.tsx
  - docs/stories/components/Select.stories.tsx
autonomous: true
requirements: [STORY-02]
user_setup: []

must_haves:
  truths:
    - "6 stories are Solid CSF (CircularProgress, LinearProgress, Tag, Pagination, TabList, Select)"
    - "Version.tsx is a Solid component with zero react import"
    - "gettingStarted.mdx imports the Solid Version component and renders under Solid autodocs"
  artifacts:
    - path: "docs/stories/shared/Version.tsx"
      provides: "Solid Version component (no react import)"
      contains: "export default Version"
    - path: "docs/stories/gettingStarted.mdx"
      provides: "MDX getting-started page importing the Solid Version"
      contains: "Version"
  key_links:
    - from: "docs/stories/gettingStarted.mdx"
      to: "docs/stories/shared/Version.tsx"
      via: "import Version"
      pattern: "import Version"
---

<objective>
Wave 2 bulk port (batch 4 of 4). The remaining 6 component stories (CircularProgress, LinearProgress, Tag, Pagination, TabList, Select) plus the Version shared component and the gettingStarted.mdx page (which imports Version). Version + mdx are co-located in this plan so the mdx's Version import resolves to a Solid component within one plan's ownership.

Purpose: Mechanical React→Solid CSF translation per D-04/D-05, plus the mdx verification. Runs in parallel with plans 02, 03, 04, 06 (disjoint files).
Output: 6 Solid stories + Solid Version + Solid-compatible mdx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-storybook/06-CONTEXT.md
@.planning/phases/06-storybook/06-RESEARCH.md
@.planning/phases/06-storybook/06-01-SUMMARY.md

<read_first>
- .planning/phases/06-storybook/06-RESEARCH.md — Pattern 3/4, Pattern 5 (shared Solid component), Pitfalls 1/5/7, CSF3+Autodocs+MDX section
- .planning/phases/06-storybook/06-01-SUMMARY.md — proven base + any --legacy-peer-deps note
- docs/stories/components/Button.stories.tsx — ported canonical template
- docs/stories/shared/Version.tsx — React FC (imports `react` + packages/package.json) to port to Solid
- docs/stories/gettingStarted.mdx — MDX page importing Version (already uses `class` not className; verify only the Version import + any React component refs)
- Each story being ported (CircularProgress/LinearProgress/Tag/Pagination/TabList/Select .stories.tsx) — read before porting
</read_first>

<interfaces>
<!-- D-04 base translation (mirror Button.stories.tsx). -->
<!-- Version.tsx (D-05): drop `import React from 'react'`; it just renders <span>v{packageJson.version}</span>. -->
  // Solid Version:  const Version = () => <span>v{packageJson.version}</span>;  export default Version;
<!-- gettingStarted.mdx already uses class-not-className; only ensure `import Version from './shared/Version'` resolves to the Solid component; keep `<Meta title="Getting started"/>` + `<Version/>`. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port Version.tsx to Solid + verify gettingStarted.mdx</name>
  <files>docs/stories/shared/Version.tsx, docs/stories/gettingStarted.mdx</files>
  <read_first>docs/stories/shared/Version.tsx current (imports react); docs/stories/gettingStarted.mdx current; 06-RESEARCH.md Pitfall 7 + MDX section</read_first>
  <action>
Version.tsx (D-05): remove `import React from 'react'`. Keep the JSON import of ../../../packages/package.json and `const Version = () => <span>v{packageJson.version}</span>; export default Version;`. No className present. Result: zero react import.
gettingStarted.mdx: it imports `Meta` from '@storybook/addon-docs/blocks' (framework-agnostic, keep) and `Version` from './shared/Version' (now Solid, keep import as-is). It already uses `class` not `className` in its JSX. Verify no other React-component import exists; if the body text references "Moon React", optionally leave content as-is (cosmetic, out of strict scope) but the `<Version/>` usage must render the Solid component. Do NOT change the Meta import.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const v=fs.readFileSync('docs/stories/shared/Version.tsx','utf8'); const m=fs.readFileSync('docs/stories/gettingStarted.mdx','utf8'); if(/from .react.|import React/.test(v))throw new Error('react in Version'); if(!/export default Version/.test(v))throw new Error('Version export lost'); if(!/import Version/.test(m))throw new Error('mdx Version import lost'); if(/className/.test(m))throw new Error('className in mdx'); console.log('version+mdx ok')"</automated>
  </verify>
  <done>Version.tsx has zero react import and exports default; gettingStarted.mdx imports the Solid Version, uses class not className, keeps the addon-docs Meta import.</done>
</task>

<task type="auto">
  <name>Task 2: Port 6 stories — CircularProgress, LinearProgress, Tag, Pagination, TabList, Select</name>
  <files>docs/stories/components/CircularProgress.stories.tsx, docs/stories/components/LinearProgress.stories.tsx, docs/stories/components/Tag.stories.tsx, docs/stories/components/Pagination.stories.tsx, docs/stories/components/TabList.stories.tsx, docs/stories/components/Select.stories.tsx</files>
  <read_first>each of the 6 story files current; docs/stories/components/Button.stories.tsx; packages/src/components Pagination/TabList/Select Solid APIs</read_first>
  <action>
Apply the D-04 base translation to each (Meta/StoryObj from 'storybook-solidjs-vite'; component from '@moondesignsystem/solid'; ComponentProps from 'solid-js'; render returns &lt;X/&gt; directly, no double-wrapper). Convert any `className`→`class`. Convert any `.map()` over arrays in render JSX to `<For each={...}>{(item)=>...}</For>` (import For from 'solid-js') — Pagination/TabList/Select likely iterate. Keep compound API names (TabList.*, Select.*) and argTypes/args/LinksBlock identical.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for(const c of ['CircularProgress','LinearProgress','Tag','Pagination','TabList','Select']){const f='docs/stories/components/'+c+'.stories.tsx'; const s=fs.readFileSync(f,'utf8'); if(/@storybook\/react|@moondesignsystem\/react|from .react.|import React/.test(s))throw new Error('react in '+c); if(!/storybook-solidjs-vite/.test(s)||!/@moondesignsystem\/solid/.test(s))throw new Error('not solid '+c); if(/className/.test(s))throw new Error('className in '+c); if(/=>\s*\(\s*\)\s*=>/.test(s))throw new Error('double-wrapper '+c);} console.log('batch5 ok')"</automated>
  </verify>
  <done>6 stories are Solid CSF; zero className; zero react import; no double-wrapper; compound APIs preserved.</done>
</task>

</tasks>

<verification>
- grep across this plan's 6 stories: zero `@storybook/react` / `@moondesignsystem/react` / `from "react"` / `import React`; all have `storybook-solidjs-vite` + `@moondesignsystem/solid`; zero `className`; zero double-wrapper render.
- Version.tsx zero react import; mdx imports Solid Version.
</verification>

<success_criteria>
- 6 stories + Version + mdx Solid; disjoint from other wave-2 plans; full build green finalized at Wave 3 gate (06-06).
</success_criteria>

<output>
After completion, create `.planning/phases/06-storybook/06-05-SUMMARY.md`.
</output>
