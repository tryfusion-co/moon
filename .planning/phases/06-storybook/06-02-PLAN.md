---
phase: 06-storybook
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - docs/stories/shared/icons/StarIcon.tsx
  - docs/stories/shared/icons/UserIcon.tsx
  - docs/stories/components/Accordion.stories.tsx
  - docs/stories/components/Alert.stories.tsx
  - docs/stories/components/Avatar.stories.tsx
  - docs/stories/components/Badge.stories.tsx
  - docs/stories/components/Breadcrumb.stories.tsx
  - docs/stories/components/IconButton.stories.tsx
  - docs/stories/components/List.stories.tsx
  - docs/stories/components/Menu.stories.tsx
autonomous: true
requirements: [STORY-02]
user_setup: []

must_haves:
  truths:
    - "8 component stories (Accordion, Alert, Avatar, Badge, Breadcrumb, IconButton, List, Menu) are Solid CSF"
    - "StarIcon and UserIcon shared icons are Solid components (zero react import)"
    - "build-storybook stays green after porting this batch"
  artifacts:
    - path: "docs/stories/shared/icons/StarIcon.tsx"
      provides: "Solid star icon (class not className)"
      contains: "export default StarIcon"
    - path: "docs/stories/components/IconButton.stories.tsx"
      provides: "Solid CSF IconButton story importing StarIcon"
      contains: "storybook-solidjs-vite"
  key_links:
    - from: "docs/stories/components/IconButton.stories.tsx"
      to: "docs/stories/shared/icons/StarIcon.tsx"
      via: "import StarIcon"
      pattern: "import StarIcon"
---

<objective>
Wave 2 bulk port (batch 1 of 4). Convert 8 simpler component stories to Solid CSF and port the StarIcon + UserIcon shared icons. These stories have no local state; the only structural nuance is the 3 StarIcon consumers (IconButton, Menu, List).

Purpose: Mechanical React→Solid CSF translation per D-04/D-05 on the de-risked spike base. Runs in parallel with plans 03-06 (disjoint files).
Output: 8 Solid stories + 2 Solid icons; build-storybook still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-storybook/06-CONTEXT.md
@.planning/phases/06-storybook/06-RESEARCH.md
@.planning/phases/06-storybook/06-01-SUMMARY.md

<read_first>
- .planning/phases/06-storybook/06-RESEARCH.md — Pattern 3 (render signature), Pattern 4 (Button as the canonical Solid CSF template), Pattern 5, Pitfalls 1/5/7
- .planning/phases/06-storybook/06-01-SUMMARY.md — the proven Button story + LinksBlock pattern + any --legacy-peer-deps / addon notes
- docs/stories/components/Button.stories.tsx — the ALREADY-PORTED canonical Solid CSF example to mirror
- docs/stories/shared/LinksBlock.tsx — already-ported Solid shared component (pattern for StarIcon/UserIcon)
- packages/src/assets/icons/*.tsx — Solid icon pattern (class, no react)
- Each story being ported (Accordion/Alert/Avatar/Badge/Breadcrumb/IconButton/List/Menu .stories.tsx) — read before porting
</read_first>

<interfaces>
<!-- Canonical Solid CSF translation (D-04), mirror docs/stories/components/Button.stories.tsx: -->
  import type { Meta, StoryObj } from 'storybook-solidjs-vite';   // was @storybook/react
  import { X } from '@moondesignsystem/solid';                    // was @moondesignsystem/react
  import type { ComponentProps } from 'solid-js';                 // was React.ComponentProps
  type Type = ComponentProps<typeof X>;
  render: (args) => <X {...args} />,                              // NO () => () => double-wrapper
<!-- JSX: className -> class; .map() over reactive arrays -> <For>; StarIcon/UserIcon are local imports. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port StarIcon + UserIcon shared icons to Solid</name>
  <files>docs/stories/shared/icons/StarIcon.tsx, docs/stories/shared/icons/UserIcon.tsx</files>
  <read_first>docs/stories/shared/icons/StarIcon.tsx + UserIcon.tsx current; packages/src/assets/icons/*.tsx Solid pattern</read_first>
  <action>
Both are arrow-function components returning inline SVG with no props. They already use `fill-rule`/`clip-rule` (SVG attrs, Solid-compatible) and no className. Ensure: zero `react` import (they have none — confirm), keep them as `const Icon = () => (<svg>...)` returning Solid JSX, `export default`. If any attribute uses camelCase React form (e.g. fillRule), convert to kebab SVG form. No other change needed — these are framework-pure SVG once React is absent.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for(const f of ['docs/stories/shared/icons/StarIcon.tsx','docs/stories/shared/icons/UserIcon.tsx']){const s=fs.readFileSync(f,'utf8'); if(/from .react.|import React/.test(s))throw new Error('react in '+f); if(/className|fillRule|clipRule/.test(s))throw new Error('react jsx in '+f);} console.log('icons ok')"</automated>
  </verify>
  <done>Both icons render Solid SVG; zero react import; SVG attrs in kebab form; no className.</done>
</task>

<task type="auto">
  <name>Task 2: Port 4 stateless stories — Accordion, Alert, Avatar, Badge, Breadcrumb</name>
  <files>docs/stories/components/Accordion.stories.tsx, docs/stories/components/Alert.stories.tsx, docs/stories/components/Avatar.stories.tsx, docs/stories/components/Badge.stories.tsx, docs/stories/components/Breadcrumb.stories.tsx</files>
  <read_first>each of the 5 story files current; docs/stories/components/Button.stories.tsx (template)</read_first>
  <action>
For each story apply the D-04 translation mirroring the ported Button story: Meta/StoryObj from 'storybook-solidjs-vite'; component import from '@moondesignsystem/solid'; `type Type = ComponentProps<typeof X>` with ComponentProps from 'solid-js'; render bodies return `<X .../>` directly (NO double-wrapper); convert any `className`→`class`; convert any `.map()` over arrays in render JSX to `<For each={...}>{(item)=>...}</For>` (import For from 'solid-js') — required for reactivity (Pitfall 5); keep LinksBlock docs.container parameters and argTypes/args structurally identical. Avatar: the lib provides its own User-icon fallback internally — do NOT add UserIcon here unless the source story imports it (it does not).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for(const c of ['Accordion','Alert','Avatar','Badge','Breadcrumb']){const f='docs/stories/components/'+c+'.stories.tsx'; const s=fs.readFileSync(f,'utf8'); if(/@storybook\/react|@moondesignsystem\/react|from .react.|import React/.test(s))throw new Error('react in '+c); if(!/storybook-solidjs-vite/.test(s)||!/@moondesignsystem\/solid/.test(s))throw new Error('not solid '+c); if(/className/.test(s))throw new Error('className in '+c); if(/=>\s*\(\s*\)\s*=>/.test(s))throw new Error('double-wrapper '+c);} console.log('batch ok')"</automated>
  </verify>
  <done>5 stories import from storybook-solidjs-vite + @moondesignsystem/solid; zero react/@storybook/react; no className; no double-wrapper render.</done>
</task>

<task type="auto">
  <name>Task 3: Port 3 StarIcon stories — IconButton, List, Menu — and batch-build gate</name>
  <files>docs/stories/components/IconButton.stories.tsx, docs/stories/components/List.stories.tsx, docs/stories/components/Menu.stories.tsx</files>
  <read_first>each of the 3 story files current; docs/stories/shared/icons/StarIcon.tsx (now Solid); docs/stories/components/Button.stories.tsx</read_first>
  <action>
Apply the same D-04 translation. Each imports `StarIcon from '../shared/icons/StarIcon'` (keep the import — StarIcon is now Solid). Convert `className`→`class`, `.map()`→`<For>` where present in render JSX. Keep `<StarIcon />` usages. Then run a batch build to confirm this plan's files don't break the build (the OTHER wave-2 plans' stories may still be React at this point — if the build picks up un-ported sibling stories and fails on them, that is expected and is gated by plan 06; for THIS plan's verification, the automated grep gates below are authoritative; the full-glob build green is enforced by the Wave 3 final gate plan 06-06).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for(const c of ['IconButton','List','Menu']){const f='docs/stories/components/'+c+'.stories.tsx'; const s=fs.readFileSync(f,'utf8'); if(/@storybook\/react|@moondesignsystem\/react|from .react.|import React/.test(s))throw new Error('react in '+c); if(!/storybook-solidjs-vite/.test(s)||!/@moondesignsystem\/solid/.test(s))throw new Error('not solid '+c); if(!/import StarIcon/.test(s))throw new Error('StarIcon import lost '+c); if(/className/.test(s))throw new Error('className in '+c);} console.log('staricon batch ok')"</automated>
  </verify>
  <done>IconButton, List, Menu are Solid CSF, still import the (now Solid) StarIcon, no className, no react import.</done>
</task>

</tasks>

<verification>
- grep across this plan's 8 stories: zero `@storybook/react` / `@moondesignsystem/react` / `from "react"` / `import React`; all have `storybook-solidjs-vite` + `@moondesignsystem/solid`; zero `className`; zero double-wrapper render.
- StarIcon + UserIcon: zero react import, SVG kebab attrs.
</verification>

<success_criteria>
- 8 stories + 2 icons fully Solid; disjoint from other wave-2 plans; build-storybook green is finalized at the Wave 3 gate (06-06).
</success_criteria>

<output>
After completion, create `.planning/phases/06-storybook/06-02-SUMMARY.md`.
</output>
