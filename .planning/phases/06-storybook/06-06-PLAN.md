---
phase: 06-storybook
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - docs/stories/components/Table.stories.tsx
autonomous: false
requirements: [STORY-02]
user_setup: []

must_haves:
  truths:
    - "Table story uses @tanstack/solid-table (createSolidTable, createSignal, <For>) — zero @tanstack/react-table"
    - "All 37 ported files are Solid: zero @storybook/react, zero @moondesignsystem/react, zero bare react imports across docs/stories"
    - "Full build-storybook (all globs, all 33 component stories + mdx) exits 0"
    - "Storybook dev renders all stories incl Table tanstack example + getting-started mdx with all addons loaded"
  artifacts:
    - path: "docs/stories/components/Table.stories.tsx"
      provides: "Solid CSF Table story with @tanstack/solid-table"
      contains: "createSolidTable"
  key_links:
    - from: "docs/stories/components/Table.stories.tsx"
      to: "@tanstack/solid-table"
      via: "import { createSolidTable }"
      pattern: "@tanstack/solid-table"
---

<objective>
Wave 3 — final gate (D-06 + D-07). Port the most complex single story (Table → @tanstack/solid-table) using the RESEARCH-provided Solid Table example, then run the FULL build-storybook with all 37 ported files and a suite-wide grep sweep proving zero React remnants. Human-verify the complete docs site.

Purpose: Close STORY-02 — the Table tanstack conversion plus the all-stories-green build gate. This plan runs AFTER all wave-2 story batches so the full-glob build covers every ported file at once.
Output: Solid Table story + green full build-storybook with all 37 + human approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-storybook/06-CONTEXT.md
@.planning/phases/06-storybook/06-RESEARCH.md
@.planning/phases/06-storybook/06-01-SUMMARY.md
@.planning/phases/06-storybook/06-02-SUMMARY.md
@.planning/phases/06-storybook/06-03-SUMMARY.md
@.planning/phases/06-storybook/06-04-SUMMARY.md
@.planning/phases/06-storybook/06-05-SUMMARY.md

<read_first>
- .planning/phases/06-storybook/06-RESEARCH.md — "Complete Solid Table Story" (use VERBATIM), the React v8→Solid v8 mapping table, Pitfalls 4 (no v9 alpha) & 5 (<For> not .map), Validation Architecture
- docs/stories/components/Table.stories.tsx — current React tanstack story (two stories: simple Table + TableWithTanstackTable; preserve both + the docs.description source block)
- docs/stories/components/Button.stories.tsx — ported canonical template
- All 06-0{2..5}-SUMMARY.md — confirm each wave-2 batch landed + any addon/peer-dep notes
- packages/src/components Table compound exports (Table.Head/Row/HeadCell/Body/Cell)
</read_first>

<interfaces>
<!-- D-06: @tanstack/react-table -> @tanstack/solid-table (v8.21.3, NOT v9 alpha — Pitfall 4): -->
  import { createColumnHelper, flexRender, getCoreRowModel, createSolidTable } from '@tanstack/solid-table';
  import { createSignal, For } from 'solid-js';
  // useReactTable -> createSolidTable ; useState -> createSignal ; data: data -> get data() { return data() }
  // .map() over header groups / rows / cells -> <For each={...}>{(x)=>...}</For>
<!-- The full ready-to-use Solid Table story is in 06-RESEARCH.md "Complete Solid Table Story" — adapt it to match the source story's two exports + the docs.description.source block. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port Table.stories.tsx to @tanstack/solid-table</name>
  <files>docs/stories/components/Table.stories.tsx</files>
  <read_first>06-RESEARCH.md "Complete Solid Table Story" (verbatim source) + mapping table + Pitfalls 4/5; docs/stories/components/Table.stories.tsx current</read_first>
  <action>
Rewrite Table.stories.tsx per the RESEARCH "Complete Solid Table Story" example:
- Imports: Meta/StoryObj from 'storybook-solidjs-vite'; `Table as TableComponent` from '@moondesignsystem/solid'; ComponentProps from 'solid-js'; `createSignal, For` from 'solid-js'; `createColumnHelper, flexRender, getCoreRowModel, createSolidTable` from '@tanstack/solid-table'; LinksBlock.
- Keep BOTH exports: the simple `Table` story (render with rows/cols arrays → use `<For>` not `.map()`) and `TableWithTanstackTable`.
- TanstackTableExample: `useReactTable`→`createSolidTable` with `get data() { return data(); }`; `useState(() => [...])`→`createSignal([...])`; all `.map()` over getHeaderGroups/headers/rows/cells → `<For each={...}>{(x)=>...}</For>`; remove `key` props (not needed on <For>). Columns/columnHelper defined outside or inside per RESEARCH (static columns can live module-scope).
- Preserve the `docs.description.story` text on both stories and the `docs.description.source.code` block (it is a string literal — update its import lines to the solid-table equivalents so the displayed source matches the actual Solid code).
- Zero @tanstack/react-table, zero useState, zero useReactTable, zero `.map(` in render JSX, zero className.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('docs/stories/components/Table.stories.tsx','utf8'); if(/@tanstack\/react-table|useReactTable|from .react.|import React|@storybook\/react|@moondesignsystem\/react/.test(s))throw new Error('react/react-table remnant'); if(!/@tanstack\/solid-table/.test(s)||!/createSolidTable/.test(s)||!/get data\(\)/.test(s)||!/createSignal/.test(s)||!/<For /.test(s))throw new Error('solid-table not wired'); if(/className/.test(s))throw new Error('className in Table'); console.log('table ok')"</automated>
  </verify>
  <done>Table story imports @tanstack/solid-table (createSolidTable + getter + createSignal + &lt;For&gt;), zero react-table/useState/useReactTable, both story exports preserved, no className.</done>
</task>

<task type="auto">
  <name>Task 2: Suite-wide React-remnant sweep + full build-storybook gate</name>
  <files>docs/stories/components/Table.stories.tsx</files>
  <read_first>06-RESEARCH.md Validation Architecture; all 06-0{1..5}-SUMMARY.md</read_first>
  <action>
Run the suite-wide grep sweep across docs/stories and docs/.storybook to prove zero React remnants, then the full build:
1. Grep sweep (must all be zero): `@storybook/react`, `@moondesignsystem/react`, `@tanstack/react-table`, `import React`, `from "react"`/`from 'react'`, `className`, `useState`, double-wrapper `=> () =>` in render fns, across docs/stories/**/* and docs/.storybook/*.
2. Confirm main.ts stories globs are the FULL set (restored after the spike) so all 33 component stories + mdx are built.
3. Run `cd docs && npm run build-storybook` — must exit 0 with all stories.
If the build fails on any story, fix the offending file (it belongs to a wave-2 batch but THIS gate owns making the full build green). If a wave-2 grep gate passed but the build reveals a Solid runtime issue (e.g. a missed `.map()`→`<For>` or a signal read), correct it here.
  </action>
  <verify>
    <automated>cd docs && npm run build-storybook</automated>
  </verify>
  <done>Full build-storybook exits 0 with all 33 component stories + gettingStarted.mdx; suite-wide grep shows zero @storybook/react, @moondesignsystem/react, @tanstack/react-table, react imports, className, useState across docs/stories + docs/.storybook.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Solid Storybook: all 33 component stories ported to Solid CSF, Table on @tanstack/solid-table, getting-started mdx, 4 shared files Solid, all 5 addons — build-storybook green.</what-built>
  <how-to-verify>
1. Run `cd docs && npm run dev`, open http://localhost:6006.
2. Spot-check a sample across batches: Button, Snackbar (createSignal state toggles), Dialog (portal opens), Table both stories (simple + TanStack example renders rows/cells), IconButton (StarIcon shows), Select/Pagination/TabList.
3. Open the "Getting started" mdx page — it renders and the Version (`v3.0.0`) shows.
4. Confirm Controls update the rendered components; a11y panel, Themes (light/dark), Text Direction (LTR/RTL) toolbars all work; no fatal console errors (addon-vitest missing-Playwright warning acceptable).
5. Confirm `storybook-static/` was produced by the green build-storybook.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 6, or describe any broken story/addon.</resume-signal>
</task>

</tasks>

<verification>
- `cd docs && npm run build-storybook` exits 0 with all 37 (33 stories + mdx + shared) Solid.
- Suite-wide grep: zero @storybook/react, @moondesignsystem/react, @tanstack/react-table, react imports, className, useState, double-wrapper across docs/stories + docs/.storybook.
- Table uses @tanstack/solid-table (createSolidTable + <For>).
- Human-verify: full site renders, all addons load.
</verification>

<success_criteria>
- All 37 stories Solid CSF; Table on @tanstack/solid-table; build-storybook green; phase human-approved.
- @storybook/react-vite and all React deps removed from docs/ (verified in 06-01 + here).
</success_criteria>

<output>
After completion, create `.planning/phases/06-storybook/06-06-SUMMARY.md` and mark Phase 6 done. Note any deferred CI/chromatic-workflow follow-ups for Phase 7.
</output>
