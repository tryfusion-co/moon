---
phase: 03-stateful-atoms-carousel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/src/components/Checkbox.tsx
  - packages/src/components/Input.tsx
  - packages/src/components/Textarea.tsx
  - packages/src/tests/atoms/Checkbox.test.tsx
  - packages/src/tests/atoms/Input.test.tsx
  - packages/src/tests/atoms/Textarea.test.tsx
autonomous: true
requirements: [FORM-01]

must_haves:
  truths:
    - "Checkbox renders a bare checkbox input (no label) with class moon-checkbox, and a label-wrapped variant when label prop is set"
    - "Input renders an input with class moon-input plus size/variant/error modifiers in the exact React order"
    - "Textarea renders a textarea with class moon-textarea plus size/variant/error modifiers in the exact React order"
    - "All three forward arbitrary native attributes via {...rest} with no prop destructuring"
    - "No file imports from \"react\"; every JSX attribute uses class not className"
  artifacts:
    - path: "packages/src/components/Checkbox.tsx"
      provides: "Solid Checkbox pass-through with label/bare branches"
      contains: "splitProps"
    - path: "packages/src/components/Input.tsx"
      provides: "Solid Input pass-through with size/variant/error"
      contains: "splitProps"
    - path: "packages/src/components/Textarea.tsx"
      provides: "Solid Textarea pass-through with size/variant/error"
      contains: "splitProps"
    - path: "packages/src/tests/atoms/Checkbox.test.tsx"
      provides: "render + class-parity assertions for both branches"
    - path: "packages/src/tests/atoms/Input.test.tsx"
      provides: "render + class-parity assertions"
    - path: "packages/src/tests/atoms/Textarea.test.tsx"
      provides: "render + class-parity assertions"
  key_links:
    - from: "packages/src/components/Input.tsx"
      to: "packages/src/helpers/mergeClasses.ts"
      via: "inline class={mergeClasses(...)}"
      pattern: "class=\\{mergeClasses"
---

<objective>
Port the three trivial pass-through form atoms — Checkbox, Input, Textarea — from React to SolidJS using the Phase-2 canonical pattern (mergeProps + splitProps + inline class + {...rest}). Add minimal per-component Solid render/class-parity tests.

Purpose: These are the lowest-complexity FORM-01 components — pure native-input wrappers with no internal state. They establish that the Phase-2 atom pattern transfers verbatim to form inputs.
Output: 3 ported component files + 3 test files, all React-free, class-parity preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-stateful-atoms-carousel/03-CONTEXT.md
@.planning/phases/02-stateless-atoms/02-CONTEXT.md

<read_first>
MANDATORY before editing — read each:
- packages/src/components/Checkbox.tsx — React source (parity target: label branch + bare branch, Omit<...,"type">)
- packages/src/components/Input.tsx — React source (size/variant/error, Omit<...,"size">, modifier order)
- packages/src/components/Textarea.tsx — React source (size/variant/error on <textarea>, modifier order)
- packages/src/components/Chip.tsx — reference Solid atom (splitProps + inline class pattern)
- packages/src/components/Alert.tsx — reference Solid Component<Props> typing
- packages/src/tests/atoms/Chip.test.tsx, packages/src/tests/atoms/Alert.test.tsx — reference Solid test style (render(() => ...), container.querySelector, className equality)
</read_first>

<interfaces>
Canonical Solid atom shape (from Chip.tsx — replicate, do NOT destructure):
```tsx
import { mergeProps, splitProps, type Component, type JSX } from "solid-js";
import mergeClasses from "../helpers/mergeClasses";

const X: Component<XProps> = (props) => {
  const merged = mergeProps({ /* defaults */ } as const, props);
  const [local, rest] = splitProps(merged, ["class", /* local keys */]);
  return <el class={mergeClasses("moon-x", /* modifiers in React order */, local.class)} {...rest} />;
};
export default X;
```

Type mappings (from 02-CONTEXT D-04):
- React.ComponentProps<"input"> → JSX.InputHTMLAttributes<HTMLInputElement>
- React.ComponentProps<"textarea"> → JSX.TextareaHTMLAttributes<HTMLTextAreaElement>
- Omit<React.ComponentProps<"input">, "size"> → Omit<JSX.InputHTMLAttributes<HTMLInputElement>, "size">
- className?: string → class?: string

Preserve exported type names verbatim (barrel depends on them): InputSizes, InputVariants, TextareaSizes, TextareaVariants. Checkbox exports no extracted types.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port Checkbox.tsx to Solid (label + bare branches)</name>
  <files>packages/src/components/Checkbox.tsx, packages/src/tests/atoms/Checkbox.test.tsx</files>
  <read_first>packages/src/components/Checkbox.tsx (React source), packages/src/tests/atoms/Chip.test.tsx (test style)</read_first>
  <behavior>
    - With no label: renders bare `<input type="checkbox">` with class exactly "moon-checkbox"; appends caller `class` last ("moon-checkbox extra")
    - With label="L": renders `<label>` wrapping `<input type="checkbox" class="moon-checkbox">` + `<span>L</span>`; the label's class is the caller's class verbatim (React sets className={className} on the label, no moon- base)
    - Forwards rest props (e.g. checked, name, aria-label) onto the input via {...rest}
  </behavior>
  <action>
    Replace the React import with `import { mergeProps, splitProps, type Component, type JSX } from "solid-js"`. Keep `import mergeClasses from "../helpers/mergeClasses"`.
    Type: `type CheckboxProps = Omit<JSX.InputHTMLAttributes<HTMLInputElement>, "type"> & { label?: string; class?: string };`.
    Component: `const Checkbox: Component<CheckboxProps> = (props) => { const [local, rest] = splitProps(props, ["class", "label"]); ... }`. No mergeProps needed (no defaults).
    Preserve BOTH React branches exactly (use a plain ternary per D-03/Discretion):
    - if (local.label): return `<label class={local.label ? local.class : local.class}>` — match React: the with-label branch sets `className={className}` on the `<label>` (bare class, no moon- base) and `className="moon-checkbox"` (static, no caller class) on the input, then `<span>{local.label}</span>`. Spread {...rest} on the input.
    - else: return bare `<input type="checkbox" class={mergeClasses("moon-checkbox", local.class)} {...rest} />`.
    IMPORTANT class parity: in the label branch the input class is the STATIC string "moon-checkbox" (React did NOT merge className into the input there), and the caller class goes on the `<label>`. In the bare branch the caller class merges into the input. Do not "fix" this asymmetry — it is the parity contract.
    Drop `Checkbox.displayName`. `export default Checkbox`.
    Write Checkbox.test.tsx in src/tests/atoms/ mirroring Chip.test.tsx style: assert bare-input class === "moon-checkbox", bare-input with class="extra" === "moon-checkbox extra", label branch renders label>input.moon-checkbox + span text, and rest prop (e.g. checked) lands on the input.
  </action>
  <verify>
    <automated>cd packages; npx vitest run src/tests/atoms/Checkbox.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages; grep -c 'from "react"' src/components/Checkbox.tsx` returns 0
    - `cd packages; grep -c 'className' src/components/Checkbox.tsx` returns 0
    - `cd packages; grep -c 'splitProps' src/components/Checkbox.tsx` returns >= 1
    - `cd packages; npx vitest run src/tests/atoms/Checkbox.test.tsx` passes
  </acceptance_criteria>
  <done>Checkbox.tsx is React-free, uses splitProps + inline class, preserves both branches with exact class parity, and Checkbox.test.tsx passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port Input.tsx and Textarea.tsx to Solid</name>
  <files>packages/src/components/Input.tsx, packages/src/components/Textarea.tsx, packages/src/tests/atoms/Input.test.tsx, packages/src/tests/atoms/Textarea.test.tsx</files>
  <read_first>packages/src/components/Input.tsx, packages/src/components/Textarea.tsx (React sources — copy modifier order exactly), packages/src/components/Chip.tsx</read_first>
  <behavior>
    - Input default (size="md", variant="fill", error=false): class === "moon-input"; with size="lg" → "moon-input moon-input-lg"; with variant="outline" → "moon-input moon-input-outline"; with error → "moon-input moon-input-error"; combined preserves order base→size→variant→error→caller class
    - Textarea identical with "moon-textarea" base; renders a `<textarea>` element
    - The `type` prop on Input is forwarded (React reads it explicitly and re-applies it); keep it forwarded via {...rest} after splitting, or split it and re-apply — match React (React splits type and sets type={type})
  </behavior>
  <action>
    Input.tsx: import `{ mergeProps, splitProps, type Component, type JSX } from "solid-js"`. Keep mergeClasses + `import type { Sizes, Variants } from "../types"`. Preserve `export type InputSizes = Extract<Sizes, "sm" | "md" | "lg" | "xl">` and `export type InputVariants = Extract<Variants, "fill" | "outline">` UNCHANGED.
    Type: `type InputProps = Omit<JSX.InputHTMLAttributes<HTMLInputElement>, "size"> & { size?: InputSizes; variant?: InputVariants; error?: boolean; class?: string };`.
    Body: `const merged = mergeProps({ size: "md", variant: "fill", error: false } as const, props); const [local, rest] = splitProps(merged, ["class", "type", "size", "variant", "error"]);` then return `<input type={local.type} class={mergeClasses("moon-input", local.size !== "md" && \`moon-input-${local.size}\`, local.variant !== "fill" && \`moon-input-${local.variant}\`, local.error && "moon-input-error", local.class)} {...rest} />`. Modifier order MUST match React: size, variant, error, class.
    Textarea.tsx: same pattern. import solid primitives; preserve `export type TextareaSizes`/`TextareaVariants`. Type from `JSX.TextareaHTMLAttributes<HTMLTextAreaElement>` (React used full ComponentProps<"textarea"> with no Omit — do NOT Omit size here unless React did; React's Textarea does NOT Omit, so keep `JSX.TextareaHTMLAttributes<HTMLTextAreaElement> & { size?: TextareaSizes; variant?: TextareaVariants; error?: boolean; class?: string }`). Note: HTMLTextAreaElement has no native `size`, so no Omit needed — confirm against React source which used no Omit. splitProps keys: ["class", "size", "variant", "error"]. Render `<textarea class={mergeClasses("moon-textarea", ...)} {...rest} />`.
    Drop displayName on both. Default exports.
    Write Input.test.tsx + Textarea.test.tsx in src/tests/atoms/: assert default class, each modifier individually, combined order, and caller class appended last (mirror Chip.test.tsx assertions).
  </action>
  <verify>
    <automated>cd packages; npx vitest run src/tests/atoms/Input.test.tsx src/tests/atoms/Textarea.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages; grep -c 'from "react"' src/components/Input.tsx` returns 0 and same for Textarea.tsx
    - `cd packages; grep -c 'className' src/components/Input.tsx` returns 0 and same for Textarea.tsx
    - `cd packages; grep -c 'splitProps' src/components/Input.tsx` and Textarea.tsx each return >= 1
    - `cd packages; grep -c 'export type InputSizes' src/components/Input.tsx` returns 1 (exported type name preserved)
    - `cd packages; npx vitest run src/tests/atoms/Input.test.tsx src/tests/atoms/Textarea.test.tsx` passes
  </acceptance_criteria>
  <done>Input.tsx and Textarea.tsx are React-free, modifier class order matches React exactly, exported type names preserved, and both test files pass.</done>
</task>

</tasks>

<verification>
- `cd packages; npx vitest run src/tests/atoms/Checkbox.test.tsx src/tests/atoms/Input.test.tsx src/tests/atoms/Textarea.test.tsx` all green
- No `from "react"` and no `className` in any of the 3 component files
- Exported type names (InputSizes, InputVariants, TextareaSizes, TextareaVariants) unchanged so the barrel regeneration in the final plan finds them
</verification>

<success_criteria>
Checkbox, Input, Textarea render correct DOM with class-parity to React, forward native attrs via {...rest}, use no React imports and no prop destructuring, and pass their Solid tests. eslint un-ignore + barrel wiring are handled by 03-05 (final plan) — do NOT edit eslint.config.js, vitest.config.ts, components/index.ts, or src/index.ts here.
</success_criteria>

<output>
After completion, create `.planning/phases/03-stateful-atoms-carousel/03-01-SUMMARY.md`
</output>
