---
phase: 02-stateless-atoms
plan: 05
type: execute
wave: 2
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - packages/src/components/index.ts
  - packages/src/index.ts
  - packages/eslint.config.js
  - packages/vitest.config.ts
autonomous: false
requirements: [ATOM-01]

must_haves:
  truths:
    - "components/index.ts barrel re-exports all 12 ported atoms with their unchanged type names"
    - "src/index.ts re-exports the real components barrel (no longer the Phase-1 stub)"
    - "eslint lints the 12 ported atoms and solid/no-destructure reports zero violations"
    - "vitest runs all src/tests/atoms/*.test.tsx and they pass"
    - "npm run build emits dual output (.js compiled + .jsx raw-JSX + .d.ts) for the ported atoms"
  artifacts:
    - path: "packages/src/components/index.ts"
      provides: "regenerated barrel exporting the 12 atoms"
      contains: "export { default as Button }"
    - path: "packages/src/index.ts"
      provides: "library entry repointed to ./components"
      contains: "./components"
  key_links:
    - from: "packages/src/index.ts"
      to: "packages/src/components/index.ts"
      via: "export * from ./components"
      pattern: "export \\* from \"./components\""
    - from: "packages/eslint.config.js"
      to: "the 12 ported atom files"
      via: "negated ignore entries un-ignore ported atoms"
      pattern: "!src/components/Button.tsx"
---

<objective>
Wire the 12 ported atoms back into the library and prove the whole Phase-2 surface is green. Regenerate the components barrel, repoint `src/index.ts` from the Phase-1 stub to the real components barrel, un-ignore the 12 ported atoms in ESLint so `solid/no-destructure` actually lints them, register the new `src/tests/atoms/` tests in vitest's `include`, then run the build (dual `.js`+`.jsx`+`.d.ts`), test, and lint gate. Final human-verify checkpoint confirms ATOM-01 is satisfied end-to-end.

Purpose: ATOM-01 (completion + green gate). This is the sequential Wave-2 plan that owns the shared files (barrel, entry, eslint, vitest config) and must run AFTER all four atom-port plans.
Output: regenerated barrel, repointed entry, lint/test config updated, build+test+lint proven green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-stateless-atoms/02-CONTEXT.md

<interfaces>
<!-- From packages/package.json scripts: -->
<!--   barrels: node scripts/generate-barrel.cjs   (regenerates src/components/index.ts) -->
<!--   build:   vite build && node scripts/build-solid-condition.mjs && tsc ... --emitDeclarationOnly  (dual .js + .jsx + .d.ts) -->
<!--   test:    vitest run     lint: eslint . -->

<!-- Current packages/src/index.ts (Phase-1 stub — must be repointed): -->
```ts
export * from "./_stub";
```

<!-- Current packages/eslint.config.js ignores block contains: "src/components/**" (line ~24). -->
<!-- Flat-config global ignores: use "src/components/**/*" + negation "!src/components/<Atom>.tsx" -->
<!-- to un-ignore ONLY the 12 ported atoms (the other ~22 React components stay ignored). -->

<!-- Current packages/vitest.config.ts include is hardcoded to ["src/tests/toolchain.test.tsx"]. -->
<!-- Add "src/tests/atoms/**/*.test.tsx" so the new atom tests run. -->

<!-- The 12 atoms (barrel + un-ignore list): Button, IconButton, Badge, Tag, Chip, Avatar, -->
<!-- Loader, CircularProgress, LinearProgress, Placeholder, Alert, Breadcrumb. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Un-ignore ported atoms in ESLint + register atom tests in vitest</name>
  <files>packages/eslint.config.js, packages/vitest.config.ts</files>
  <read_first>
    - packages/eslint.config.js (current ignores block — `src/components/**` at ~line 24; solid/no-destructure rule active)
    - packages/vitest.config.ts (current include hardcoded to toolchain.test.tsx)
    - .planning/phases/02-stateless-atoms/02-CONTEXT.md (eslint must stay green; no-destructure enforces D-02)
  </read_first>
  <action>
    ESLint (packages/eslint.config.js): the un-ported React components must STAY ignored (they still import React and would fail), but the 12 ported atoms must be LINTED so `solid/no-destructure` covers them.
    - Change the ignore entry `"src/components/**"` to `"src/components/**/*"` (the `/*` form is required for negation to work in flat-config global ignores).
    - Immediately after it, add a negated un-ignore line for EACH of the 12 ported atoms:
      `"!src/components/Button.tsx"`, `"!src/components/IconButton.tsx"`, `"!src/components/Badge.tsx"`, `"!src/components/Tag.tsx"`, `"!src/components/Chip.tsx"`, `"!src/components/Avatar.tsx"`, `"!src/components/Loader.tsx"`, `"!src/components/CircularProgress.tsx"`, `"!src/components/LinearProgress.tsx"`, `"!src/components/Placeholder.tsx"`, `"!src/components/Alert.tsx"`, `"!src/components/Breadcrumb.tsx"`.
    - Do NOT touch the legacy src/tests/*.test.tsx ignores.

    vitest (packages/vitest.config.ts): add the atom tests to `include`:
    - Change `include: ["src/tests/toolchain.test.tsx"]` → `include: ["src/tests/toolchain.test.tsx", "src/tests/atoms/**/*.test.tsx"]`.
    - Leave the legacy React tests excluded (they are not matched by either glob).
  </action>
  <verify>
    <automated>cd packages && npx eslint src/components/Button.tsx src/components/Alert.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `eslint.config.js` contains `"src/components/**/*"` (with the trailing `/*`)
    - `eslint.config.js` contains all 12 negated entries (grep `!src/components/Button.tsx` ... `!src/components/Breadcrumb.tsx`)
    - `vitest.config.ts` include contains `src/tests/atoms/**/*.test.tsx`
    - `npx eslint src/components/Button.tsx` is NOT skipped (no "ignored" warning) and exits 0
  </acceptance_criteria>
  <done>The 12 ported atoms are linted (not ignored); atom tests are registered in vitest include; both configs valid.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate barrel + repoint src/index.ts to real components</name>
  <files>packages/src/components/index.ts, packages/src/index.ts</files>
  <read_first>
    - packages/src/components/index.ts (current barrel — exact exported type names to preserve)
    - packages/src/index.ts (Phase-1 stub: `export * from "./_stub"`)
    - packages/scripts/generate-barrel.cjs (barrel generator — derives type exports from `<Name>Sizes`/`<Name>Variants` and `<Name>\w+` sub-exports)
    - .planning/phases/02-stateless-atoms/02-CONTEXT.md (D-09 barrel type names IDENTICAL; repoint instructions)
  </read_first>
  <action>
    - Run the barrel generator: `cd packages && npm run barrels` (node scripts/generate-barrel.cjs). This regenerates src/components/index.ts from the .tsx files present. It will include the 12 ported atoms AND the still-React components (the generator does not check React-vs-Solid — it lists all .tsx). That is acceptable: only the entry (src/index.ts) decides what builds. After regen, confirm the 12 atoms and their type exports appear with IDENTICAL names (ButtonSizes, ButtonVariants, BadgeVariants, ChipSizes, ChipVariants, AvatarSizes, AvatarVariants, IconButtonSizes, IconButtonVariants, TagSizes, TagVariants, LoaderSizes, CircularProgressSizes, LinearProgressSizes, AlertVariants — Breadcrumb/Placeholder have no exported size/variant types, matching current barrel).
    - Repoint src/index.ts: replace `export * from "./_stub";` with `export * from "./components";`.
    - IMPORTANT — build scope: the build pipeline (vite + esbuild solid-condition) bundles from src/index.ts which now pulls in the full components barrel, including the ~22 still-React components that would break the Solid build. To keep Phase-2 buildable WITHOUT prematurely porting Phase 3-4 components, DO NOT have src/index.ts pull unported components into the build graph. Instead, repoint src/index.ts to export ONLY the 12 ported atoms explicitly:
      ```ts
      export * from "./types";
      export { default as Alert } from "./components/Alert";
      export type { AlertVariants } from "./components/Alert";
      export { default as Avatar } from "./components/Avatar";
      export type { AvatarSizes, AvatarVariants } from "./components/Avatar";
      export { default as Badge } from "./components/Badge";
      export type { BadgeVariants } from "./components/Badge";
      export { default as Breadcrumb } from "./components/Breadcrumb";
      export { default as Button } from "./components/Button";
      export type { ButtonSizes, ButtonVariants } from "./components/Button";
      export { default as Chip } from "./components/Chip";
      export type { ChipSizes, ChipVariants } from "./components/Chip";
      export { default as CircularProgress } from "./components/CircularProgress";
      export type { CircularProgressSizes } from "./components/CircularProgress";
      export { default as IconButton } from "./components/IconButton";
      export type { IconButtonSizes, IconButtonVariants } from "./components/IconButton";
      export { default as LinearProgress } from "./components/LinearProgress";
      export type { LinearProgressSizes } from "./components/LinearProgress";
      export { default as Loader } from "./components/Loader";
      export type { LoaderSizes } from "./components/Loader";
      export { default as Placeholder } from "./components/Placeholder";
      export { default as Tag } from "./components/Tag";
      export type { TagSizes, TagVariants } from "./components/Tag";
      ```
      Rationale: src/components/index.ts (the generated barrel) still lists ALL components for when later phases land, but the BUILD ENTRY (src/index.ts) only exposes what is actually ported, so `npm run build` stays green this phase. Later phases extend src/index.ts as they port. Document this in the SUMMARY.
    - NOTE: `npm run prebuild` also runs `npm run barrels`, so the build regenerates the full barrel each time — that is fine; only src/index.ts gates what compiles.
  </action>
  <verify>
    <automated>cd packages && npm run barrels</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/index.ts` contains `export { default as Button } from "./Button";` and the 12 atoms' lines with unchanged type names (grep ButtonSizes, AlertVariants, ChipSizes, etc.)
    - `src/index.ts` no longer contains `./_stub`
    - `src/index.ts` exports exactly the 12 ported atoms (+ `export * from "./types"`); grep shows Button, Alert, Avatar, Badge, Breadcrumb, Chip, CircularProgress, IconButton, LinearProgress, Loader, Placeholder, Tag — and does NOT export any unported component (e.g. no `Dialog`, `Checkbox`, `Carousel`)
    - `npm run barrels` exits 0
  </acceptance_criteria>
  <done>Barrel regenerated with all 12 atoms (identical type names); src/index.ts repointed to export only the 12 ported atoms so the build stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Build + test + lint green gate</name>
  <files>(no source edits — runs the pipeline; fixes only if a gate fails)</files>
  <read_first>
    - packages/package.json (scripts: build = vite build && build-solid-condition.mjs && tsc --emitDeclarationOnly; test = vitest run; lint = eslint .)
    - packages/scripts/build-solid-condition.mjs (dual-build .jsx raw-JSX pass)
    - .planning/phases/01-toolchain-foundation/01-CONTEXT.md (dual-build two-pass expectations)
  </read_first>
  <action>
    Run the three gates in order; if any fails, fix the offending ported atom or config and re-run (own the failure — do not defer):
    1. `cd packages && npm run build` — must exit 0 and emit, for each of the 12 atoms, a compiled `dist/components/<Atom>.js`, a raw-JSX `dist/components/<Atom>.jsx`, and a `dist/components/<Atom>.d.ts`.
    2. `cd packages && npm run test` — vitest runs toolchain.test.tsx + all src/tests/atoms/*.test.tsx; zero failures, zero skips.
    3. `cd packages && npm run lint` — eslint exits 0 with zero `solid/no-destructure` violations across the 12 now-linted atoms.
    Verify dual output for a sample: confirm `dist/components/Button.jsx` contains raw `<button` JSX (NOT compiled `_tmpl$`/`createComponent`), and `dist/components/Button.js` contains the compiled Solid form. Confirm `dist/components/Button.d.ts` exists and re-exports ButtonSizes/ButtonVariants types.
  </action>
  <verify>
    <automated>cd packages && npm run build && npm run test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `dist/components/Button.js`, `dist/components/Button.jsx`, `dist/components/Button.d.ts` all exist (sample the dual+types pipeline); same trio exists for Alert and Breadcrumb
    - `dist/components/Button.jsx` contains a raw `<button` tag (preserved JSX), confirming the solid-condition pass ran
    - `npm run test` exits 0 with all 12 atom test files passing (13 files total incl. toolchain), zero skips
    - `npm run lint` exits 0 (zero solid/no-destructure violations)
  </acceptance_criteria>
  <done>Build emits dual .js+.jsx+.d.ts for all 12 atoms; full vitest suite green; eslint green with no-destructure satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All 12 stateless atoms ported to SolidJS (Button, IconButton, Badge, Tag, Chip, Avatar, Loader, CircularProgress, LinearProgress, Placeholder, Alert, Breadcrumb), barrel regenerated, src/index.ts repointed to the 12 ported atoms, and the build (dual .js+.jsx+.d.ts) + vitest + eslint (solid/no-destructure) gate proven green.</what-built>
  <how-to-verify>
    1. `cd packages && npm run build` → exits 0; spot-check `dist/components/Button.jsx` shows raw `<button` JSX and `dist/components/Button.js` shows compiled Solid output.
    2. `cd packages && npm run test` → all atom tests under src/tests/atoms/ pass (zero skips).
    3. `cd packages && npm run lint` → exits 0 (no solid/no-destructure violations).
    4. Confirm ATOM-01 success criteria: each atom renders correct moon-* class output (per the per-atom tests) and exported type names are unchanged from the React barrel.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 2 complete, or describe any failing gate / class-parity mismatch.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0; dual .js+.jsx+.d.ts present for all 12 atoms
- `npm run test` green (toolchain + 12 atom test files)
- `npm run lint` green (solid/no-destructure satisfied across the 12 atoms)
- src/index.ts exports the 12 atoms (no stub, no unported components); barrel type names unchanged
</verification>

<success_criteria>
- All 12 atoms wired into the library entry and building through the dual pipeline
- ESLint lints the 12 atoms with zero no-destructure violations
- Full vitest atom suite passes
- ATOM-01 satisfied: 12 atoms ported, identical class output + public API/type names, passing tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-stateless-atoms/02-05-SUMMARY.md`
</output>
