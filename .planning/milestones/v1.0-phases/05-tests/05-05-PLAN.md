---
phase: 05-tests
plan: 05
type: execute
wave: 2
depends_on: [01, 02, 03, 04]
files_modified:
  - packages/vitest.config.ts
  - packages/eslint.config.js
autonomous: false
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "Every component has exactly ONE Solid test in src/tests/atoms/ (34 files); zero flat legacy *.test.tsx remain except toolchain.test.tsx + setupTests.ts"
    - "vitest run is fully green across the whole suite with zero failures and zero skips"
    - "Zero @testing-library/react imports, zero jest. references anywhere under src/tests"
    - "eslint runs on ALL test files (legacy ignores removed) with 0 errors"
    - "All test filenames PascalCase; no lowercase accordion.test.tsx exists"
  artifacts:
    - path: "packages/vitest.config.ts"
      provides: "include glob covering the consolidated atoms/ location + toolchain; stale legacy comment removed"
      contains: "src/tests/atoms"
    - path: "packages/eslint.config.js"
      provides: "no legacy-test ignore entries; all tests linted"
  key_links:
    - from: "packages/eslint.config.js"
      to: "packages/src/tests/atoms/*.test.tsx"
      via: "tests no longer ignored — linted as Solid"
      pattern: "ignores"
---

<objective>
Finalize Phase 5: remove the legacy-test ignores from `vitest.config.ts` and `eslint.config.js` (D-05), confirm exactly one Solid test per component remains with no duplicate legacy+atom pairs and no flat legacy files, and run the full green gate (D-06) — `vitest run` all-green, `eslint .` 0 errors, build still green. A human-verify checkpoint confirms the suite passes before the phase closes.

Purpose: Phase 5 success criteria — 34 test files passing, render(() => <X/>) form everywhere, zero jest.*, PascalCase filenames, all tests linted (TEST-01, TEST-02).
Output: cleaned config files; full green test + lint gate; phase-complete confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tests/05-CONTEXT.md

<consolidation_decision>
CHOSEN DIRECTORY: `packages/src/tests/atoms/` (consolidate DOWN). All 34 component tests now live in `src/tests/atoms/*.test.tsx` (already inside the vitest `include` glob). The flat legacy files were deleted by plans 05-01..05-04. `toolchain.test.tsx` and `setupTests.ts` stay at `src/tests/`. The lowercase `accordion.test.tsx` was removed (its merged form is `atoms/Accordion.test.tsx`, PascalCase) — TEST-02 satisfied. This plan ONLY edits the two shared config files + runs the gate.
</consolidation_decision>

<interfaces>
Current configs to edit:
@packages/vitest.config.ts
@packages/eslint.config.js
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify single-source consolidation + remove legacy ignores from configs</name>
  <files>packages/vitest.config.ts, packages/eslint.config.js</files>
  <read_first>
    - packages/vitest.config.ts (current include: ["src/tests/toolchain.test.tsx", "src/tests/atoms/**/*.test.tsx"] + stale legacy comment)
    - packages/eslint.config.js (ignores array contains the 19 legacy src/tests/*.test.tsx entries + accordion.test.tsx to remove)
  </read_first>
  <action>
    1. CONSOLIDATION GUARD — before editing configs, confirm the upstream plans left a clean single-source state. There must be ZERO flat legacy component test files left under `src/tests/` (only `toolchain.test.tsx` and `setupTests.ts` may remain there); all 34 component tests live under `src/tests/atoms/`. If any flat legacy `src/tests/<Component>.test.tsx` still exists, STOP and report it — do not proceed (an upstream plan failed to delete it).
    2. `vitest.config.ts`: keep the include covering `src/tests/toolchain.test.tsx` and `src/tests/atoms/**/*.test.tsx` (this already covers every consolidated test). Remove the now-stale Phase-01/02/07 comment block referencing legacy React tests / "migrated in Phase 07". Ensure no `exclude` entry hides any test.
    3. `eslint.config.js`: DELETE every legacy-test ignore entry — the 18 PascalCase `src/tests/*.test.tsx` lines AND the `src/tests/accordion.test.tsx` line (lines for Alert/Badge/BottomSheet/Breadcrumb/Button/CircularProgress/Dialog/Drawer/Dropdown/IconButton/List/Menu/Pagination/Snackbar/Table/TabList/Tag/Tooltip/accordion). Keep the non-test ignores (`dist/**`, `node_modules/**`, `cli/**`, `bin/**`, `scripts/**`, `vite.config.ts`, `vitest.config.ts`). The deleted files no longer exist, but removing the ignore entries also prevents stale-path lint config and lets the atoms/ tests be linted.
  </action>
  <verify>
    <automated>cd packages && npx eslint . && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - No flat legacy component test files: `find packages/src/tests -maxdepth 1 -name '*.test.tsx' ! -name 'toolchain.test.tsx' | grep . && echo HAS_LEGACY || echo CLEAN` prints `CLEAN`
    - `test ! -f packages/src/tests/accordion.test.tsx`
    - `grep -c "src/tests/.*\.test\.tsx" packages/eslint.config.js` returns 0 (no legacy test ignore entries remain; count ignores comment-free: `grep -v '^\s*//' packages/eslint.config.js | grep -c "src/tests/.*\.test\.tsx"` is 0)
    - `cd packages && npx eslint .` exits 0 (zero errors)
    - `cd packages && npx vitest run` exits 0
  </acceptance_criteria>
  <done>Configs cleaned; consolidation confirmed single-source; eslint + vitest both green from configs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Full green gate — suite-wide invariants + build</name>
  <files>packages/vitest.config.ts</files>
  <read_first>
    - packages/vitest.config.ts (confirm include after Task 1 edit)
  </read_first>
  <action>
    Run the Phase-5 gate (D-06) and assert the suite-wide invariants:
    1. `cd packages && npx vitest run` — full suite green, zero failures, zero skips. Note the passing file count (expect 34 atom component tests + toolchain.test.tsx).
    2. Grep invariants across the whole test tree (exclude none of atoms/): zero `@testing-library/react` imports, zero `jest.` references, every component test contains `render(() =>`.
    3. `cd packages && npx eslint .` — 0 errors.
    4. `cd packages && npm run build` (or the project build script) — confirm the build still succeeds (tests don't affect build, but run once to confirm no breakage per D-06).
    If any invariant fails, fix-forward within the consolidated atoms/ tests or report the specific failing file. Do NOT re-introduce flat files.
  </action>
  <verify>
    <automated>cd packages && npx vitest run && npx eslint . && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages && npx vitest run` exits 0 with zero failures and zero skips
    - `grep -rl "@testing-library/react" packages/src/tests` returns nothing (zero files)
    - `grep -rn "jest\." packages/src/tests` returns nothing
    - Every component test uses the function-wrapper form: `grep -rL "render(() =>" packages/src/tests/atoms` returns nothing (every atoms file contains it)
    - All filenames PascalCase: `find packages/src/tests/atoms -name '*.test.tsx' | grep -E '/[a-z]' ` (a lowercase-initial filename) returns nothing
    - `cd packages && npx eslint .` exits 0
    - `cd packages && npm run build` exits 0
  </acceptance_criteria>
  <done>Full suite green, all invariants hold, build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 5 complete: all 34 component tests consolidated into single Solid test files under src/tests/atoms/, legacy React tests migrated+merged (parity ≥ legacy) and flat files deleted, accordion→Accordion normalized, legacy ignores removed from vitest+eslint configs, full vitest + eslint + build green.</what-built>
  <how-to-verify>
    1. Run `cd packages && npx vitest run` — confirm all test files pass, zero failures, zero skips (expect 34 atom component tests + toolchain).
    2. Run `cd packages && npx eslint .` — confirm 0 errors.
    3. Confirm no duplicate test pairs: there should be NO flat `src/tests/<Component>.test.tsx` (only `toolchain.test.tsx` + `setupTests.ts`); every component test lives in `src/tests/atoms/`.
    4. Spot-check one merged test (e.g. `src/tests/atoms/Button.test.tsx`) — confirm it has BOTH the class-matrix coverage AND the onClick(vi.fn) handler coverage merged from legacy.
    5. Confirm `accordion.test.tsx` is gone and `atoms/Accordion.test.tsx` exists (PascalCase).
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 5, or describe any failing test / missing coverage / leftover duplicate.</resume-signal>
</task>

</tasks>

<verification>
- `cd packages && npx vitest run` — full suite green
- `cd packages && npx eslint .` — 0 errors
- `cd packages && npm run build` — green
- Zero @testing-library/react, zero jest., all render(() => ...), PascalCase filenames, single test per component
</verification>

<success_criteria>
- vitest run reports the full consolidated suite passing (zero failures, zero skips)
- vite/eslint legacy-test ignores removed; all tests linted with 0 errors
- Exactly ONE Solid test per component (no duplicate legacy+atom pairs); 19 legacy merged, 15 atom-only relocated/verified, accordion→Accordion
- Build still green
- Human verified and approved
</success_criteria>

<output>
After completion, create `.planning/phases/05-tests/05-05-SUMMARY.md`
</output>
