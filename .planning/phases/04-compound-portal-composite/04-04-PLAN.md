---
phase: 04-compound-portal-composite
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/src/components/Table.tsx
  - packages/src/components/List.tsx
  - packages/src/components/Authenticator.tsx
  - packages/src/tests/atoms/Table.test.tsx
  - packages/src/tests/atoms/List.test.tsx
  - packages/src/tests/atoms/Authenticator.test.tsx
autonomous: true
requirements: [COMP-01]

must_haves:
  truths:
    - "Table renders identical moon-table* output with zero @tanstack/react-table imports (it never had any) and full thead/tbody/tr/th/td compound API"
    - "List renders moon-list* with {...rest} spread and preserved ListSizes type"
    - "Authenticator uses createSignal(value) + a local let inputs-ref array (Carousel pattern), <Index> over length, onInput per keystroke, focus advance + paste handling preserved"
    - "Exported type names preserved (TableSizes, ListSizes, AuthenticatorSizes, AuthenticatorVariants)"
  artifacts:
    - path: "packages/src/components/Table.tsx"
      provides: "Solid Table compound (Head/Body/Foot/Row/HeadCell/Cell/Caption)"
      contains: "moon-table"
    - path: "packages/src/components/List.tsx"
      provides: "Solid List compound (Item/Meta)"
      contains: "moon-list"
    - path: "packages/src/components/Authenticator.tsx"
      provides: "Solid Authenticator with createSignal + Index inputs"
      contains: "createSignal"
  key_links:
    - from: "Authenticator inputs"
      to: "length-count render"
      via: "<Index each={Array.from({length})}>"
      pattern: "<Index|<For"
    - from: "Authenticator value"
      to: "controlled input"
      via: "createSignal + onInput per keystroke"
      pattern: "onInput"
---

<objective>
Port the list/misc composites — Table, List, Authenticator — from React to SolidJS. Table and List are stateless compounds (pure class/spread). Authenticator is the stateful outlier: `useState`+`useRef` array → `createSignal(value)` + a local `let inputsRef` array (the Carousel let-ref pattern), `<Index>` over `length`, `onInput` per keystroke with focus-advance and paste handling.

Purpose: Complete COMP-01 and the 14th-and-final component file of the 37-set. Confirm Table needs no tanstack dependency.
Output: 3 ported components + 3 minimal Solid tests. Zero React, exact class + type-name parity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-compound-portal-composite/04-CONTEXT.md

<interfaces>
<!-- Authenticator: reuse Carousel local-let-ref + createSignal pattern. -->
```tsx
import { createSignal, mergeProps, splitProps, Index, type Component, type JSX } from "solid-js";
// let inputs: HTMLInputElement[] = [];   // local array ref, set via ref callback in Index
// const [internalValue, setInternalValue] = createSignal(value);
// <Index each={Array.from({ length: local.length })}>{(_, i) => (
//    <input ref={(el) => (inputs[i] = el)} value={internalValue()[i] || ""} onInput={(e)=>handleChange(i, e.currentTarget.value.slice(-1))} ... />
// )}</Index>
```

Type names to preserve verbatim:
- Table: `TableSizes`
- List: `ListSizes`
- Authenticator: `AuthenticatorSizes`, `AuthenticatorVariants`

NOTE: Authenticator React source imports `{ mergeClasses }` (NAMED). All other components use the DEFAULT import `import mergeClasses from "../helpers/mergeClasses"`. Use the DEFAULT import for consistency with Phase 1-3 ported components (verify helpers/mergeClasses exports a default — it does, per SegmentedControl.tsx).
</interfaces>

<test_pattern>
```tsx
import { render, fireEvent } from "@solidjs/testing-library";
import { describe, it, expect } from "vitest";
// Table: render(() => <Table><Table.Body><Table.Row><Table.Cell>x</Table.Cell></Table.Row></Table.Body></Table>)
// query table.moon-table, td, etc.
```
</test_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port Table + List (stateless compounds, spread)</name>
  <files>packages/src/components/Table.tsx, packages/src/components/List.tsx</files>
  <read_first>
    - packages/src/components/Table.tsx, List.tsx (React sources — parity targets)
    - .planning/phases/04-compound-portal-composite/04-CONTEXT.md (D-06)
    - packages/src/components/Alert.tsx (compound Object.assign + atom pattern precedent)
  </read_first>
  <action>
    Table.tsx:
    - Preserve `export type TableSizes = Extract<Sizes,"sm"|"md"|"lg"|"xl">;`. `import type { Sizes } from "../types"`.
    - Sub-components Head/Body/Foot/Row/HeadCell/Cell/Caption: each splitProps `["class"]` (+ rest) and render the native element passing `class={local.class}` + `{...rest}`. Match the EXACT React structure — note React passed `className={className}` (raw, no mergeClasses) for these sub-elements and `{...props}`; keep that: `<thead class={local.class} {...rest} />`, `<tbody class={local.class} {...rest} />`, etc. (HeadCell/Cell/Caption put `{...rest}` before `class` in React — order is irrelevant to output; keep `class={local.class} {...rest}`.)
    - Root: `mergeProps({ size:"md" }, props)`; splitProps `["class","size"]` (+ rest); `<table class={mergeClasses("moon-table", local.size!=="md"&&\`moon-table-${local.size}\`)} {...rest} />` (NOTE React Root did NOT merge `className` into the table class — it only used size; preserve that exactly: do not append local.class to the table — React Root destructured `className` but never used it in the class string. Replicate: table class is `mergeClasses("moon-table", size modifier)` only, and `{...rest}` carries any class the consumer spreads. To be safe, splitProps only `["size"]` on Root so a consumer-passed class flows through `{...rest}` exactly as React's `{...props}` did after destructuring out className — MATCH React: React destructured `className` OUT and did not re-apply it, so a consumer class on `<Table>` was dropped. Replicate by splitProps `["class","size"]` and NOT spreading local.class. Keep this parity quirk.)
    - `Object.assign(Root, { Head, Body, Foot, Row, HeadCell, Cell, Caption })`. Use JSX.* attribute types (JSX.HTMLAttributes<HTMLTableSectionElement> etc.). Zero React/className, drop displayName.
    List.tsx:
    - Preserve `export type ListSizes = Extract<Sizes,"sm"|"md"|"lg">;`.
    - Item: `<li class={mergeClasses("moon-list-item", local.class)} {...rest}>{local.children}</li>`.
    - Meta: `<div class={mergeClasses("moon-list-item-meta", local.class)} {...rest}>{local.children}</div>`.
    - Root: `mergeProps({ size:"md" }, props)`; `<ul class={mergeClasses("moon-list", local.size!=="md"&&\`moon-list-${local.size}\`, local.class)} {...rest}>{local.children}</ul>`.
    - `Object.assign(Root, { Item, Meta })`. splitProps everywhere, JSX.* types, zero React/className.
  </action>
  <verify>
    <automated>cd packages; node -e "const fs=require('fs');for(const [f,cls,t] of [['src/components/Table.tsx','moon-table','TableSizes'],['src/components/List.tsx','moon-list','ListSizes']]){const s=fs.readFileSync(f,'utf8');const fail=[];if(/from \"react/.test(s))fail.push('react');if(/className/.test(s))fail.push('className');if(/@tanstack/.test(s))fail.push('tanstack');if(!/splitProps/.test(s))fail.push('splitProps');if(!new RegExp(cls).test(s))fail.push('class '+cls);if(!new RegExp('export type '+t).test(s))fail.push('type '+t);if(fail.length)throw new Error(f+': '+fail.join(', '));}console.log('OK')"</automated>
  </verify>
  <done>Table (full 7-part compound, moon-table* parity incl. the no-consumer-class-on-root quirk, zero @tanstack imports) and List (moon-list*, ListSizes) ported to Solid with exact class output, compound API, zero React/className.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port Authenticator (createSignal + local let inputs-ref + Index)</name>
  <files>packages/src/components/Authenticator.tsx</files>
  <read_first>
    - packages/src/components/Authenticator.tsx (React source — parity target; read handleChange/handleKeyDown/handlePaste exactly)
    - packages/src/components/Carousel.tsx (local let-ref + createSignal precedent — Authenticator analog)
    - packages/src/components/Chip.tsx (createSignal controlled/uncontrolled precedent)
    - .planning/phases/04-compound-portal-composite/04-CONTEXT.md (D-05, D-06)
  </read_first>
  <behavior>
    - Renders `div[role=group].moon-authenticator` containing `length` (default 6) `<input maxLength=1>` elements via <Index>.
    - size="lg" adds `moon-authenticator-lg`; variant="outline" adds `moon-authenticator-outline`; error adds `moon-authenticator-error`.
    - Typing a char in input i sets value[i], fires onChange(newValue), and focuses input i+1 (when i < length-1).
    - Backspace on an empty input i focuses input i-1.
    - Paste fills from clipboard (cleaned alphanumeric, sliced to length) and focuses the next slot.
    - disabled disables all inputs.
  </behavior>
  <action>
    Rewrite Authenticator.tsx in Solid (default export single component, like Carousel — no compound).
    - Preserve `export type AuthenticatorSizes`/`AuthenticatorVariants`. `import mergeClasses from "../helpers/mergeClasses"` (DEFAULT import — change from the React named import).
    - `const merged = mergeProps({ id:"", length:6, size:"md", variant:"fill", error:false, disabled:false, value:"" }, props); const [local] = splitProps(merged, [...all keys...]);`
    - `const [internalValue, setInternalValue] = createSignal(local.value);` `let inputs: HTMLInputElement[] = [];` (local let array — Carousel pattern; not shared via context so no signal-wrap needed).
    - `handleChange(index, char)`: `const arr = internalValue().split(""); arr[index] = char; const next = arr.join(""); setInternalValue(next); local.onChange?.(next); if (char && index < local.length - 1) inputs[index + 1]?.focus();`
    - `handleKeyDown(index, e)`: `if (e.key === "Backspace" && !internalValue()[index] && index > 0) inputs[index - 1]?.focus();`
    - `handlePaste(e)`: same clean/slice logic; `setInternalValue(clean.padEnd(local.length, "")); local.onChange?.(clean); inputs[Math.min(clean.length, local.length-1)]?.focus(); e.preventDefault();`
    - Render `<div class={mergeClasses("moon-authenticator", local.size!=="md"&&\`moon-authenticator-${local.size}\`, local.variant!=="fill"&&\`moon-authenticator-${local.variant}\`, local.error&&"moon-authenticator-error", local.class)} role="group">` containing `<Index each={Array.from({ length: local.length })}>{(_, index) => (<input id={index===0 ? local.id : \`${local.id}${index}\`} ref={(el)=>(inputs[index]=el)} type="text" maxLength={1} value={internalValue()[index] || ""} onInput={(e)=>handleChange(index, e.currentTarget.value.slice(-1))} onKeyDown={(e)=>handleKeyDown(index, e)} onPaste={handlePaste} autocomplete="off" inputMode="text" pattern="[0-9a-zA-Z]*" disabled={local.disabled} />)}</Index></div>`.
    - CRITICAL: use `onInput` (not React's `onChange`) for per-keystroke updates (PITFALLS #9); use `e.currentTarget`. `Index` (not For) since slots are index-keyed and value re-derives from signal. Zero React/react-dom/useState/useRef/className, drop displayName.
  </action>
  <verify>
    <automated>cd packages; node -e "const s=require('fs').readFileSync('src/components/Authenticator.tsx','utf8');const fail=[];if(/from \"react/.test(s))fail.push('react');if(/className/.test(s))fail.push('className');if(/useState|useRef/.test(s))fail.push('react hook');if(!/createSignal/.test(s))fail.push('createSignal');if(!/onInput/.test(s))fail.push('onInput');if(!/<Index|<For/.test(s))fail.push('no Index/For');if(!/splitProps/.test(s))fail.push('splitProps');if(!/moon-authenticator\b/.test(s))fail.push('class');if(!/export type AuthenticatorSizes/.test(s)||!/export type AuthenticatorVariants/.test(s))fail.push('types');if(!/import mergeClasses from/.test(s))fail.push('default mergeClasses import');if(fail.length)throw new Error(fail.join(', '));console.log('Authenticator OK')"</automated>
  </verify>
  <done>Authenticator.tsx is Solid: createSignal(value) + local let inputs array, <Index> over length, onInput per keystroke, focus-advance + backspace + paste preserved, moon-authenticator* class parity, types preserved, default mergeClasses import, zero React.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Minimal Solid tests for Table, List, Authenticator</name>
  <files>packages/src/tests/atoms/Table.test.tsx, packages/src/tests/atoms/List.test.tsx, packages/src/tests/atoms/Authenticator.test.tsx</files>
  <read_first>
    - packages/src/tests/atoms/SegmentedControl.test.tsx (scaffold), packages/src/tests/atoms/Input.test.tsx (input/onInput precedent if present)
    - packages/src/components/Table.tsx, List.tsx, Authenticator.tsx (just ported)
    - .planning/phases/04-compound-portal-composite/04-CONTEXT.md (D-08)
  </read_first>
  <behavior>
    - Table: full compound renders `table.moon-table` with thead/tbody/tr/th/td present; size="lg" adds `moon-table-lg`.
    - List: `ul.moon-list`; size="sm" adds `moon-list-sm`; Item renders `li.moon-list-item`.
    - Authenticator: renders 6 inputs by default (length default); length=4 renders 4; role=group + moon-authenticator class; size/variant/error modifiers; typing into the first input fires onChange with that char (onInput).
  </behavior>
  <action>
    Write 3 minimal Solid test files in src/tests/atoms/. Table test: render the full compound, assert `table.moon-table` plus presence of thead/tbody/tr/th/td and size modifier. List test: assert `ul.moon-list`, size modifier, `li.moon-list-item`. Authenticator test: render with onChange spy, assert `div[role=group].moon-authenticator`, count inputs === length (default 6 and explicit 4), assert a modifier class, `fireEvent.input(firstInput, { target: { value: "5" } })` and assert spy called (value contains "5"). Use `fireEvent.input` (not change) to match onInput. Keep minimal (full suite is Phase 5).
  </action>
  <verify>
    <automated>cd packages; npx vitest run src/tests/atoms/Table.test.tsx src/tests/atoms/List.test.tsx src/tests/atoms/Authenticator.test.tsx</automated>
  </verify>
  <done>Three test files pass: Table compound + class, List class + Item, Authenticator input-count + onInput state all asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clipboard paste → Authenticator value | handlePaste reads clipboard text into component state |
| consumer children → table/list cells | Arbitrary JSX rendered into td/li |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | Authenticator handlePaste | mitigate | Preserve React original's sanitization: `clean = pasted.replace(/[^0-9a-zA-Z]/g, "").slice(0, length)` — pasted content is regex-filtered to alphanumeric before entering state |
| T-04-08 | Information disclosure | Table/List {...rest} on native elements | accept | 1:1 port; rest spread matches React.ComponentProps surface — no new sink, JSX auto-escapes children |
</threat_model>

<verification>
- No `from "react"` / `className` / `@tanstack` / `useState` / `useRef` in any of the 3 files.
- TableSizes, ListSizes, AuthenticatorSizes, AuthenticatorVariants exported and identical.
- Authenticator uses onInput + <Index>; paste sanitization regex preserved.
- vitest passes the 3 new test files.
</verification>

<success_criteria>
- Table, List, Authenticator ported to Solid with exact class output and preserved type names.
- Table has zero @tanstack/react-table imports (confirmed — it never had any).
- Authenticator uses createSignal + local let inputs-ref + <Index> + onInput.
- 3 minimal Solid tests green. This completes all 14 component source files of Phase 4.
</success_criteria>

<output>
After completion, create `.planning/phases/04-compound-portal-composite/04-04-SUMMARY.md`
</output>
