---
phase: 07-cli-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - packages/README.md
  - packages/CHANGELOG.md
  - .changeset/moon-solid-3-0-0.md
autonomous: true
requirements: [REL-01]
user_setup: []

must_haves:
  truths:
    - "Root README and packages/README document @moondesignsystem/solid, Solid usage, and the moon-solid CLI"
    - "Both READMEs contain zero 'moon-react' / '@moondesignsystem/react' install/usage references and zero 'className' in code examples"
    - "CHANGELOG.md has a 3.0.0 major entry documenting the React->SolidJS breaking change"
    - "A 3.0.0 major changeset md exists in .changeset/ (prepared, not published)"
  artifacts:
    - path: "README.md"
      provides: "Root README rewritten for Solid"
      contains: "@moondesignsystem/solid"
    - path: "packages/README.md"
      provides: "Published README rewritten for Solid"
      contains: "@moondesignsystem/solid"
    - path: "packages/CHANGELOG.md"
      provides: "3.0.0 React->Solid breaking-change entry"
      contains: "3.0.0"
    - path: ".changeset/moon-solid-3-0-0.md"
      provides: "Prepared major changeset"
      contains: "major"
  key_links:
    - from: "packages/CHANGELOG.md"
      to: "package.json version 3.0.0"
      via: "version header"
      pattern: "## 3\\.0\\.0"
    - from: ".changeset/moon-solid-3-0-0.md"
      to: "@moondesignsystem/solid major bump"
      via: "changeset frontmatter"
      pattern: "@moondesignsystem/solid.*major"
---

<objective>
Rewrite both READMEs for the Solid release and write the 3.0.0 major CHANGELOG entry + a prepared changeset documenting the React->SolidJS breaking change.

Purpose: Completes REL-01. The package is already named/versioned `@moondesignsystem/solid@3.0.0` (Phase 1), but the docs still say "Moon React" and the CHANGELOG stops at 2.5.21 under the old package name. This plan makes the published docs and changelog reflect Solid. Per D-04 we write the changeset md + CHANGELOG entry DIRECTLY (no interactive `npx changeset`, and explicitly NO `changeset version`/`changeset publish`).

Output: Solid-rewritten README.md (root) + packages/README.md, a 3.0.0 major CHANGELOG.md entry, and a prepared major changeset md in .changeset/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-cli-release/07-CONTEXT.md

<read_first>
- README.md (root — "# Moon React", install/usage/CLI/Moon-UI sections to Solid-ify)
- packages/README.md (published copy — same content, same Solid rewrite)
- packages/CHANGELOG.md (header "# @moondesignsystem/react", top entry 2.5.21 — add 3.0.0 entry above)
- .changeset/config.json (changeset tooling already configured; we write the md directly)
- packages/package.json (confirm name @moondesignsystem/solid, version 3.0.0, peer solid-js — for accurate doc text)
- packages/src/components/Button.tsx (canonical Solid usage example: `import { Button } from "@moondesignsystem/solid"`, `class` not `className`)
- .planning/PROJECT.md Key Decisions (display:contents triggers, Chip uncontrolled-toggle divergence, hard-cut — the breaking-change notes for the CHANGELOG)
</read_first>

<breaking_change_notes>
From PROJECT.md Key Decisions + 07-CONTEXT D-04, the 3.0.0 entry must document:
- Migrated from React to SolidJS; package renamed @moondesignsystem/react -> @moondesignsystem/solid.
- Peer dependency is now solid-js@^1.9.13 (React/react-dom peer deps removed).
- Public API preserved: component names, prop names, exported types, Tailwind class output identical.
- `class` replaces `className` (Solid convention).
- Drawer/BottomSheet/Dropdown triggers use `display:contents` wrapper spans (one extra layout-invisible DOM node) instead of React.cloneElement.
- Chip uncontrolled toggle behavior improved (clicking now activates when uncontrolled — was dead code in React).
- The mandatory `solid` export condition resolves to raw-JSX `dist/index.jsx` for SolidStart/Vite consumers.
</breaking_change_notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite root README.md and packages/README.md for Solid</name>
  <files>README.md, packages/README.md</files>
  <action>
Rewrite BOTH README files (keep their section structure parity, Solid-ified). Both currently have identical "Moon React" content — apply the same rewrite to each. Changes:
1. Title: "# Moon React" -> "# Moon Solid" (or "Moon SolidJS"). Intro prose: "Moon React" -> "Moon Solid"/"Moon SolidJS".
2. Install commands: `@moondesignsystem/react` -> `@moondesignsystem/solid` (npm/yarn/pnpm blocks).
3. CLI usage: `npx @moondesignsystem/react --add button` -> `npx @moondesignsystem/solid --add button` (and the multi-add + `--add-components` examples). Mention the `moon-solid` bin.
4. The Moon UI CLI option examples that use `@moondesignsystem/react` -> `@moondesignsystem/solid`. NOTE: the line `npx @moondesignsystem/ui --target css --preflight` references the separate Moon UI CSS package — leave `@moondesignsystem/ui` references intact (that is NOT this package).
5. Component Usage examples: `import { Button } from "@moondesignsystem/react"` -> `import { Button } from "@moondesignsystem/solid"`. Convert any React-specific JSX to Solid (use `class` not `className`; no `import React`). The existing `const App = () => <Button>Click me</Button>` is already Solid-compatible — keep, under the Solid import.
6. Add a peer-dependency note: requires `solid-js@^1.9.13`.
7. Add a short note on the `solid` export condition: SolidStart/Vite consumers resolve the package via the `solid` condition (raw JSX at `dist/index.jsx`); standard bundlers use `import`.
8. Versioning section repository-tags link: `github.com/moondesignsystem/react/tags` -> `github.com/moondesignsystem/solid/tags`.
9. Fix the pre-existing unterminated string-literal in the local-import example (`"../local-path-to-moon-components;` -> `"../local-path-to-moon-components"`).
  </action>
  <verify>
    <automated>grep -q "@moondesignsystem/solid" README.md && grep -q "@moondesignsystem/solid" packages/README.md && ! grep -q "@moondesignsystem/react" README.md && ! grep -q "@moondesignsystem/react" packages/README.md && ! grep -q "className" README.md && ! grep -q "className" packages/README.md && ! grep -q "# Moon React" README.md</automated>
  </verify>
  <acceptance_criteria>
    - Both READMEs contain `@moondesignsystem/solid` (install + usage + CLI)
    - Zero `@moondesignsystem/react` in either README; zero `# Moon React` title
    - Zero `className` in code examples in either README (Solid uses `class`)
    - `@moondesignsystem/ui` (Moon CSS pkg) references preserved where they refer to the separate CSS tool
    - solid-js peer note + `solid` export-condition note present; tags link points to /solid
    - Both READMEs are content-identical Solid rewrites (published == root)
  </acceptance_criteria>
  <done>Both READMEs document the Solid package, Solid usage, the moon-solid CLI, the solid-js peer, and the solid export condition — zero React/react install or usage references.</done>
</task>

<task type="auto">
  <name>Task 2: Write 3.0.0 CHANGELOG entry and prepared major changeset</name>
  <files>packages/CHANGELOG.md, .changeset/moon-solid-3-0-0.md</files>
  <action>
1. packages/CHANGELOG.md: update the top header `# @moondesignsystem/react` -> `# @moondesignsystem/solid`. Insert a new `## 3.0.0` section directly above the existing `## 2.5.21` entry, under a `### Major Changes` heading. Write the breaking-change body from the `<breaking_change_notes>` block above (React->Solid migration, package rename, solid-js peer, public-API-preserved, class-replaces-className, display:contents triggers, Chip toggle improvement, solid export condition). Keep all existing historical entries (2.5.21 and below) untouched.
2. Create `.changeset/moon-solid-3-0-0.md` with changeset frontmatter and the same summary:
   ```
   ---
   "@moondesignsystem/solid": major
   ---

   Migrate Moon Design System from React to SolidJS. Package renamed @moondesignsystem/react -> @moondesignsystem/solid; peer dependency is now solid-js. Public API (component names, props, exported types, Tailwind class output) preserved. `class` replaces `className`. Drawer/BottomSheet/Dropdown triggers use display:contents wrappers; Chip uncontrolled toggle behavior improved.
   ```
3. Do NOT run `npx changeset`, `changeset version`, or `changeset publish`. The version is already 3.0.0 in package.json — we only PREPARE the changeset + CHANGELOG entry. The changeset config `ignore: ["moon-react-docs"]` is fine; do not edit config.json.
  </action>
  <verify>
    <automated>grep -q "^## 3.0.0" packages/CHANGELOG.md && grep -iq "solid" packages/CHANGELOG.md && grep -q "# @moondesignsystem/solid" packages/CHANGELOG.md && test -f .changeset/moon-solid-3-0-0.md && grep -q "major" .changeset/moon-solid-3-0-0.md && grep -q "@moondesignsystem/solid" .changeset/moon-solid-3-0-0.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "^## 3.0.0" packages/CHANGELOG.md` matches (new top entry)
    - CHANGELOG 3.0.0 section mentions SolidJS migration + package rename (breaking change documented)
    - CHANGELOG header is `# @moondesignsystem/solid`; historical 2.5.21- entries preserved
    - `.changeset/moon-solid-3-0-0.md` exists with `"@moondesignsystem/solid": major` frontmatter
    - No `changeset version`/`changeset publish` was run (version stays 3.0.0, no new bumps applied)
  </acceptance_criteria>
  <done>CHANGELOG has a 3.0.0 major React->Solid entry under the Solid package header, and a prepared major changeset exists — no publish performed.</done>
</task>

</tasks>

<verification>
- Both READMEs: contain `@moondesignsystem/solid`, zero `@moondesignsystem/react`, zero `className`
- CHANGELOG: `## 3.0.0` entry present, mentions SolidJS, header is Solid
- `.changeset/moon-solid-3-0-0.md` exists with major frontmatter
- No changeset version/publish executed
</verification>

<success_criteria>
REL-01 satisfied: README (root + packages) Solid-rewritten, CHANGELOG has a coherent 3.0.0 React->Solid major entry, and a major changeset is prepared (not published).
</success_criteria>

<output>
After completion, create `.planning/phases/07-cli-release/07-02-SUMMARY.md`
</output>
