---
phase: 03-stateful-atoms-carousel
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/src/components/Radio.tsx
  - packages/src/components/SegmentedControl.tsx
  - packages/src/tests/atoms/Radio.test.tsx
  - packages/src/tests/atoms/SegmentedControl.test.tsx
autonomous: true
requirements: [FORM-01]

must_haves:
  truths:
    - "Radio renders an input[type=radio] with class moon-radio (bare) or a label-wrapped variant when label is set"
    - "Radio.Group renders a div[role=radiogroup].moon-radio-group and injects its name onto each child Radio (so each radio input gets the group name)"
    - "SegmentedControl renders div[role=tablist].moon-segmented-control with size modifier, and its Item children render button[role=tab] with active state driven by createSignal-backed selection"
    - "SegmentedControl supports controlled (activeIndex/setActiveIndex props) and uncontrolled (internal createSignal) selection; clicking an Item updates the active item in uncontrolled mode"
    - "Neither file imports from \"react\", uses React.Children/React.cloneElement, or uses className"
  artifacts:
    - path: "packages/src/components/Radio.tsx"
      provides: "Solid Radio + Radio.Group with name injection (no cloneElement)"
      contains: "Object.assign"
    - path: "packages/src/components/SegmentedControl.tsx"
      provides: "Solid SegmentedControl compound with createContext + createSignal selection"
      contains: "createSignal"
    - path: "packages/src/tests/atoms/Radio.test.tsx"
      provides: "render + class-parity + name-injection assertions"
    - path: "packages/src/tests/atoms/SegmentedControl.test.tsx"
      provides: "render + class-parity + active-toggle (controlled & uncontrolled) assertions"
  key_links:
    - from: "packages/src/components/SegmentedControl.tsx"
      to: "createSignal selection state"
      via: "getter called in JSX (isActive())"
      pattern: "createSignal"
---

<objective>
Port the two cloneElement-complex form atoms: Radio (with Radio.Group injecting `name` into children) and SegmentedControl (createContext + useState selection + Children.map). Both use React.Children.map + React.cloneElement, which have NO direct Solid equivalent (AF-4) — translate via Solid context so child injection happens reactively without cloning.

Purpose: These are the genuinely stateful/compound form atoms. SegmentedControl validates createSignal getter-call semantics (D-01/D-02) and the createContext pattern; Radio.Group validates the children-injection-via-context replacement for cloneElement.
Output: 2 ported component files + 2 test files, React-free, class-parity and controlled/uncontrolled behavior preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-stateful-atoms-carousel/03-CONTEXT.md
@.planning/phases/02-stateless-atoms/02-CONTEXT.md

<read_first>
MANDATORY before editing — read each:
- packages/src/components/Radio.tsx — React source (Root: label/bare branches like Checkbox; Group: div[role=radiogroup].moon-radio-group + React.Children.map/cloneElement injecting name)
- packages/src/components/SegmentedControl.tsx — React source (createContext, useContext guard hook, useState internal selection, controlled-via-props override, Children.map cloning index onto Items, Item: button[role=tab] with aria-selected/tabIndex/onClick)
- packages/src/components/Chip.tsx — reference createSignal + getter-call in JSX (active() pattern), controlled-vs-uncontrolled (local.isActive ?? active())
- packages/src/components/Alert.tsx — reference Object.assign compound + per-subcomponent splitProps
- packages/src/tests/atoms/Chip.test.tsx, packages/src/tests/atoms/Alert.test.tsx — reference test style (fireEvent.click, controlled/uncontrolled assertions)
- .planning/research/FEATURES.md — TS-4 (createContext/useContext, guard hook), AF-4 (cloneElement forbidden — replacement via context), TS-1 (createSignal getter-call), Pitfall 5 (children() helper)
</read_first>

<interfaces>
createSignal + controlled/uncontrolled (from Chip.tsx):
```tsx
const [active, setActive] = createSignal(0);
const current = () => local.activeIndex ?? active();   // getter CALLED in JSX
```
createContext/useContext + guard hook (from FEATURES.md TS-4):
```tsx
import { createContext, useContext } from "solid-js";
const Ctx = createContext<CtxType>();
function useCtx() { const c = useContext(Ctx); if (!c) throw new Error("..."); return c; }
<Ctx.Provider value={{ /* getters, not snapshots */ }}>{props.children}</Ctx.Provider>
```
cloneElement replacement (AF-4): do NOT clone children. Inject shared values (name for Radio.Group, activeIndex/setActiveIndex/size for SegmentedControl) via a Context the children read with useContext. Children render via `{props.children}` directly (Provider must wrap children so context resolves — see Pitfall 5: do NOT pre-resolve with children() above the Provider).

Type mappings: React.ComponentProps<"input"> → JSX.InputHTMLAttributes<HTMLInputElement>; React.ComponentProps<"button"> → JSX.ButtonHTMLAttributes<HTMLButtonElement>; React.ReactNode → JSX.Element; className → class. Preserve exported type SegmentedControlSizes (Extract<Sizes, "sm" | "md">). Radio exports no extracted types.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port Radio.tsx — Root (label/bare) + Group with context-based name injection</name>
  <files>packages/src/components/Radio.tsx, packages/src/tests/atoms/Radio.test.tsx</files>
  <read_first>packages/src/components/Radio.tsx (React source), packages/src/components/Alert.tsx (compound), .planning/research/FEATURES.md AF-4 + TS-4, packages/src/tests/atoms/Chip.test.tsx</read_first>
  <behavior>
    - Radio (Root) bare: `<input type="radio" class="moon-radio">`; caller class appended → "moon-radio extra"
    - Radio (Root) with label="L": `<label class={callerClass}><input type="radio" class="moon-radio"/><span>L</span></label>` (same asymmetry as Checkbox: input class is static "moon-radio", caller class on the label)
    - Radio.Group: `<div role="radiogroup" class="moon-radio-group">` (+ caller class); each child Radio's input receives the group's `name` attribute
    - When a Radio inside a Group also declares its own name prop, the group still injects (match React: React.cloneElement merges {name} last, so group name wins) — preserve React's behavior: group name applied
  </behavior>
  <action>
    Replace React import with `import { createContext, useContext, splitProps, type Component, type JSX } from "solid-js"`. Keep mergeClasses.
    Create a RadioGroupContext for name injection (replaces React.Children.map/cloneElement, which is AF-4 forbidden):
    `type RadioGroupCtx = { name: string }; const RadioGroupContext = createContext<RadioGroupCtx>();`.
    Type: `type RadioProps = Omit<JSX.InputHTMLAttributes<HTMLInputElement>, "type"> & { label?: string; class?: string };` and `type RadioGroupProps = { children?: JSX.Element; class?: string; name: string };`.
    Root (`const Root: Component<RadioProps>`): `const [local, rest] = splitProps(props, ["class", "label"]);` Read optional group context: `const group = useContext(RadioGroupContext);`. Compute the input's name: pass `name={group?.name ?? (rest as JSX.InputHTMLAttributes<HTMLInputElement>).name}` — but since name is in rest, simplest parity-safe approach: split "name" too — `splitProps(props, ["class", "label", "name"])` and apply `name={group?.name ?? local.name}` on the input, spreading the remaining rest. This makes the group name override the radio's own name exactly as React's cloneElement(child,{name}) did.
    Branches (ternary): with label → `<label class={local.class}><input type="radio" class="moon-radio" name={group?.name ?? local.name} {...rest} /><span>{local.label}</span></label>`; bare → `<input type="radio" class={mergeClasses("moon-radio", local.class)} name={group?.name ?? local.name} {...rest} />`.
    Group (`const Group: Component<RadioGroupProps>`): `const [local] = splitProps(props, ["children", "class", "name"]);` return `<RadioGroupContext.Provider value={{ name: local.name }}><div role="radiogroup" class={mergeClasses("moon-radio-group", local.class)}>{local.children}</div></RadioGroupContext.Provider>`. Render `{local.children}` INSIDE the Provider (Pitfall 5 — do not pre-resolve above provider).
    Drop displayName. `const Radio = Object.assign(Root, { Group }); export default Radio;`.
    Write Radio.test.tsx: bare input class "moon-radio"; with class → appended; label branch renders label>input.moon-radio + span; Group renders div[role=radiogroup].moon-radio-group; mount `<Radio.Group name="g"><Radio /><Radio /></Radio.Group>` and assert BOTH radio inputs have name="g" (querySelectorAll("input")). This proves context name-injection replaced cloneElement.
  </action>
  <verify>
    <automated>cd packages; npx vitest run src/tests/atoms/Radio.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages; grep -c 'from "react"' src/components/Radio.tsx` returns 0
    - `cd packages; grep -c 'className' src/components/Radio.tsx` returns 0
    - `cd packages; grep -c 'React.Children\|React.cloneElement\|cloneElement\|isValidElement' src/components/Radio.tsx` returns 0
    - `cd packages; grep -c 'createContext' src/components/Radio.tsx` returns >= 1
    - `cd packages; grep -c 'splitProps' src/components/Radio.tsx` returns >= 1
    - `cd packages; npx vitest run src/tests/atoms/Radio.test.tsx` passes including the both-inputs-have-group-name assertion
  </acceptance_criteria>
  <done>Radio.tsx is React-free, replaces Children.map/cloneElement with a RadioGroupContext that injects name reactively, preserves Root label/bare class parity, and Radio.test.tsx passes including name-injection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port SegmentedControl.tsx — createContext + createSignal selection + Item</name>
  <files>packages/src/components/SegmentedControl.tsx, packages/src/tests/atoms/SegmentedControl.test.tsx</files>
  <read_first>packages/src/components/SegmentedControl.tsx (React source — context, guard hook, useState internal selection, controlled override, Children.map index injection, Item button[role=tab]), packages/src/components/Chip.tsx (createSignal + getter-call + controlled/uncontrolled), .planning/research/FEATURES.md TS-1/TS-4/AF-4</read_first>
  <behavior>
    - Root: `<div role="tablist" class="moon-segmented-control">`; size="sm" → "moon-segmented-control moon-segmented-control-sm"; size="md" → no modifier (default); caller class appended last
    - Item: `<button role="tab">` with class "moon-segmented-control-item"; the active Item gets "moon-segmented-control-item-active", aria-selected=true, tabIndex=0; inactive Items aria-selected=false, tabIndex=-1
    - Uncontrolled: internal createSignal(0); first Item active initially; clicking Item N makes Item N active (others inactive)
    - Controlled: when activeIndex prop is provided, that index is active; clicking calls setActiveIndex prop; internal signal is NOT the source of truth
    - Each Item must know its index. React used Children.map to inject index. Replace via context-provided index assignment OR an Item that reads its own position. SIMPLEST PARITY approach: keep the Item's optional `index` prop and require/derive it — but to preserve "no index prop needed" ergonomics, inject index via context iteration. Read the React source: React falls back childProps.index ?? mapIndex. Reproduce: provide a per-Item index through a Solid mechanism (see action).
  </behavior>
  <action>
    Replace React import with `import { createContext, useContext, createSignal, splitProps, type Component, type JSX } from "solid-js"`. Keep mergeClasses + `import type { Sizes } from "../types"`. Preserve `export type SegmentedControlSizes = Extract<Sizes, "sm" | "md">` UNCHANGED.
    Context type: `type SegmentedControlContextType = { activeIndex: () => number; setActiveIndex: (idx: number) => void; size: SegmentedControlSizes };` — note activeIndex is a GETTER function (Pitfall 2: context stores getters not snapshots). `const SegmentedControlContext = createContext<SegmentedControlContextType>();`.
    Guard hook: `function useSegmentedControlContext() { const c = useContext(SegmentedControlContext); if (!c) throw new Error("SegmentedControl components must be used within <SegmentedControl> wrapper"); return c; }`.
    Root (`const Root: Component<SegmentedControlProps>`): props type `{ children?: JSX.Element; size?: SegmentedControlSizes; activeIndex?: number; setActiveIndex?: (idx: number) => void; class?: string }`. `const merged = mergeProps({ size: "md" } as const, props);` (import mergeProps too) `const [local] = splitProps(merged, ["children", "size", "activeIndex", "setActiveIndex", "class"]);`. Internal signal: `const [internal, setInternal] = createSignal(0);`. Derive getters: `const activeIndex = () => local.activeIndex ?? internal();` and `const setActiveIndex = (idx: number) => { if (local.setActiveIndex) local.setActiveIndex(idx); else setInternal(idx); };`. Provider value `{ activeIndex, setActiveIndex, size: local.size }`. Render `<SegmentedControlContext.Provider value={...}><div role="tablist" class={mergeClasses("moon-segmented-control", local.size !== "md" && \`moon-segmented-control-${local.size}\`, local.class)}>{local.children}</div></SegmentedControlContext.Provider>`.
    Item index handling: React injected index via Children.map. In Solid, do NOT clone. Keep the Item's `index?: number` prop (default via a counter). Reproduce React's auto-index by computing the index from DOM position is fragile; instead require Items to be rendered and self-assign using a module-scoped-per-instance counter is also fragile. PREFERRED parity approach: keep `index` prop on Item, and in Root use the Solid `children` helper to map children and assign index when missing — but cloneElement is forbidden. CLEANEST: have Item accept an optional `index` and, when omitted, read a running index from context. Implement: add `registerIndex` is overkill. Use this concrete, test-backed approach — Item requires its `index` to be derivable: keep `index?: number` prop; if a consumer passes index, use it; otherwise the Item resolves its index from its position among siblings via `getChildIndex` is not available. THEREFORE: change Item to read index from an explicit `index` prop that Root assigns by wrapping children with a `<For>` over `children(() => props.children).toArray()` and passing index — BUT For-cloning is also not idiomatic for arbitrary children. FINAL DECISION (lowest risk, parity-preserving): Root uses `children(() => local.children)` to resolve the child array, and renders them through `<Index>`/`.map` only to read position, providing each Item's index via context keyed by render order is not feasible. So adopt the documented Solid-idiomatic equivalent: require the `index` prop on Item in the public API IS a behavior change — AVOID. Instead implement index auto-assignment with the `children()` helper: `const resolved = children(() => local.children); const items = () => resolved.toArray();` and provide a context method `getIndexFor(el)`. This is complex.
    SIMPLEST CORRECT IMPLEMENTATION (do this): keep Item's `index?: number` and have Root assign indices by resolving children and re-creating Item elements with index via a thin map that re-instantiates each child as `<SegmentedControl.Item>` only when child is an Item — but cloneElement is forbidden. Because arbitrary-child index injection without cloneElement is genuinely non-trivial, IMPLEMENT name injection the Radio.Group way is not applicable (index differs per child).
    CONCRETE RESOLUTION the executor MUST follow: change the internal contract so each Item reads its index from context via a self-registration counter that is reset per Root render. Specifically: in the context add `register: () => number` backed by a `let counter = 0` closure in Root that increments and returns on each Item mount; Item calls `const index = ctx.register()` once at setup (component bodies run once in Solid, so this assigns a stable index in mount order). The Item then uses that index for active comparison and setActiveIndex. This preserves the public API (no required index prop), needs no cloneElement, and matches React's mount-order indexing. Keep the optional `index?: number` prop as an override (if provided, use it instead of register()).
    Item (`const Item: Component<SegmentProps>`): props `JSX.ButtonHTMLAttributes<HTMLButtonElement> & { children?: JSX.Element; class?: string; index?: number }`. `const ctx = useSegmentedControlContext(); const [local, rest] = splitProps(props, ["children", "class", "index"]); const index = local.index ?? ctx.register(); const isActive = () => ctx.activeIndex() === index;` return `<button role="tab" aria-selected={isActive()} class={mergeClasses("moon-segmented-control-item", isActive() && "moon-segmented-control-item-active", local.class)} onClick={() => ctx.setActiveIndex(index)} tabIndex={isActive() ? 0 : -1} {...rest}>{local.children}</button>`.
    Root must therefore expose `register` in context: `let counter = 0; const register = () => counter++;` included in the Provider value.
    Drop displayName. `const SegmentedControl = Object.assign(Root, { Item }); export default SegmentedControl;`.
    Write SegmentedControl.test.tsx: render `<SegmentedControl><SegmentedControl.Item>A</SegmentedControl.Item><SegmentedControl.Item>B</SegmentedControl.Item></SegmentedControl>`; assert wrapper div[role=tablist].moon-segmented-control, two button[role=tab]; first Item active (aria-selected="true", has moon-segmented-control-item-active, tabIndex 0), second inactive; fireEvent.click second button → second becomes active, first inactive (uncontrolled). Add a size="sm" test asserting "moon-segmented-control moon-segmented-control-sm". Add a controlled test: render with activeIndex={1} and a setActiveIndex spy; assert second active, and clicking first calls the spy with 0 while active item does NOT change without re-render (controlled).
  </action>
  <verify>
    <automated>cd packages; npx vitest run src/tests/atoms/SegmentedControl.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages; grep -c 'from "react"' src/components/SegmentedControl.tsx` returns 0
    - `cd packages; grep -c 'className' src/components/SegmentedControl.tsx` returns 0
    - `cd packages; grep -c 'React.Children\|cloneElement\|isValidElement' src/components/SegmentedControl.tsx` returns 0
    - `cd packages; grep -c 'createSignal' src/components/SegmentedControl.tsx` returns >= 1
    - `cd packages; grep -c 'createContext' src/components/SegmentedControl.tsx` returns 1
    - `cd packages; grep -c 'export type SegmentedControlSizes' src/components/SegmentedControl.tsx` returns 1
    - `cd packages; npx vitest run src/tests/atoms/SegmentedControl.test.tsx` passes (uncontrolled toggle + controlled + size)
  </acceptance_criteria>
  <done>SegmentedControl.tsx is React-free, replaces Children.map/cloneElement with a context-provided register() index + createSignal selection (getter-called in JSX), supports controlled and uncontrolled modes, preserves class parity and aria/tabIndex behavior, and SegmentedControl.test.tsx passes.</done>
</task>

</tasks>

<verification>
- `cd packages; npx vitest run src/tests/atoms/Radio.test.tsx src/tests/atoms/SegmentedControl.test.tsx` green
- Zero React.Children / cloneElement / isValidElement in either file (AF-4 satisfied)
- SegmentedControl uses createSignal with getter-call in JSX; context stores the activeIndex getter (not a snapshot)
- Exported type SegmentedControlSizes preserved
</verification>

<success_criteria>
Radio (+ Group name injection) and SegmentedControl (context + createSignal selection, controlled/uncontrolled) render with class-parity to React, contain no React imports / no Children.map / no cloneElement / no destructuring, and pass their Solid tests. Do NOT edit eslint.config.js, vitest.config.ts, components/index.ts, or src/index.ts — the final plan (03-05) owns those.
</success_criteria>

<output>
After completion, create `.planning/phases/03-stateful-atoms-carousel/03-03-SUMMARY.md`
</output>
