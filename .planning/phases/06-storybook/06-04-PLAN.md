---
phase: 06-storybook
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - docs/stories/components/Dialog.stories.tsx
  - docs/stories/components/Drawer.stories.tsx
  - docs/stories/components/BottomSheet.stories.tsx
  - docs/stories/components/Dropdown.stories.tsx
  - docs/stories/components/Tooltip.stories.tsx
  - docs/stories/components/Placeholder.stories.tsx
  - docs/stories/components/Radio.stories.tsx
  - docs/stories/components/Loader.stories.tsx
autonomous: true
requirements: [STORY-02]
user_setup: []

must_haves:
  truths:
    - "8 stories are Solid CSF (Dialog, Drawer, BottomSheet, Dropdown, Tooltip, Placeholder, Radio, Loader)"
    - "The 5 className-using stories (Dialog, Drawer, BottomSheet, Dropdown, Placeholder) use class not className"
    - "Portal compound stories (Dialog/Drawer/BottomSheet/Dropdown) render via @moondesignsystem/solid compound API"
  artifacts:
    - path: "docs/stories/components/Dialog.stories.tsx"
      provides: "Solid CSF Dialog story (class not className)"
      contains: "storybook-solidjs-vite"
    - path: "docs/stories/components/Dropdown.stories.tsx"
      provides: "Solid CSF Dropdown story"
      contains: "@moondesignsystem/solid"
  key_links:
    - from: "docs/stories/components/Dialog.stories.tsx"
      to: "@moondesignsystem/solid"
      via: "import { Dialog }"
      pattern: "@moondesignsystem/solid"
---

<objective>
Wave 2 bulk port (batch 3 of 4). The portal/className batch: Dialog, Drawer, BottomSheet, Dropdown (compound portal components, className-heavy), plus Tooltip, Placeholder, Radio, Loader.

Purpose: Mechanical React→Solid CSF translation per D-04. These stories carry the bulk of the `className`→`class` conversions. Runs in parallel with plans 02, 03, 05, 06 (disjoint files).
Output: 8 Solid stories with class-not-className.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-storybook/06-CONTEXT.md
@.planning/phases/06-storybook/06-RESEARCH.md
@.planning/phases/06-storybook/06-01-SUMMARY.md

<read_first>
- .planning/phases/06-storybook/06-RESEARCH.md — Pattern 3 (render signature), Pattern 4 (Button template), Pitfalls 1/5
- .planning/phases/06-storybook/06-01-SUMMARY.md — proven base + any --legacy-peer-deps note
- docs/stories/components/Button.stories.tsx — ported canonical template
- Each story being ported (Dialog/Drawer/BottomSheet/Dropdown/Tooltip/Placeholder/Radio/Loader .stories.tsx) — read before porting; Dialog/Drawer/BottomSheet/Dropdown/Placeholder use className
- packages/src/components — the Solid compound APIs (Dialog.Trigger/Content, Drawer, BottomSheet, Dropdown) the stories type against
</read_first>

<interfaces>
<!-- D-04 base translation (mirror Button.stories.tsx). KEY for this batch: -->
  className -> class   (on EVERY element — these 5 stories are className-heavy)
  <!-- compound components are imported by name from @moondesignsystem/solid (e.g. Dialog, Dialog.Trigger, Dialog.Content) -->
  <!-- .map() over arrays in render JSX -> <For each={...}>{(item)=>...}</For>  (Pitfall 5) -->
  render: (args) => <X {...args} />,   // NO double-wrapper
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port 4 portal compound stories — Dialog, Drawer, BottomSheet, Dropdown</name>
  <files>docs/stories/components/Dialog.stories.tsx, docs/stories/components/Drawer.stories.tsx, docs/stories/components/BottomSheet.stories.tsx, docs/stories/components/Dropdown.stories.tsx</files>
  <read_first>each of the 4 story files current; docs/stories/components/Button.stories.tsx; packages/src/components Dialog/Drawer/BottomSheet/Dropdown Solid compound exports</read_first>
  <action>
Apply the D-04 base translation to each (Meta/StoryObj from 'storybook-solidjs-vite'; compound component from '@moondesignsystem/solid'; ComponentProps from 'solid-js'; render returns &lt;X/&gt; directly, no double-wrapper). Convert EVERY `className`→`class` (these 4 are className-heavy). Convert any `.map()` in render JSX to `<For>` (import For from 'solid-js'). Keep the compound usage (`Dialog.Trigger`, `Dialog.Content`, `Dropdown.Options`, etc.) — the Solid lib exposes the same compound API names (Phase 4). Keep argTypes/args/LinksBlock docs.container identical.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for(const c of ['Dialog','Drawer','BottomSheet','Dropdown']){const f='docs/stories/components/'+c+'.stories.tsx'; const s=fs.readFileSync(f,'utf8'); if(/@storybook\/react|@moondesignsystem\/react|from .react.|import React/.test(s))throw new Error('react in '+c); if(!/storybook-solidjs-vite/.test(s)||!/@moondesignsystem\/solid/.test(s))throw new Error('not solid '+c); if(/className/.test(s))throw new Error('className in '+c); if(/=>\s*\(\s*\)\s*=>/.test(s))throw new Error('double-wrapper '+c);} console.log('portal batch ok')"</automated>
  </verify>
  <done>Dialog, Drawer, BottomSheet, Dropdown are Solid CSF; zero className; zero react import; compound API preserved; no double-wrapper.</done>
</task>

<task type="auto">
  <name>Task 2: Port 4 stories — Tooltip, Placeholder, Radio, Loader</name>
  <files>docs/stories/components/Tooltip.stories.tsx, docs/stories/components/Placeholder.stories.tsx, docs/stories/components/Radio.stories.tsx, docs/stories/components/Loader.stories.tsx</files>
  <read_first>each of the 4 story files current; docs/stories/components/Button.stories.tsx</read_first>
  <action>
Apply the D-04 base translation to each. Placeholder uses `className` (convert to `class`). Convert any `.map()` over arrays in render JSX to `<For>`. Radio may render a group — keep the compound API. Keep argTypes/args/LinksBlock identical.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for(const c of ['Tooltip','Placeholder','Radio','Loader']){const f='docs/stories/components/'+c+'.stories.tsx'; const s=fs.readFileSync(f,'utf8'); if(/@storybook\/react|@moondesignsystem\/react|from .react.|import React/.test(s))throw new Error('react in '+c); if(!/storybook-solidjs-vite/.test(s)||!/@moondesignsystem\/solid/.test(s))throw new Error('not solid '+c); if(/className/.test(s))throw new Error('className in '+c); if(/=>\s*\(\s*\)\s*=>/.test(s))throw new Error('double-wrapper '+c);} console.log('rest batch ok')"</automated>
  </verify>
  <done>Tooltip, Placeholder, Radio, Loader are Solid CSF; zero className (Placeholder converted); zero react import; no double-wrapper.</done>
</task>

</tasks>

<verification>
- grep across this plan's 8 stories: zero `@storybook/react` / `@moondesignsystem/react` / `from "react"` / `import React`; all have `storybook-solidjs-vite` + `@moondesignsystem/solid`; zero `className`; zero double-wrapper render.
</verification>

<success_criteria>
- 8 stories Solid with class-not-className; disjoint from other wave-2 plans; full build green finalized at Wave 3 gate (06-06).
</success_criteria>

<output>
After completion, create `.planning/phases/06-storybook/06-04-SUMMARY.md`.
</output>
