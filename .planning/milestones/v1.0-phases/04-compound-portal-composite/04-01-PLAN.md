---
phase: 04-compound-portal-composite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/src/components/Dialog.tsx
  - packages/src/components/Drawer.tsx
  - packages/src/components/BottomSheet.tsx
  - packages/src/tests/atoms/Dialog.test.tsx
  - packages/src/tests/atoms/Drawer.test.tsx
  - packages/src/tests/atoms/BottomSheet.test.tsx
autonomous: true
requirements: [CMPD-01, CMPD-02]

must_haves:
  truths:
    - "Dialog/Drawer/BottomSheet portal their <dialog> into document.body via <Portal>"
    - "Trigger opening a dialog calls dialogRef()?.showModal() through a signal-ref shared via context"
    - "Drawer.Trigger and BottomSheet.Trigger render a display:contents span wrapper (no React.cloneElement)"
    - "Close calls ref()?.close(); class output matches the React original exactly"
  artifacts:
    - path: "packages/src/components/Dialog.tsx"
      provides: "Solid Dialog compound with Portal + signal-ref context"
      contains: "createSignal"
    - path: "packages/src/components/Drawer.tsx"
      provides: "Solid Drawer compound with display:contents Trigger"
      contains: "display"
    - path: "packages/src/components/BottomSheet.tsx"
      provides: "Solid BottomSheet compound with display:contents Trigger"
      contains: "display"
  key_links:
    - from: "Trigger sub-components"
      to: "context signal getter"
      via: "ref()?.showModal()"
      pattern: "\\?\\(\\)\\?\\.showModal"
    - from: "Content sub-components"
      to: "document.body"
      via: "<Portal mount={document.body}>"
      pattern: "<Portal"
---

<objective>
Port the portal+ref family — Dialog, Drawer, BottomSheet — from React to SolidJS. These three share the exact same translation: `createPortal`→`<Portal>`, `useRef`+context→signal-wrapped ref shared via context (the getter), and (Drawer/BottomSheet only) `React.cloneElement`→`display:contents` span trigger.

Purpose: Establish and prove the D-01/D-02/D-03 portal-ref-trigger pattern; close the two highest-risk CMPD items.
Output: 3 ported components + 3 minimal Solid tests. Zero React imports, zero cloneElement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-compound-portal-composite/04-CONTEXT.md

<interfaces>
<!-- Canonical Solid pattern reused from Phase 3. Use directly; no codebase exploration. -->

Signal-ref via context (D-01) — context shares the GETTER, consumers call it:
```tsx
import { createSignal, useContext, createContext, type Accessor } from "solid-js";
const [dialogRef, setDialogRef] = createSignal<HTMLDialogElement>();
// <dialog ref={setDialogRef}> ...
// context value: { dialogRef }  (Accessor<HTMLDialogElement | undefined>)
// consumer: dialogRef()?.showModal() / dialogRef()?.close()
type DialogContextType = { dialogRef: Accessor<HTMLDialogElement | undefined> };
const DialogContext = createContext<DialogContextType>();
function useDialogContext() {
  const c = useContext(DialogContext);
  if (!c) throw new Error("Dialog components must be used within <Dialog>");
  return c;
}
```

Portal (D-02) — from solid-js/web:
```tsx
import { Portal } from "solid-js/web";
return (
  <Portal mount={document.body}>
    <dialog class={mergeClasses("moon-dialog")} ref={setDialogRef}>...</dialog>
  </Portal>
);
```

display:contents Trigger (D-03) for Drawer/BottomSheet — replaces cloneElement:
```tsx
const Trigger: Component<{ children?: JSX.Element }> = (props) => {
  const { drawerRef } = useDrawerContext();
  const [local] = splitProps(props, ["children"]);
  return (
    <span style={{ display: "contents" }} onClick={() => drawerRef()?.showModal()}>
      {local.children}
    </span>
  );
};
```

Canonical atom pattern (D-06) — every sub-component: `mergeProps`+`splitProps` (no destructure), `class` not `className`, inline `class={mergeClasses(...)}`, `{...rest}`, `JSX.*` types, `JSX.Element` for children, `Object.assign(Root, {...})` preserved. `import mergeClasses from "../helpers/mergeClasses"` (default import — confirmed in SegmentedControl.tsx).
</interfaces>

<test_pattern>
<!-- jsdom does NOT implement HTMLDialogElement.showModal/close — mock them per test file. -->
```tsx
import { render, fireEvent } from "@solidjs/testing-library";
import { describe, it, expect, vi, beforeAll } from "vitest";
import Dialog from "../../components/Dialog";

beforeAll(() => {
  HTMLDialogElement.prototype.showModal = vi.fn();
  HTMLDialogElement.prototype.close = vi.fn();
});
// render(() => <Dialog>...), assert container/document.body querySelector, fireEvent.click
```
</test_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port Dialog (Portal + signal-ref context, no clone)</name>
  <files>packages/src/components/Dialog.tsx</files>
  <read_first>
    - packages/src/components/Dialog.tsx (React source — parity target; read class strings + structure)
    - .planning/phases/04-compound-portal-composite/04-CONTEXT.md (D-01, D-02, D-06)
    - packages/src/components/SegmentedControl.tsx (Phase-3 createContext/useContext precedent)
    - packages/src/components/Alert.tsx (compound Object.assign + atom pattern precedent)
  </read_first>
  <action>
    Rewrite Dialog.tsx in SolidJS preserving exact class output and compound API (per D-01/D-02/D-06).
    - Replace `useRef`+`{ dialogRef }` context with `createSignal<HTMLDialogElement>()`; context type `{ dialogRef: Accessor<HTMLDialogElement | undefined> }`; Provider value `{ dialogRef }` (share the getter). Add `useDialogContext()` guard helper.
    - Root: `<DialogContext.Provider value={{ dialogRef }}>{local.children}</DialogContext.Provider>` (createSignal lives in Root).
    - Trigger: KEEP the `<p onClick={() => dialogRef()?.showModal()}>` wrapper (React Dialog did NOT clone — D-03). Use splitProps for children.
    - Content: `<Portal mount={document.body}>` from `solid-js/web` wrapping `<dialog class={mergeClasses("moon-dialog")} ref={setDialogRef}>` — BUT setDialogRef lives in Root, so wire the ref via context as the SETTER too, OR set the ref on the dialog in Root's scope. Simplest faithful form: keep `createSignal` in Root, expose BOTH `dialogRef` (getter) for Trigger/Close and `setDialogRef` for Content via context: `{ dialogRef, setDialogRef }`. Content does `ref={setDialogRef}`. Keep inner `<div class="moon-dialog-box">{children}</div>` and `<form method="dialog" class="moon-backdrop"><button></button></form>` unchanged.
    - Header: `<header class="moon-dialog-header">{children}</header>`.
    - Close: `<button class="moon-dialog-close" aria-label="Close" onClick={() => dialogRef()?.close()}><CloseIcon /></button>`.
    - `import CloseIcon from "../assets/icons/Close"` (already Solid). Every sub-component uses splitProps (no destructure). `Object.assign(Root, { Trigger, Content, Close, Header })`. Drop all `displayName` lines (React-only) OR keep as plain assignments — match Phase-3 components which dropped them; drop them.
    - ZERO `from "react"` / `react-dom`, zero `className`, zero `cloneElement`.
  </action>
  <verify>
    <automated>cd packages; node -e "const s=require('fs').readFileSync('src/components/Dialog.tsx','utf8'); const fail=[]; if(/from \"react/.test(s))fail.push('react import'); if(/react-dom/.test(s))fail.push('react-dom'); if(/className/.test(s))fail.push('className'); if(/cloneElement|React\.Children/.test(s))fail.push('clone'); if(!/<Portal/.test(s))fail.push('no Portal'); if(!/createSignal/.test(s))fail.push('no createSignal'); if(!/\?\(\)\?\.showModal|dialogRef\(\)\?\.showModal/.test(s))fail.push('no showModal via getter'); if(!/splitProps/.test(s))fail.push('no splitProps'); if(!/moon-dialog\b/.test(s))fail.push('class parity'); if(fail.length)throw new Error(fail.join(', ')); console.log('Dialog OK')"</automated>
  </verify>
  <done>Dialog.tsx is Solid: Portal mount=document.body, signal-ref via context (getter called for showModal/close), `<p>` Trigger, identical moon-dialog/-box/-header/-close + moon-backdrop classes, compound API preserved, zero React/className/cloneElement.</done>
</task>

<task type="auto">
  <name>Task 2: Port Drawer + BottomSheet (Portal + signal-ref + display:contents Trigger)</name>
  <files>packages/src/components/Drawer.tsx, packages/src/components/BottomSheet.tsx</files>
  <read_first>
    - packages/src/components/Drawer.tsx, packages/src/components/BottomSheet.tsx (React sources — parity targets)
    - .planning/phases/04-compound-portal-composite/04-CONTEXT.md (D-01, D-02, D-03, D-06)
    - packages/src/components/Dialog.tsx (just-ported sibling — reuse exact context+Portal form)
  </read_first>
  <action>
    Port BOTH files using the same D-01/D-02 pattern as Dialog, PLUS the D-03 display:contents Trigger (these two had `React.cloneElement`).
    Drawer.tsx:
    - Context `{ drawerRef: Accessor<HTMLDialogElement | undefined>, setDrawerRef }`; `useDrawerContext()` guard. createSignal in Root.
    - Trigger (REPLACE cloneElement): `<span style={{ display: "contents" }} onClick={() => drawerRef()?.showModal()}>{local.children}</span>`. splitProps children.
    - Header: `<div class={mergeClasses("moon-drawer-header", local.class)}>` (accept `class` prop, splitProps).
    - Content: `<Portal mount={document.body}><dialog class={mergeClasses("moon-drawer", local.class)} ref={setDrawerRef}><div class="moon-drawer-box">{children}</div><form method="dialog" class="moon-backdrop"><button></button></form></dialog></Portal>`.
    - Close: `<button class={mergeClasses("moon-drawer-close", local.class)} aria-label="Close" onClick={() => { drawerRef()?.close(); local.onClick?.(); }}><CloseIcon /></button>` — preserve the optional onClick chaining.
    - `Object.assign(Root, { Trigger, Content, Close, Header })`.
    BottomSheet.tsx (same family, plus `hasHandle`):
    - Context `{ bottomSheetRef, setBottomSheetRef, hasHandle: () => boolean }` (hasHandle reactive — pass getter). Root: `mergeProps({ hasHandle: false }, props)`.
    - Trigger: display:contents span calling `bottomSheetRef()?.showModal()`.
    - Content: Portal+dialog `moon-bottom-sheet` (+ caller class), `<div class="moon-bottom-sheet-box">` containing `<Show when={hasHandle()}><div class="moon-bottom-sheet-handle"></div></Show>` (D-05: Show for the boolean-flag handle) then `{children}`, then the moon-backdrop form. Import `Show` from solid-js.
    - Header: `<header class={mergeClasses("moon-bottom-sheet-header", local.class)}>`. Close: `moon-bottom-sheet-close` with onClick chaining like Drawer.
    - `Object.assign(Root, { Trigger, Content, Close, Header })`.
    Both: splitProps everywhere (no destructure), `class` not `className`, JSX.* types, CloseIcon default import, zero React/react-dom/cloneElement/Children, drop displayName.
  </action>
  <verify>
    <automated>cd packages; node -e "for(const [f,base] of [['src/components/Drawer.tsx','moon-drawer'],['src/components/BottomSheet.tsx','moon-bottom-sheet']]){const s=require('fs').readFileSync(f,'utf8');const fail=[];if(/from \"react/.test(s))fail.push('react');if(/react-dom/.test(s))fail.push('react-dom');if(/className/.test(s))fail.push('className');if(/cloneElement|React\.Children/.test(s))fail.push('clone');if(!/<Portal/.test(s))fail.push('Portal');if(!/createSignal/.test(s))fail.push('createSignal');if(!/display:\s*\"contents\"|display:\s*'contents'|\"display\":\s*\"contents\"/.test(s))fail.push('no display:contents span');if(!/\?\(\)\?\.showModal/.test(s))fail.push('showModal getter');if(!/splitProps/.test(s))fail.push('splitProps');if(!new RegExp(base).test(s))fail.push('class parity '+base);if(fail.length)throw new Error(f+': '+fail.join(', '));console.log(f+' OK');}"</automated>
  </verify>
  <done>Drawer.tsx + BottomSheet.tsx are Solid: Portal to body, signal-ref context, Trigger is a `display:contents` span calling ref()?.showModal() (zero cloneElement), BottomSheet handle via `<Show>`, all moon-drawer*/moon-bottom-sheet* classes preserved, compound API intact, zero React/className.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Minimal Solid tests for Dialog, Drawer, BottomSheet</name>
  <files>packages/src/tests/atoms/Dialog.test.tsx, packages/src/tests/atoms/Drawer.test.tsx, packages/src/tests/atoms/BottomSheet.test.tsx</files>
  <read_first>
    - packages/src/tests/atoms/SegmentedControl.test.tsx (test scaffold: render(() => ...), container.querySelector, fireEvent.click, vi)
    - packages/src/components/Dialog.tsx, Drawer.tsx, BottomSheet.tsx (just ported)
    - .planning/phases/04-compound-portal-composite/04-CONTEXT.md (D-08)
  </read_first>
  <behavior>
    - Portal mount: rendering Content mounts the `<dialog class="moon-dialog">` (resp. moon-drawer / moon-bottom-sheet) into document.body — assert `document.body.querySelector("dialog.moon-dialog")` is non-null.
    - signal-ref wiring: with `HTMLDialogElement.prototype.showModal = vi.fn()` mocked, clicking the Trigger calls showModal once.
    - Drawer/BottomSheet Trigger renders a `span[style*="display: contents"]` wrapping the child.
    - Close click calls `close()` (mocked).
    - Class parity: dialog element className equals the base class (+ caller class when passed).
  </behavior>
  <action>
    Write three minimal Solid test files in src/tests/atoms/ (already in vitest include glob). Each: `beforeAll` mocking `HTMLDialogElement.prototype.showModal`/`close` with `vi.fn()` (jsdom lacks them). Use `render(() => <Dialog><Dialog.Trigger>open</Dialog.Trigger><Dialog.Content><Dialog.Header>h</Dialog.Header><Dialog.Close/></Dialog.Content></Dialog>)`. Query `document.body.querySelector("dialog.moon-dialog")` for portal mount. fireEvent.click the trigger element, assert showModal mock called. For Drawer/BottomSheet additionally assert the Trigger wrapper is a span with display:contents style. Keep tests minimal (full suite is Phase 5). Clean up document.body between tests if needed (testing-library auto-cleanup handles component tree; portal nodes — query fresh each test).
  </action>
  <verify>
    <automated>cd packages; npx vitest run src/tests/atoms/Dialog.test.tsx src/tests/atoms/Drawer.test.tsx src/tests/atoms/BottomSheet.test.tsx</automated>
  </verify>
  <done>Three test files pass: portal mount to document.body asserted, showModal/close wired through the signal-ref, display:contents Trigger span asserted for Drawer/BottomSheet, class parity asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer children → portaled dialog | Arbitrary JSX children passed into Trigger/Content render inside a `<Portal>` mounted on document.body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Trigger span onClick | accept | Pure 1:1 port; onClick only calls ref()?.showModal() on the component's own dialog — no new attack surface vs React original |
| T-04-02 | Information disclosure | Portal mount=document.body | accept | Documented DOM divergence (display:contents span adds one node); no data crosses a trust boundary — same render target as React createPortal(document.body) |
</threat_model>

<verification>
- `grep -rn "from \"react"` across the 3 files returns nothing.
- No `cloneElement` / `React.Children` anywhere in the 3 files.
- All three dialogs portal to document.body; Trigger/Close call ref()?.showModal()/close().
- vitest passes the 3 new test files.
</verification>

<success_criteria>
- Dialog, Drawer, BottomSheet ported to Solid with `<Portal>`, signal-ref-via-context, and identical class output.
- Drawer.Trigger + BottomSheet.Trigger use `display:contents` span (CMPD-02); zero cloneElement.
- 3 minimal Solid tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-compound-portal-composite/04-01-SUMMARY.md`
</output>
