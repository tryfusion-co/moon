---
phase: 03-stateful-atoms-carousel
plan: 05
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified:
  - packages/src/components/index.ts
  - packages/src/index.ts
  - packages/eslint.config.js
autonomous: false
requirements: [FORM-01, FORM-02]

must_haves:
  truths:
    - "The barrel (components/index.ts) re-exports the 8 newly ported components and their preserved type names"
    - "src/index.ts explicitly re-exports the 8 new components (alongside the 12 atoms) and their types, keeping unported Phase-4 components out of the build graph"
    - "eslint.config.js no longer ignores the 8 new component files (negated un-ignore entries added)"
    - "npm run build emits .js + raw .jsx + .d.ts for the new components; vitest run is fully green; eslint . reports zero errors"
  artifacts:
    - path: "packages/src/index.ts"
      provides: "explicit export list extended with the 8 form/carousel components"
      contains: "Carousel"
    - path: "packages/eslint.config.js"
      provides: "un-ignore entries for the 8 new components"
      contains: "Checkbox.tsx"
  key_links:
    - from: "packages/src/index.ts"
      to: "packages/src/components/*"
      via: "explicit export { default as X } statements"
      pattern: "from \"./components/"
---

<objective>
Wire the 8 newly ported components (Checkbox, Input, Textarea, FormGroup, Switch, Radio, SegmentedControl, Carousel) into the package: regenerate the barrel, extend src/index.ts's explicit export list, un-ignore the new files in eslint, then run the full build(dual) + test + lint gate and verify green. This is the sequential final plan that owns the shared wiring files; it depends on all four Wave-1 port plans.

Purpose: Closes FORM-01 + FORM-02 by making the ported components part of the published build graph and proving the toolchain stays green with them included.
Output: Updated barrel + src/index.ts + eslint.config.js; passing build/test/lint gate confirmed by a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-stateful-atoms-carousel/03-CONTEXT.md
@.planning/phases/02-stateless-atoms/02-CONTEXT.md

<read_first>
MANDATORY before editing — read each:
- packages/src/index.ts — current explicit export list (12 atoms + types); extend it, keep the explicit approach
- packages/src/components/index.ts — auto-generated barrel (DO NOT hand-edit; regenerate via npm run barrels)
- packages/eslint.config.js — the ignores block with negated un-ignore entries (`!src/components/Button.tsx` ...); add the 8 new files
- packages/package.json — scripts: barrels (generate-barrel.cjs), build (vite build + build-solid-condition.mjs + tsc --emitDeclarationOnly), test (vitest run), lint (eslint .)
- The 8 ported files + their exported type names (from the Wave-1 SUMMARYs): Input (InputSizes/InputVariants), Textarea (TextareaSizes/TextareaVariants), Switch (SwitchSizes), SegmentedControl (SegmentedControlSizes), Carousel (ScrollDirections); Checkbox/Radio/FormGroup export no extracted types
</read_first>

<interfaces>
src/index.ts uses the explicit-export pattern (NOT export * from "./components") so unported Phase-4 components stay out of the build graph. Pattern per component:
```ts
export { default as Carousel } from "./components/Carousel";
export type { ScrollDirections } from "./components/Carousel";
```
The components/index.ts barrel is AUTO-GENERATED — already lists all files; regenerating it is safe and idempotent. The build graph is gated by src/index.ts, not the barrel.

eslint un-ignore pattern (negated entries inside the `ignores` array, matching the existing 12):
```js
"!src/components/Checkbox.tsx",
"!src/components/Input.tsx",
// ...etc for all 8
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate barrel, extend src/index.ts, un-ignore new components in eslint</name>
  <files>packages/src/components/index.ts, packages/src/index.ts, packages/eslint.config.js</files>
  <read_first>packages/src/index.ts, packages/eslint.config.js, packages/package.json (scripts)</read_first>
  <action>
    1. Regenerate the barrel: run `cd packages; npm run barrels`. This rewrites src/components/index.ts to re-export all component files including the 8 newly ported ones with their preserved type names. Do NOT hand-edit the barrel.
    2. Extend src/index.ts (explicit export list) — add, alphabetically alongside the existing 12 atoms, the 8 new components with their type exports:
       - `export { default as Carousel } from "./components/Carousel";` + `export type { ScrollDirections } from "./components/Carousel";`
       - `export { default as Checkbox } from "./components/Checkbox";`
       - `export { default as FormGroup } from "./components/FormGroup";`
       - `export { default as Input } from "./components/Input";` + `export type { InputSizes, InputVariants } from "./components/Input";`
       - `export { default as Radio } from "./components/Radio";`
       - `export { default as SegmentedControl } from "./components/SegmentedControl";` + `export type { SegmentedControlSizes } from "./components/SegmentedControl";`
       - `export { default as Switch } from "./components/Switch";` + `export type { SwitchSizes } from "./components/Switch";`
       - `export { default as Textarea } from "./components/Textarea";` + `export type { TextareaSizes, TextareaVariants } from "./components/Textarea";`
       Keep the explicit-export approach — do NOT switch to `export * from "./components"` (that would pull unported Phase-4 components into the build graph). Verify the type names against the actual exports in each ported file (they were preserved in Wave 1).
    3. In packages/eslint.config.js, add 8 negated un-ignore entries to the `ignores` array (next to the existing `!src/components/Button.tsx` block), one per new component file: `!src/components/Checkbox.tsx`, `!src/components/Input.tsx`, `!src/components/Textarea.tsx`, `!src/components/FormGroup.tsx`, `!src/components/Switch.tsx`, `!src/components/Radio.tsx`, `!src/components/SegmentedControl.tsx`, `!src/components/Carousel.tsx`. Leave the legacy React test-file ignores untouched.
  </action>
  <verify>
    <automated>cd packages; node -e "const s=require('fs').readFileSync('src/index.ts','utf8'); ['Carousel','Checkbox','FormGroup','Input','Radio','SegmentedControl','Switch','Textarea'].forEach(n=>{if(!s.includes('components/'+n))throw new Error('missing '+n)}); console.log('index ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages; grep -c 'from "./components/Carousel"' src/index.ts` returns >= 1 (and same for Checkbox, FormGroup, Input, Radio, SegmentedControl, Switch, Textarea)
    - `cd packages; grep -c 'export type { ScrollDirections }' src/index.ts` returns 1
    - `cd packages; grep -c '"!src/components/Carousel.tsx"' eslint.config.js` returns 1 (and same for the other 7 new files)
    - `cd packages; grep -c 'export \* from "./components"' src/index.ts` returns 0 (explicit approach preserved)
  </acceptance_criteria>
  <done>Barrel regenerated; src/index.ts explicitly exports all 8 new components + their preserved types; eslint un-ignores all 8 new component files; explicit-export approach preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Run the build(dual) + test + lint gate</name>
  <files>packages/src/index.ts</files>
  <read_first>packages/package.json (build/test/lint scripts)</read_first>
  <action>
    Run the three gate commands from packages/ and ensure all are green. Fix any failures by correcting the ported components or wiring (do NOT loosen eslint rules or remove tests):
    1. `cd packages; npm run build` — must complete with zero errors and emit, for each of the 8 new components, a compiled `.js`, a raw-JSX `.jsx` (from build-solid-condition.mjs), and a `.d.ts` (from tsc --emitDeclarationOnly) under dist/.
    2. `cd packages; npm run test` — full `vitest run`; all included tests green (toolchain + all atoms/**/*.test.tsx including the 6 new form tests + Carousel).
    3. `cd packages; npm run lint` — `eslint .`; zero errors. solid/no-destructure must report no violations across the 8 new files. (The 4 pre-existing Phase-2 warnings in Alert.tsx/LinearProgress.tsx are acceptable per STATE.md — zero ERRORS is the bar.)
    If build fails on a missing/incorrect type export, reconcile src/index.ts with the actual exported names in the component file. If lint fails on a new file, fix the component (e.g., a stray destructure), not the config.
  </action>
  <verify>
    <automated>cd packages; npm run build && npm run test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages; npm run build` exits 0
    - `cd packages; ls dist/components/Carousel.jsx dist/components/Carousel.js dist/components/Carousel.d.ts` all exist (dual build emitted for the new components; adjust path if dist layout differs — confirm .jsx raw-JSX output exists for at least the new components)
    - `cd packages; npm run test` exits 0 (vitest run all green)
    - `cd packages; npm run lint` exits 0 (zero eslint errors)
  </acceptance_criteria>
  <done>npm run build, npm run test, and npm run lint all exit 0; dual build artifacts (.js + .jsx + .d.ts) exist for the new components; zero eslint errors including zero solid/no-destructure violations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 3 complete: 8 components ported to SolidJS (Checkbox, Input, Textarea, FormGroup, Switch, Radio, SegmentedControl, Carousel), wired into the barrel + src/index.ts explicit export list, un-ignored in eslint, and the full build(dual)+test+lint gate is green.
  </what-built>
  <how-to-verify>
    From packages/ confirm the gate (each should exit 0):
    1. `cd packages; npm run build` — expect dual artifacts (.js + .jsx + .d.ts) for the new components under dist/.
    2. `cd packages; npm run test` — expect all vitest files passing (the 8 new components have render/class-parity tests; Carousel test asserts scroll-state + listener cleanup; SegmentedControl test asserts controlled + uncontrolled toggle).
    3. `cd packages; npm run lint` — expect zero errors (4 known Phase-2 warnings acceptable).
    Spot-check parity: open Input.tsx / Carousel.tsx and confirm zero `from "react"`, `class` (not className), Carousel uses `let reelRef` + createSignal + onMount/onCleanup with no useEffect/useCallback/useRef.
    Confirm src/index.ts still uses explicit exports (no `export * from "./components"`), so unported Phase-4 components are not pulled into the build.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 3 complete, or describe any failing gate / parity issue to fix.</resume-signal>
</task>

</tasks>

<verification>
- npm run build (dual .js+.jsx+.d.ts), npm run test (vitest all green), npm run lint (zero errors) all pass with the 8 new components included
- src/index.ts retains explicit-export approach; barrel regenerated; eslint un-ignores all 8
- FORM-01 (7 form atoms) + FORM-02 (Carousel) wired and verified
</verification>

<success_criteria>
All 8 ported components are part of the build graph via the explicit src/index.ts export list, the regenerated barrel re-exports them with preserved type names, eslint lints them (zero errors, zero solid/no-destructure violations), and the dual build emits .js + .jsx + .d.ts for each. The human-verify checkpoint confirms the green gate, closing FORM-01 and FORM-02.
</success_criteria>

<output>
After completion, create `.planning/phases/03-stateful-atoms-carousel/03-05-SUMMARY.md`
</output>
