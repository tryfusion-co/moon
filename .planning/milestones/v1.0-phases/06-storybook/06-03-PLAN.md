---
phase: 06-storybook
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - docs/stories/components/Chip.stories.tsx
  - docs/stories/components/SegmentedControl.stories.tsx
  - docs/stories/components/Snackbar.stories.tsx
  - docs/stories/components/Carousel.stories.tsx
  - docs/stories/components/Checkbox.stories.tsx
  - docs/stories/components/Input.stories.tsx
  - docs/stories/components/Textarea.stories.tsx
  - docs/stories/components/Switch.stories.tsx
autonomous: true
requirements: [STORY-02]
user_setup: []

must_haves:
  truths:
    - "8 stories are Solid CSF; the 3 stateful ones (Chip, SegmentedControl, Snackbar) use createSignal not useState"
    - "Carousel story uses class not className"
    - "Stateful story render bodies use a Solid signal getter, not React useState destructure"
  artifacts:
    - path: "docs/stories/components/Snackbar.stories.tsx"
      provides: "Solid CSF Snackbar story with createSignal-based local state"
      contains: "createSignal"
    - path: "docs/stories/components/Chip.stories.tsx"
      provides: "Solid CSF Chip story with createSignal"
      contains: "createSignal"
  key_links:
    - from: "docs/stories/components/Snackbar.stories.tsx"
      to: "solid-js"
      via: "import { createSignal }"
      pattern: "createSignal"
---

<objective>
Wave 2 bulk port (batch 2 of 4). The stateful batch: Chip, SegmentedControl, and Snackbar use React `useState` in their render helpers and must convert to Solid `createSignal`; plus the simpler Carousel/Checkbox/Input/Textarea/Switch ports.

Purpose: Mechanical React→Solid CSF translation per D-04, with the useState→createSignal conversion being the only non-trivial part. Runs in parallel with plans 02, 04, 05, 06 (disjoint files).
Output: 8 Solid stories; stateful ones use createSignal signals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-storybook/06-CONTEXT.md
@.planning/phases/06-storybook/06-RESEARCH.md
@.planning/phases/06-storybook/06-01-SUMMARY.md

<read_first>
- .planning/phases/06-storybook/06-RESEARCH.md — Pattern 3 (render signature), Pattern 4 (Button template), Pitfalls 1/5; the @tanstack/solid-table mapping table illustrates useState→createSignal + getter idiom
- .planning/phases/06-storybook/06-01-SUMMARY.md — proven base + any --legacy-peer-deps note
- docs/stories/components/Button.stories.tsx — ported canonical template
- Each story being ported (Chip/SegmentedControl/Snackbar/Carousel/Checkbox/Input/Textarea/Switch .stories.tsx) — read before porting; Chip/SegmentedControl/Snackbar contain useState
- packages/src/components — Solid component props the stories type against (Snackbar/SegmentedControl/Chip APIs)
</read_first>

<interfaces>
<!-- D-04 base translation (mirror Button.stories.tsx). PLUS stateful conversion: -->
  // React:  const [open, setOpen] = useState(false);  ... open ... setOpen(true)
  // Solid:  const [open, setOpen] = createSignal(false); ... open() ... setOpen(true)
  import { createSignal } from 'solid-js';   // replaces  import { useState } from 'react'
<!-- Every read of the signal becomes a CALL: open -> open(). Setter calls are unchanged. -->
<!-- For arrays rendered in JSX use <For each={arr()}>{(item)=>...}</For> not .map (Pitfall 5). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the 3 stateful stories — Chip, SegmentedControl, Snackbar (useState → createSignal)</name>
  <files>docs/stories/components/Chip.stories.tsx, docs/stories/components/SegmentedControl.stories.tsx, docs/stories/components/Snackbar.stories.tsx</files>
  <read_first>Chip/SegmentedControl/Snackbar .stories.tsx current (all contain useState); docs/stories/components/Button.stories.tsx</read_first>
  <action>
Apply the D-04 base translation (Meta/StoryObj from 'storybook-solidjs-vite'; component from '@moondesignsystem/solid'; ComponentProps from 'solid-js'; render returns &lt;X/&gt; directly, no double-wrapper). THEN the stateful conversion:
- Replace `import { useState } from 'react'` with `import { createSignal } from 'solid-js'` (and add `For` if any array is iterated in JSX).
- Each `const [v, setV] = useState(init)` → `const [v, setV] = createSignal(init)`.
- Every READ of the value becomes a call: `v` → `v()`. Setters (`setV(x)`) unchanged.
- Snackbar has multiple signals (localIsOpen ×2, snackbars array, nextId) — convert each; the `snackbars` array rendered in JSX must use `<For each={snackbars()}>{(s)=>...}</For>` (not .map).
- SegmentedControl localActiveIndex and Chip localActive: same call-on-read conversion.
- Convert any `className`→`class`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for(const c of ['Chip','SegmentedControl','Snackbar']){const f='docs/stories/components/'+c+'.stories.tsx'; const s=fs.readFileSync(f,'utf8'); if(/from .react.|import React|useState|@storybook\/react|@moondesignsystem\/react/.test(s))throw new Error('react remnant in '+c); if(!/createSignal/.test(s)||!/storybook-solidjs-vite/.test(s)||!/@moondesignsystem\/solid/.test(s))throw new Error('not solid '+c); if(/className/.test(s))throw new Error('className in '+c);} console.log('stateful ok')"</automated>
  </verify>
  <done>Chip, SegmentedControl, Snackbar use createSignal (zero useState/react import), are Solid CSF, no className, no double-wrapper.</done>
</task>

<task type="auto">
  <name>Task 2: Port 5 simpler stories — Carousel, Checkbox, Input, Textarea, Switch</name>
  <files>docs/stories/components/Carousel.stories.tsx, docs/stories/components/Checkbox.stories.tsx, docs/stories/components/Input.stories.tsx, docs/stories/components/Textarea.stories.tsx, docs/stories/components/Switch.stories.tsx</files>
  <read_first>each of the 5 story files current; docs/stories/components/Button.stories.tsx</read_first>
  <action>
Apply the D-04 base translation to each. Carousel uses `className` (convert to `class`). Convert any `.map()` over arrays in render JSX to `<For>`. These have no useState. Keep argTypes/args/LinksBlock docs.container structurally identical. Any React event-handler types → Solid (onInput for text inputs already handled in the lib; story render fns just pass args).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for(const c of ['Carousel','Checkbox','Input','Textarea','Switch']){const f='docs/stories/components/'+c+'.stories.tsx'; const s=fs.readFileSync(f,'utf8'); if(/@storybook\/react|@moondesignsystem\/react|from .react.|import React/.test(s))throw new Error('react in '+c); if(!/storybook-solidjs-vite/.test(s)||!/@moondesignsystem\/solid/.test(s))throw new Error('not solid '+c); if(/className/.test(s))throw new Error('className in '+c); if(/=>\s*\(\s*\)\s*=>/.test(s))throw new Error('double-wrapper '+c);} console.log('simple batch ok')"</automated>
  </verify>
  <done>Carousel, Checkbox, Input, Textarea, Switch are Solid CSF; no className (Carousel converted); no react import; no double-wrapper.</done>
</task>

</tasks>

<verification>
- grep across this plan's 8 stories: zero `@storybook/react` / `@moondesignsystem/react` / `from "react"` / `import React` / `useState`; all have `storybook-solidjs-vite` + `@moondesignsystem/solid`; the 3 stateful ones have `createSignal`; zero `className`; zero double-wrapper render.
</verification>

<success_criteria>
- 8 stories Solid; stateful trio uses createSignal; disjoint from other wave-2 plans; full build green finalized at Wave 3 gate (06-06).
</success_criteria>

<output>
After completion, create `.planning/phases/06-storybook/06-03-SUMMARY.md`.
</output>
