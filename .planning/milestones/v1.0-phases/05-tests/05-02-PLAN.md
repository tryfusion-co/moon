---
phase: 05-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/src/tests/atoms/Button.test.tsx
  - packages/src/tests/atoms/CircularProgress.test.tsx
  - packages/src/tests/atoms/Dialog.test.tsx
  - packages/src/tests/atoms/Drawer.test.tsx
  - packages/src/tests/atoms/Dropdown.test.tsx
  - packages/src/tests/atoms/Chip.test.tsx
  - packages/src/tests/atoms/FormGroup.test.tsx
  - packages/src/tests/atoms/Input.test.tsx
  - packages/src/tests/atoms/LinearProgress.test.tsx
  - packages/src/tests/Button.test.tsx
  - packages/src/tests/CircularProgress.test.tsx
  - packages/src/tests/Dialog.test.tsx
  - packages/src/tests/Drawer.test.tsx
  - packages/src/tests/Dropdown.test.tsx
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "Button, CircularProgress, Dialog, Drawer, Dropdown each have exactly ONE Solid test (atoms/) with legacy behavioral coverage merged (onClick handler, open/close, portal mounting)"
    - "Chip, FormGroup, Input, LinearProgress atom tests verified at-parity"
    - "No flat legacy file remains for the 5 merged components"
    - "Portal components (Dialog/Drawer/Dropdown) query document.body for portalled content; jsdom showModal mocked with vi.fn"
  artifacts:
    - path: "packages/src/tests/atoms/Button.test.tsx"
      provides: "Single Solid Button test incl. legacy onClick(vi.fn) handler + variant/size/context matrix"
      contains: "render(() =>"
    - path: "packages/src/tests/atoms/Dialog.test.tsx"
      provides: "Single Solid Dialog test: portal-to-body + Trigger showModal + Close + Header (merged)"
    - path: "packages/src/tests/atoms/Drawer.test.tsx"
      provides: "Single Solid Drawer test merged with legacy open/close coverage"
    - path: "packages/src/tests/atoms/Dropdown.test.tsx"
      provides: "Single Solid Dropdown test merged with legacy trigger/content coverage"
  key_links:
    - from: "packages/src/tests/atoms/*.test.tsx"
      to: "packages/src/components/*.tsx"
      via: "render(() => <X/>) + document.body query for portals"
      pattern: "render\\(\\(\\) =>"
---

<objective>
Migrate and merge the legacy React tests for Button, CircularProgress, Dialog, Drawer, Dropdown into their existing Solid atom tests (one authoritative file each in `src/tests/atoms/`), preserving richer legacy coverage (Button onClick handler + variant matrix; Dialog/Drawer/Dropdown open-close + portal behavior). Verify atom-only tests for Chip, FormGroup, Input, LinearProgress are at parity. Delete the 5 flat legacy files.

Purpose: Phase 5 goal — ONE authoritative Solid test per component, parity ≥ legacy (D-01, D-07). Dialog/Drawer/Dropdown are Portal components requiring `document.body` queries + jsdom dialog mocks (D-03, PITFALLS #8/#14e).
Output: 9 verified single-source atom test files; 5 flat legacy files deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tests/05-CONTEXT.md
@.planning/research/PITFALLS.md

<consolidation_decision>
CHOSEN DIRECTORY: `packages/src/tests/atoms/` (consolidate DOWN). Vitest `include` already covers `src/tests/atoms/**`, so merged tests run immediately — NO config edit here. EDIT the existing `atoms/X.test.tsx` (enrich with legacy coverage), DELETE the flat legacy `src/tests/X.test.tsx`. DO NOT touch `vitest.config.ts` or `eslint.config.js` — plan 05-05 owns those.
</consolidation_decision>

<translation_rules>
Per D-03 / PITFALLS #14:
- `@testing-library/react` → `@solidjs/testing-library`
- `render(<X/>)` → `render(() => <X/>)` (MANDATORY)
- `jest.fn()` → `vi.fn()`, `jest.spyOn` → `vi.spyOn`; import `{ vi }` from "vitest"
- `className=` → `class=`; keep jest-dom matchers; rely on auto-cleanup (no manual cleanup)
- Portal components (Dialog/Drawer/Dropdown): content portals to `document.body` — query via `document.body.querySelector(...)` not just the render container (PITFALLS #8).
- jsdom does NOT implement `HTMLDialogElement.prototype.showModal/close` — mock with `vi.fn()` in `beforeAll` (PITFALLS #14e). Pattern already present in `atoms/Dialog.test.tsx`.
</translation_rules>

<interfaces>
Reference Solid test style + merged portal example (already correct — match these):
@packages/src/tests/atoms/Button.test.tsx
@packages/src/tests/atoms/Dialog.test.tsx
Legacy React style being migrated FROM:
@packages/src/tests/Button.test.tsx
@packages/src/tests/Dialog.test.tsx
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Merge legacy Button + CircularProgress; verify Chip/LinearProgress atom parity</name>
  <files>packages/src/tests/atoms/Button.test.tsx, packages/src/tests/atoms/CircularProgress.test.tsx, packages/src/tests/atoms/Chip.test.tsx, packages/src/tests/atoms/LinearProgress.test.tsx, packages/src/tests/Button.test.tsx, packages/src/tests/CircularProgress.test.tsx</files>
  <read_first>
    - packages/src/tests/Button.test.tsx (legacy: default render+role, variant/size/context matrix, onClick via jest.fn, extra props/disabled)
    - packages/src/tests/CircularProgress.test.tsx (legacy coverage)
    - packages/src/tests/atoms/Button.test.tsx (existing Solid: base class, modifier-class order, local.class last, rest-spread disabled)
    - packages/src/tests/atoms/CircularProgress.test.tsx, atoms/Chip.test.tsx, atoms/LinearProgress.test.tsx (existing Solid coverage)
    - packages/src/components/Button.tsx, CircularProgress.tsx, Chip.tsx, LinearProgress.tsx (parity targets)
  </read_first>
  <action>
    - Button: the atom test already covers class-order, local.class, rest-spread. ADD the legacy-only coverage translated to Solid: the `onClick` handler test using `vi.fn()` (render(() => <Button onClick={fn}>Click</Button>), `fireEvent.click`, assert `toHaveBeenCalledTimes(1)`), and the variant/size/`context` matrix if the atom file doesn't already cover those exact combos. Drop exact-duplicate assertions. Then `git rm src/tests/Button.test.tsx`.
    - CircularProgress: merge legacy assertions into the atom file; `git rm src/tests/CircularProgress.test.tsx`.
    - Chip, LinearProgress: atom-only (no legacy file). Verify render + key class/DOM + primary behavior coverage is adequate; enrich only if a clearly-supported primary behavior is unasserted. Do NOT delete — no flat legacy file exists.
  </action>
  <verify>
    <automated>cd packages && npx vitest run src/tests/atoms/Button.test.tsx src/tests/atoms/CircularProgress.test.tsx src/tests/atoms/Chip.test.tsx src/tests/atoms/LinearProgress.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The vitest run above exits 0
    - Flat files gone: `test ! -f packages/src/tests/Button.test.tsx && test ! -f packages/src/tests/CircularProgress.test.tsx`
    - Button atom test fires `fireEvent.click` against a `vi.fn()` and asserts call count (legacy onClick coverage preserved)
    - `grep -rl "@testing-library/react" packages/src/tests/atoms/Button.test.tsx packages/src/tests/atoms/CircularProgress.test.tsx` returns nothing
    - `grep -n "jest\." packages/src/tests/atoms/Button.test.tsx packages/src/tests/atoms/CircularProgress.test.tsx` returns nothing
  </acceptance_criteria>
  <done>Button (with onClick + matrix) and CircularProgress single-source merged; Chip/LinearProgress verified; flat legacy Button/CircularProgress removed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Merge legacy Dialog + Drawer + Dropdown (portals); verify FormGroup/Input atom parity</name>
  <files>packages/src/tests/atoms/Dialog.test.tsx, packages/src/tests/atoms/Drawer.test.tsx, packages/src/tests/atoms/Dropdown.test.tsx, packages/src/tests/atoms/FormGroup.test.tsx, packages/src/tests/atoms/Input.test.tsx, packages/src/tests/Dialog.test.tsx, packages/src/tests/Drawer.test.tsx, packages/src/tests/Dropdown.test.tsx</files>
  <read_first>
    - packages/src/tests/Dialog.test.tsx (legacy: trigger render, show-on-click, close-on-close-click, header) — note legacy mocks showModal/close/show via jest.fn in beforeAll
    - packages/src/tests/Drawer.test.tsx, packages/src/tests/Dropdown.test.tsx (legacy coverage)
    - packages/src/tests/atoms/Dialog.test.tsx (existing Solid: portal-to-body, showModal mock via vi.fn, Trigger/Close/Header/box/backdrop) — already strong
    - packages/src/tests/atoms/Drawer.test.tsx, atoms/Dropdown.test.tsx, atoms/FormGroup.test.tsx, atoms/Input.test.tsx (existing Solid coverage)
    - packages/src/components/Dialog.tsx, Drawer.tsx, Dropdown.tsx, FormGroup.tsx, Input.tsx (parity targets — confirm portal mount, trigger element type, close button class)
  </read_first>
  <action>
    - Dialog: the atom test is already comprehensive (portal, showModal, close, header, box, backdrop). Cross-check the legacy test for any behavior NOT covered (e.g. an explicit "trigger renders text" assertion) and add only the gaps. Then `git rm src/tests/Dialog.test.tsx`.
    - Drawer, Dropdown: merge legacy behavioral coverage (open/close, trigger→content, portal mounting) into the atom files. Query portalled content via `document.body` (PITFALLS #8). Mock `HTMLDialogElement.showModal/close` with `vi.fn()` in `beforeAll` where the component uses a `<dialog>` (Drawer). `git rm src/tests/Drawer.test.tsx` and `src/tests/Dropdown.test.tsx`.
    - FormGroup, Input: atom-only (no legacy). Verify render + class + primary behavior. Input is a text input — if the atom test fires a value change it MUST use `fireEvent.input` (not change) per D-03/PITFALLS #9. Enrich only if a primary behavior is unasserted. Do NOT delete.
  </action>
  <verify>
    <automated>cd packages && npx vitest run src/tests/atoms/Dialog.test.tsx src/tests/atoms/Drawer.test.tsx src/tests/atoms/Dropdown.test.tsx src/tests/atoms/FormGroup.test.tsx src/tests/atoms/Input.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The vitest run above exits 0
    - Flat files gone: `test ! -f packages/src/tests/Dialog.test.tsx && test ! -f packages/src/tests/Drawer.test.tsx && test ! -f packages/src/tests/Dropdown.test.tsx`
    - `grep -rl "@testing-library/react" packages/src/tests/atoms/Dialog.test.tsx packages/src/tests/atoms/Drawer.test.tsx packages/src/tests/atoms/Dropdown.test.tsx packages/src/tests/atoms/FormGroup.test.tsx packages/src/tests/atoms/Input.test.tsx` returns nothing
    - `grep -n "jest\." packages/src/tests/atoms/Dialog.test.tsx packages/src/tests/atoms/Drawer.test.tsx packages/src/tests/atoms/Dropdown.test.tsx` returns nothing
    - Drawer/Dropdown atom tests query `document.body` for portalled content; Input atom test uses `fireEvent.input` if it fires a value change
    - All five files contain `render(() =>`
  </acceptance_criteria>
  <done>Dialog/Drawer/Dropdown single-source merged with portal-aware queries; FormGroup/Input verified; flat legacy Dialog/Drawer/Dropdown removed.</done>
</task>

</tasks>

<verification>
- `cd packages && npx vitest run src/tests/atoms/` passes for all files this plan touched
- No `@testing-library/react` import, no `jest.` reference in any file this plan owns
- The 5 flat legacy files owned by this plan are deleted
</verification>

<success_criteria>
- Button, CircularProgress, Dialog, Drawer, Dropdown each = ONE Solid test ≥ legacy coverage (Button onClick preserved; portals query body)
- Chip, FormGroup, Input, LinearProgress atom tests verified at parity (Input uses onInput)
- 5 flat legacy files removed via git rm
- All touched tests use render(() => <X/>), vi.*, @solidjs/testing-library
</success_criteria>

<output>
After completion, create `.planning/phases/05-tests/05-02-SUMMARY.md`
</output>
