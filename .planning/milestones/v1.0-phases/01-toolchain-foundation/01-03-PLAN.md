---
phase: 01-toolchain-foundation
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - packages/src/tests/toolchain.test.tsx
  - packages/src/_destructure-probe.tsx
autonomous: false
requirements: [TOOL-07]

must_haves:
  truths:
    - "vite build --lib completes with zero errors, emitting dist/index.js, dist/index.jsx, and dist/index.d.ts"
    - "the solid export condition in package.json resolves to the preserved-JSX dist/index.jsx output"
    - "vitest run passes on a stub/barrel test with no 'Client-only API called on server side' failure"
    - "eslint runs green on the stub source"
    - "eslint-plugin-solid/no-destructure fires on a deliberate destructure probe, confirming the rule is active"
    - "the repo is left in a green build+test+lint state with the probe removed"
  artifacts:
    - path: "packages/src/tests/toolchain.test.tsx"
      provides: "minimal Solid render test proving the test pipeline works end-to-end"
      contains: "render"
    - path: "packages/dist/index.jsx"
      provides: "preserved-JSX output for the solid export condition (build artifact)"
      contains: "MOON_SOLID_TOOLCHAIN_READY"
  key_links:
    - from: "packages/package.json exports.solid"
      to: "packages/dist/index.jsx"
      via: "vite build emits preserveModules .jsx entry"
      pattern: "index.jsx"
    - from: "packages/vitest.config.ts resolve.conditions"
      to: "solid-js browser build"
      via: "browser conditions prevent server-build resolution under jsdom"
      pattern: "browser"
---

<objective>
Prove the Solid toolchain works end-to-end BEFORE any component is ported (TOOL-07 / D-12). Run `vite build --lib` and confirm it emits ESM `.js` + preserved-JSX `.jsx` (the `solid` condition target) + `.d.ts`. Add a minimal Solid render test and run `vitest run` to confirm the test pipeline works with no "Client-only API called on server side" failure. Run `eslint` green on the stub. Then add a deliberate prop-destructure probe to confirm `solid/no-destructure` fires, and remove it so the repo is left green. This is the terminal validation gate for the phase.

Purpose: Validating the pipeline independently on a stub entry means the hardest-to-diagnose toolchain failures (missing solid condition, wrong resolve.conditions, inactive no-destructure rule) are caught here, not buried inside component work in Phases 2-4.
Output: Proven-green build/test/lint pipeline; confirmation the no-destructure guard is active; clean repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-toolchain-foundation/01-CONTEXT.md
@.planning/research/PITFALLS.md

# Depends on plan 01 (toolchain configs + stub entry) and plan 02 (foundation/icons).
# Locked decision implemented here: D-12 (validation gate).
# At this point src/index.ts exports from ./_stub (set in plan 01); the build/test target the stub, not the 37 components.

<interfaces>
<!-- Stub entry (from plan 01): packages/src/_stub.ts exports `MOON_SOLID_TOOLCHAIN_READY = true as const`. -->
<!-- src/index.ts: `export * from "./_stub";` -->
<!-- Vitest is configured (plan 01 vitest.config.ts) with @solidjs/testing-library available and globals: true. -->
<!-- @solidjs/testing-library render() takes a FUNCTION: render(() => <Component />). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a minimal Solid render test and prove build + test + lint green</name>
  <files>packages/src/tests/toolchain.test.tsx</files>
  <read_first>
    - packages/src/_stub.ts (the stub entry under test)
    - packages/vitest.config.ts (confirms render conditions + setup)
    - packages/vite.config.ts (confirms lib build output)
    - .planning/research/PITFALLS.md (Pitfall 14a — render(() => <Comp/>) function-wrapper form; Pitfall 4/10 — resolve.conditions)
    - .planning/phases/01-toolchain-foundation/01-CONTEXT.md (D-12)
  </read_first>
  <action>
    Create packages/src/tests/toolchain.test.tsx — a minimal Solid render test that exercises the @solidjs/testing-library + vite-plugin-solid + jsdom pipeline (this proves the resolve.conditions fix works; a render of a Solid component touches client-only Solid APIs that would throw "Client-only API called on the server side" if the browser conditions were wrong):
    ```tsx
    import { render } from "@solidjs/testing-library";
    import { describe, it, expect } from "vitest";
    import type { Component } from "solid-js";

    const Probe: Component = () => <div data-testid="probe">ok</div>;

    describe("toolchain", () => {
      it("renders a Solid component in jsdom without server-build errors", () => {
        const { getByTestId } = render(() => <Probe />);
        expect(getByTestId("probe").textContent).toBe("ok");
      });
    });
    ```

    Then run, IN ORDER, from packages/:
    1. `npm run build` — expect exit 0; confirm dist/index.js, dist/index.jsx, and dist/index.d.ts exist. The dist/index.jsx is the `solid` export-condition target and must contain preserved JSX / the stub export.
    2. `npx vitest run` — expect exit 0, the toolchain test passing, and NO "Client-only API called on the server side" in output.
    3. `npx eslint .` — expect exit 0 (green) on the stub + icon + foundation source. Fix any genuine lint errors surfaced in the ported icons (e.g., an accidental unused var) so eslint is green; do NOT disable solid/no-destructure or any rule to achieve green.

    Record the three exit codes in the SUMMARY.
  </action>
  <verify>
    <automated>cd packages && npm run build && test -f dist/index.js && test -f dist/index.jsx && test -f dist/index.d.ts && npx vitest run && npx eslint .</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages && npm run build` exits 0 and `dist/index.js`, `dist/index.jsx`, `dist/index.d.ts` all exist
    - `packages/dist/index.jsx` contains the stub export `MOON_SOLID_TOOLCHAIN_READY`
    - `npx vitest run` exits 0 with the toolchain test passing and zero "Client-only API called on the server side" occurrences in output
    - `npx eslint .` exits 0 with no rule disabled to achieve it
  </acceptance_criteria>
  <done>Build emits all three artifacts including the solid-condition .jsx; vitest passes a real Solid render; eslint is green.</done>
</task>

<task type="auto">
  <name>Task 2: Prove solid/no-destructure fires, then remove the probe to leave the repo green</name>
  <files>packages/src/_destructure-probe.tsx</files>
  <read_first>
    - packages/eslint.config.js (confirms solid/no-destructure is "error")
    - .planning/research/PITFALLS.md (Pitfall 1 — props destructuring kills reactivity)
    - .planning/phases/01-toolchain-foundation/01-CONTEXT.md (D-12)
  </read_first>
  <action>
    Create packages/src/_destructure-probe.tsx with a DELIBERATELY destructured Solid component to confirm the lint guard is active:
    ```tsx
    import type { Component } from "solid-js";

    // Intentional violation — must trip eslint-plugin-solid/no-destructure.
    const Probe: Component<{ label: string }> = ({ label }) => <span>{label}</span>;

    export default Probe;
    ```

    Run `cd packages && npx eslint src/_destructure-probe.tsx` and confirm it exits NON-ZERO with a `solid/no-destructure` violation reported. Capture the rule id from the output for the SUMMARY. (Use a command that records the failure without aborting, e.g. `npx eslint src/_destructure-probe.tsx; echo "exit=$?"`.)

    Then DELETE packages/src/_destructure-probe.tsx (`rm packages/src/_destructure-probe.tsx`) so the deliberate violation does not remain in the repo.

    Finally, re-run `cd packages && npx eslint .` and confirm it exits 0 (green) again, proving the repo is left clean.
  </action>
  <verify>
    <automated>cd packages && printf 'import type { Component } from "solid-js";\nconst P: Component<{ label: string }> = ({ label }) => <span>{label}</span>;\nexport default P;\n' > src/_destructure-probe.tsx && ! npx eslint src/_destructure-probe.tsx && rm src/_destructure-probe.tsx && npx eslint . && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint src/_destructure-probe.tsx` exits non-zero and reports a `solid/no-destructure` violation while the probe exists
    - `packages/src/_destructure-probe.tsx` does not exist after this task completes
    - `npx eslint .` exits 0 after the probe is removed
  </acceptance_criteria>
  <done>The no-destructure rule is proven active by a deliberate violation, the probe is removed, and eslint is green again.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full Solid build/test/lint pipeline, validated on the stub entry: `vite build --lib` emits ESM + preserved-JSX (`dist/index.jsx`, the `solid` condition) + `.d.ts`; `vitest run` passes a real Solid render with the browser-conditions fix; `eslint` is green; and `solid/no-destructure` was confirmed to fire on a deliberate destructure (then removed).</what-built>
  <how-to-verify>
    From the repo root:
    1. `cd packages && npm run build` — should exit 0; confirm `packages/dist/index.js`, `packages/dist/index.jsx`, and `packages/dist/index.d.ts` exist.
    2. `cd packages && npx vitest run` — should exit 0 with the toolchain test passing and NO "Client-only API called on the server side" message.
    3. `cd packages && npx eslint .` — should exit 0.
    4. Confirm `packages/src/_destructure-probe.tsx` does NOT exist and `packages/package.json` shows `@moondesignsystem/solid@3.0.0` with the `solid` export condition.
    This is the Phase 1 exit gate: the Solid toolchain is operational and the foundation (icons, helpers, types) is ported with zero React imports.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 1 complete, or describe any failing command.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/test/lint commands → developer machine | Validation runs build, test, and lint toolchains locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | temporary destructure probe file | mitigate | Probe is created, asserted, and immediately deleted within the same task; acceptance criterion verifies the file is gone and eslint is green |
| T-01-07 | Denial of service | build/test runaway | accept | Vitest runs a single trivial render; build targets a one-export stub; both complete in seconds |
</threat_model>

<verification>
- `npm run build` exits 0 and emits dist/index.js + dist/index.jsx + dist/index.d.ts
- `npx vitest run` exits 0 with no client-on-server error
- `npx eslint .` exits 0
- destructure probe trips solid/no-destructure, then is removed; eslint green again
- human checkpoint approves the Phase 1 exit gate
</verification>

<success_criteria>
- Build green emitting all three conditions' artifacts (including the solid .jsx)
- Test green with the resolve.conditions fix proven by a real Solid render
- Lint green, with solid/no-destructure proven active and the probe removed
- Repo left clean; Phase 1 toolchain + foundation validated end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-toolchain-foundation/01-03-SUMMARY.md`
</output>
