---
phase: 05-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/src/tests/atoms/IconButton.test.tsx
  - packages/src/tests/atoms/List.test.tsx
  - packages/src/tests/atoms/Menu.test.tsx
  - packages/src/tests/atoms/Pagination.test.tsx
  - packages/src/tests/atoms/Snackbar.test.tsx
  - packages/src/tests/atoms/Loader.test.tsx
  - packages/src/tests/atoms/Placeholder.test.tsx
  - packages/src/tests/atoms/Radio.test.tsx
  - packages/src/tests/atoms/SegmentedControl.test.tsx
  - packages/src/tests/IconButton.test.tsx
  - packages/src/tests/List.test.tsx
  - packages/src/tests/Menu.test.tsx
  - packages/src/tests/Pagination.test.tsx
  - packages/src/tests/Snackbar.test.tsx
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "IconButton, List, Menu, Pagination, Snackbar each have exactly ONE Solid test (atoms/) with legacy behavioral coverage merged"
    - "Loader, Placeholder, Radio, SegmentedControl atom tests verified at-parity"
    - "No flat legacy file remains for the 5 merged components"
  artifacts:
    - path: "packages/src/tests/atoms/IconButton.test.tsx"
      provides: "Single Solid IconButton test merged with legacy coverage"
      contains: "render(() =>"
    - path: "packages/src/tests/atoms/Pagination.test.tsx"
      provides: "Single Solid Pagination test merged with legacy page-change behavior"
    - path: "packages/src/tests/atoms/Menu.test.tsx"
      provides: "Single Solid Menu test merged with legacy coverage"
    - path: "packages/src/tests/atoms/Snackbar.test.tsx"
      provides: "Single Solid Snackbar test merged with legacy coverage"
    - path: "packages/src/tests/atoms/List.test.tsx"
      provides: "Single Solid List test merged with legacy For-rendering coverage"
  key_links:
    - from: "packages/src/tests/atoms/*.test.tsx"
      to: "packages/src/components/*.tsx"
      via: "render(() => <X/>)"
      pattern: "render\\(\\(\\) =>"
---

<objective>
Migrate and merge the legacy React tests for IconButton, List, Menu, Pagination, Snackbar into their existing Solid atom tests (one authoritative file each in `src/tests/atoms/`), preserving richer legacy coverage (handler invocation, page-change, list iteration, variant matrices). Verify atom-only tests for Loader, Placeholder, Radio, SegmentedControl are at parity. Delete the 5 flat legacy files.

Purpose: Phase 5 goal — ONE authoritative Solid test per component, parity ≥ legacy (D-01, D-07).
Output: 9 verified single-source atom test files; 5 flat legacy files deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tests/05-CONTEXT.md
@.planning/research/PITFALLS.md

<consolidation_decision>
CHOSEN DIRECTORY: `packages/src/tests/atoms/` (consolidate DOWN). Vitest `include` already covers `src/tests/atoms/**`, so merged tests run immediately — NO config edit here. EDIT the existing `atoms/X.test.tsx`, DELETE the flat legacy `src/tests/X.test.tsx`. DO NOT touch `vitest.config.ts` or `eslint.config.js` — plan 05-05 owns those.
</consolidation_decision>

<translation_rules>
Per D-03 / PITFALLS #14:
- `@testing-library/react` → `@solidjs/testing-library`
- `render(<X/>)` → `render(() => <X/>)` (MANDATORY)
- `jest.fn()` → `vi.fn()`, `jest.spyOn` → `vi.spyOn`; import `{ vi }` from "vitest"
- `className=` → `class=`; keep jest-dom matchers; rely on auto-cleanup (no manual cleanup)
- Reactivity is synchronous — assert immediately after `fireEvent`.
- Radio uses controlled inputs — fire `fireEvent.input`/`fireEvent.click` not `fireEvent.change` for value binding (D-03/PITFALLS #9).
</translation_rules>

<interfaces>
Reference Solid test style (already correct — match this):
@packages/src/tests/atoms/Button.test.tsx
Legacy React style being migrated FROM (sample):
@packages/src/tests/accordion.test.tsx
Auto-cleanup + matchers wiring:
@packages/src/tests/setupTests.ts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Merge legacy IconButton + List + Menu; verify Loader/Placeholder atom parity</name>
  <files>packages/src/tests/atoms/IconButton.test.tsx, packages/src/tests/atoms/List.test.tsx, packages/src/tests/atoms/Menu.test.tsx, packages/src/tests/atoms/Loader.test.tsx, packages/src/tests/atoms/Placeholder.test.tsx, packages/src/tests/IconButton.test.tsx, packages/src/tests/List.test.tsx, packages/src/tests/Menu.test.tsx</files>
  <read_first>
    - packages/src/tests/IconButton.test.tsx, packages/src/tests/List.test.tsx, packages/src/tests/Menu.test.tsx (legacy coverage to preserve)
    - packages/src/tests/atoms/IconButton.test.tsx, atoms/List.test.tsx, atoms/Menu.test.tsx (existing Solid coverage to merge INTO)
    - packages/src/tests/atoms/Loader.test.tsx, atoms/Placeholder.test.tsx (atom-only — verify)
    - packages/src/components/IconButton.tsx, List.tsx, Menu.tsx, Loader.tsx, Placeholder.tsx (parity targets)
  </read_first>
  <action>
    - IconButton, List, Menu: take the union of meaningful coverage from legacy + atom into the single `atoms/X.test.tsx`, translated per <translation_rules>. Preserve legacy handler/variant/iteration assertions (e.g. List item rendering via For, Menu item click handlers, IconButton onClick + size/variant classes). Drop exact-duplicate assertions. Then `git rm` each flat legacy file: `src/tests/IconButton.test.tsx`, `src/tests/List.test.tsx`, `src/tests/Menu.test.tsx`.
    - Loader, Placeholder: atom-only (no legacy). Verify render + key class/DOM coverage is adequate; enrich only if a clearly-supported primary behavior is unasserted. Do NOT delete.
  </action>
  <verify>
    <automated>cd packages && npx vitest run src/tests/atoms/IconButton.test.tsx src/tests/atoms/List.test.tsx src/tests/atoms/Menu.test.tsx src/tests/atoms/Loader.test.tsx src/tests/atoms/Placeholder.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The vitest run above exits 0
    - Flat files gone: `test ! -f packages/src/tests/IconButton.test.tsx && test ! -f packages/src/tests/List.test.tsx && test ! -f packages/src/tests/Menu.test.tsx`
    - `grep -rl "@testing-library/react" packages/src/tests/atoms/IconButton.test.tsx packages/src/tests/atoms/List.test.tsx packages/src/tests/atoms/Menu.test.tsx` returns nothing
    - `grep -n "jest\." packages/src/tests/atoms/IconButton.test.tsx packages/src/tests/atoms/List.test.tsx packages/src/tests/atoms/Menu.test.tsx` returns nothing
    - All five files contain `render(() =>`
  </acceptance_criteria>
  <done>IconButton/List/Menu single-source merged ≥ legacy; Loader/Placeholder verified; flat legacy IconButton/List/Menu removed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Merge legacy Pagination + Snackbar; verify Radio/SegmentedControl atom parity</name>
  <files>packages/src/tests/atoms/Pagination.test.tsx, packages/src/tests/atoms/Snackbar.test.tsx, packages/src/tests/atoms/Radio.test.tsx, packages/src/tests/atoms/SegmentedControl.test.tsx, packages/src/tests/Pagination.test.tsx, packages/src/tests/Snackbar.test.tsx</files>
  <read_first>
    - packages/src/tests/Pagination.test.tsx, packages/src/tests/Snackbar.test.tsx (legacy coverage to preserve)
    - packages/src/tests/atoms/Pagination.test.tsx, atoms/Snackbar.test.tsx (existing Solid coverage to merge INTO)
    - packages/src/tests/atoms/Radio.test.tsx, atoms/SegmentedControl.test.tsx (atom-only — verify)
    - packages/src/components/Pagination.tsx, Snackbar.tsx, Radio.tsx, SegmentedControl.tsx (parity targets)
  </read_first>
  <action>
    - Pagination: merge legacy page-change behavioral coverage (clicking next/prev/page → callback or active-page class update) into the atom file, translated to Solid (`vi.fn()`, synchronous assert after `fireEvent.click`). `git rm src/tests/Pagination.test.tsx`.
    - Snackbar: merge legacy coverage (render variants, show/dismiss) into the atom file. Snackbar may use `<Show>` — query for presence/absence accordingly. `git rm src/tests/Snackbar.test.tsx`.
    - Radio, SegmentedControl: atom-only (no legacy). Verify render + class + selection behavior. Radio.Group injects `name` via context and uses controlled inputs — if the test fires selection, prefer `fireEvent.click` on the radio; if it fires a value change use `fireEvent.input` not change (D-03). SegmentedControl uses context + createSignal selection — verify the active-segment assertion exists. Enrich only if a primary behavior is unasserted. Do NOT delete.
  </action>
  <verify>
    <automated>cd packages && npx vitest run src/tests/atoms/Pagination.test.tsx src/tests/atoms/Snackbar.test.tsx src/tests/atoms/Radio.test.tsx src/tests/atoms/SegmentedControl.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The vitest run above exits 0
    - Flat files gone: `test ! -f packages/src/tests/Pagination.test.tsx && test ! -f packages/src/tests/Snackbar.test.tsx`
    - `grep -rl "@testing-library/react" packages/src/tests/atoms/Pagination.test.tsx packages/src/tests/atoms/Snackbar.test.tsx packages/src/tests/atoms/Radio.test.tsx packages/src/tests/atoms/SegmentedControl.test.tsx` returns nothing
    - `grep -n "jest\." packages/src/tests/atoms/Pagination.test.tsx packages/src/tests/atoms/Snackbar.test.tsx` returns nothing
    - Pagination atom test exercises a page-change interaction (fireEvent.click) and asserts the result
    - All four files contain `render(() =>`
  </acceptance_criteria>
  <done>Pagination/Snackbar single-source merged ≥ legacy; Radio/SegmentedControl verified; flat legacy Pagination/Snackbar removed.</done>
</task>

</tasks>

<verification>
- `cd packages && npx vitest run src/tests/atoms/` passes for all files this plan touched
- No `@testing-library/react` import, no `jest.` reference in any file this plan owns
- The 5 flat legacy files owned by this plan are deleted
</verification>

<success_criteria>
- IconButton, List, Menu, Pagination, Snackbar each = ONE Solid test ≥ legacy coverage
- Loader, Placeholder, Radio, SegmentedControl atom tests verified at parity
- 5 flat legacy files removed via git rm
- All touched tests use render(() => <X/>), vi.*, @solidjs/testing-library
</success_criteria>

<output>
After completion, create `.planning/phases/05-tests/05-03-SUMMARY.md`
</output>
